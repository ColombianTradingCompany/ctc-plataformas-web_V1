Log_Documentacion_Interactiva_V15
Status: PENDING -- not yet wrapped. Only .0 (major) versions are ever compiled
into a real HTML file; this log is where every change lives, in plain text,
until a Version Wrap is actually invoked and produces Documentacion_Interactiva_V16.0(<sha>).html.
Covers: everything since Documentacion_Interactiva_V15.0(59c4217).html, which is
now the current real HTML snapshot (Log_Documentacion_Interactiva_V14.txt is
closed and left as historical record of what went into producing it).

## [2026-07-20] ⚠ NOTA ARRASTRADA — el walkthrough EN VIVO de la Arena sigue pendiente
- Es lo único grande que queda sin confirmar. El owner lo hará en una SESIÓN NUEVA.
- Estado del terreno, dejado a propósito para eso: los lotes "Pacamara Doble Fermentación 60h"
  y el de Catia siguen PRÍSTINOS en stage=ficha_completa, esperando en la columna EVA.
  Ninguno tiene inscripción todavía. No hay sesión de Arena abierta; las 2 que existen están
  completed con capacity=5.
- OJO con la compuerta: los dos lotes tienen eudr_risk_level = NULL, así que "Marcar Apto"
  se RECHAZA hasta resolver el EUDR del lote. La ruta corta para desbloquearlo, en el panel
  EUDR de la nueva checklist: indicios de ilegalidad = No, documentos disponibles = Sí
  (país Colombia = Estándar) ⇒ deriva 'insignificante' ⇒ el gate pasa a eudr_ready.
- Qué probar, en orden: EVA (checklist 5/5 + EUDR ✓ ⇒ Apto) → postular (KRA- acuñado, código
  KRX- opcional con descuento en vivo) → /bcp/nominados: confirmar pago + muestra → sondeo
  (aprobado ⇒ fila / suboptimal ⇒ retirado + cashback 80% + doc IA) → asignar a sesión →
  jornada v2 (descartes con grado por ronda, reveal_origin, veredicto ordenado) → finalize
  (TODOS galardonado, Red/Blue/Gold contrato, Black negociación, Tyrian subasta, membresía
  automática).
- ⚠ Para correr una jornada COMPLETA hacen falta 5 o 7 lotes en fila (startJornada exige el
  roster EXACTO). Con solo Pacamara+Catia no alcanza: o se siembran lotes de relleno, o el
  walkthrough se detiene en "En Fila".
- Si algo no se comporta como se describe, corregirlo ANTES de construir encima.

## [2026-07-20] ✅ CONFIRMADO POR EL OWNER en la sesión de V15 (no re-verificar)
- /ecp/herramientas carga bien tras el arreglo de props no serializables. "Works!"
- El arreglo de sesión funciona: ya no lo expulsa cada hora. "Seems so :)"
- El disco Agtron V7 no necesita cambios de copy. "Nothing, all good."

## [2026-07-20] 📌 CARRY-OVERS pendientes (no bloquean nada, siguen abiertos)
- Número Nequi real: falta llenar NEQUI en src/lib/arena/payment.ts. Mientras esté vacío, la
  vista del productor oculta las instrucciones de pago y apunta a info@ctcexport.com.
  NUNCA poner un número a medias.
- Cherry Picked Roast: faltan las cifras reales de los "honorarios de orquestación" por grado.
- GIF "Visit ctcexport.com": el archivo tiene el typo "Expore" horneado; regenerar.
- Confirmar con el owner el canal real del envío consolidado del sondeo (se asumió Fedecafé) y
  el del pago del cashback (se asumió Nequi).
- SEGURIDAD, aplazado por decisión del owner: "Leaked password protection" requiere plan Pro
  (la org está en FREE) y vive en Authentication → Providers → Email, NO en Database → Policies.
  Lo que SÍ se puede endurecer hoy gratis en esa misma pantalla: longitud mínima y tipos de
  carácter exigidos.
- RENDIMIENTO, con umbral: los 45 índices de claves foráneas que marca el linter son prematuros
  (la tabla más grande tiene ~306 filas). Revisar cuando alguna pase de ~10.000 filas,
  priorizando lots.producer_id, lots.finca_id, arena_inscriptions.lot_id, producer_comm_log.lot_id.

(commit del wrap V15: 59c4217 — el contenido corresponde a ese commit; todo lo que describe
está desplegado READY en producción.)

## [2026-07-20] Pase de feedback del owner tras el primer intento de walkthrough de la Arena
- SONDEO PARTIDO EN DOS PASOS (afecta DICT `sondeo`, `nominados`, escenario `arena`): el tablero
  Nominados pasa de 4 a 5 columnas — "Envío a Sondeo" (organizar muestra + consolidar en
  sondeo_batches + despachar) y "Resultados del Laboratorio" (solo lotes cuyo envío está
  cerrado/resultados; la separación se DERIVA del status del batch, sin nuevo valor de phase).
  recordSondeoResult ahora EXIGE envío despachado, y el resultado se registra con una planilla
  B2/B3 estructurada (nuevo componente compartido src/components/bcp/LabEvalEditor.tsx +
  src/lib/arena/labEvaluation.ts; se guarda en arena_inscriptions.sondeo_evaluation jsonb, el
  total SCA se vuelve sondeo_score) + archivo del laboratorio POR LOTE
  (createSondeoLotResultUploadUrl → sondeo_result_storage_path/filename). Migración:
  arena_v3_lab_registration_and_cert_verifications.
- ARENA COMO KANBAN (afecta nodo `n-bcp`, DICT `jornada`/`nominados`, escenario `jornada`):
  /bcp/arena rediseñado en 3 columnas Preparando (roster incompleto) / Agendada (roster lleno o
  jornada en curso — CADA café tiene su modal "Registro B2/B3" persistido en
  arena_sessions.cup_registrations jsonb vía saveCupRegistration) / Culminada (ganador).
  ⚠ La jornada NO se siembra todavía de cup_registrations — aplazado a la segunda pasada de la
  Arena que el owner pidió revisar.
- VERIFICACIÓN DE CERTIFICADOS A3/A4 (afecta DICT `eva`, `intake`): en el panel FT2·Certificados
  de EVA cada certificado declarado es una fila con su soporte EN LÍNEA + enlace al registro
  público del esquema (nuevo src/lib/certRegistry.ts, investigado 2026-07-20: Rainforest/FLOCERT/
  USDA OID/GLOBALG.A.P./CQI etc.) + veredicto Confirmado/No confirmado de BCP, corregible siempre,
  persistido en lots.cert_verifications (jsonb, protegido del productor por
  guard_lot_protected_columns — el guard aprendió la columna en la misma migración).
- PANEL EVA REORDENADO (DICT `eva`): FT muestra chip Apta/No Apta/Pendiente de la finca declarada;
  FT2 físico muestra los 10 atributos SCA completos + «No lo sé» explícito + enlace al directorio
  CQI de Q-Graders; EUDR: Complejidad bajo Etapas de custodia, chip de esquemas que cruza al panel
  FT2, la pregunta de mitigación es campo directo de BCP ("evaluación de BCP"), Responsable al
  final sin botón Corregir.
- UI BCP: columnas de kanban crecen a todo el ancho (.column flex:1 1 260px); las "Tareas
  pendientes de CTC" aterrizan en el elemento exacto (hash #lot-/#finca-/#lead-<id> abre el modal
  destino — anchorId en FincaModalRow/LeadModalRow); Kaffetal Club debajo de Nominados en el nav;
  la caja de código de campaña se CIERRA una vez aplicado un KRX- (lado productor Y lado BCP).
- DATOS: campaña "Fundadores" eliminada (sin uso, 1 código nunca emitido borrado con ella).
- El walkthrough EN VIVO sigue pendiente — el terreno quedó listo para reintentarlo con el flujo
  nuevo: Pacamara en "Resultados del Laboratorio" (su envío Sondeo Test 1 está cerrado), Catia en
  "Envío a Sondeo" (sin batch), 3 lotes QA de relleno en "En Fila" (productor
  qa-arena-filler@ctc-qa-test.co / borrar tras el walkthrough: primero
  producer_comm_log del producer 31ac070c-..., luego scripts/delete-qa-user.mjs).
(commit: 5cd4079)

## [2026-07-20] Insignia de versión + pie legal compartido en toda la plataforma
- MOTIVO: el owner preguntó "¿seguro que lo último está en vivo?" y no había forma de
  saberlo mirando la pantalla. Ahora la hay.
- NUEVO src/lib/version.ts — fuente ÚNICA. APP_VERSION="1.0" ⇒ se pinta "V1.0". El menor
  sube en el MISMO commit que despliega una tanda. next.config.ts copia
  VERCEL_GIT_COMMIT_SHA (7 chars) a NEXT_PUBLIC_BUILD_SHA y el title de cada insignia lo
  muestra: el SHA es la identidad real del build, la versión es la etiqueta legible.
  ⚠ NO es la versión de la documentación interactiva (esta numera snapshots de documento).
- NUEVO src/components/LegalFooter.tsx (+ .module.css) — barra legal compartida con el loro
  CTC en negro (nuevo asset public/images/shared/ctc-logo-parrot-black.webp, 240×268, 11 KB,
  recortado del reference_logos original; silueta negra pura con alfa real: 0 píxeles opacos
  no-negros, así que invierte limpio a blanco sobre el navy), reserva de derechos, letra
  pequeña legal (trilingüe es/en/de) y la insignia de versión. Dos tonos: dark para el pie
  navy de CTC Home, light para el resto.
- DÓNDE QUEDA: rail de las consolas (junto a "CTC Web Platform", afecta nodo n-consoles),
  CTC Home, Kaffetal Regal landing Y panel del productor (no tenía pie alguno), Cherry Picked
  (las 3), las 4 pantallas de acceso interno vía el nuevo InternalAuthShell
  (/login, /verify, /panel, /cambiar-contrasena) y las 2 páginas de cada socio.
- Para que la barra se apoye al fondo del viewport sin scroll, .wrap de auth.module.css y de
  hub.module.css cambió min-height:100vh por flex:1 1 auto (el shell es la columna 100vh).
- SIN DATOS INVENTADOS: el pie NO lleva NIT ni matrícula — no están en el repo. Van en
  LEGAL_ES/EN/DE cuando el owner los entregue. Tampoco enlaza política de privacidad porque
  no existe (sigue el pendiente GDPR con Resend como subprocesador).
- Contraste MEDIDO en cada fondo, todo AA: letra pequeña 5.58 sobre navy, 6.14 (blanco),
  6.40 (Cherry Picked), 7.90 (acceso y socios); reservas 14.26–18.31. La letra pequeña solo
  se atenúa (0.78) en el tono oscuro — al 0.78 sobre blanco medía 3.73 y REPROBABA AA.
(commit: d1202b2 · el NIT del pie llegó en d00d03b)

## [2026-07-20] Consolas internas usables en teléfono — rail plegable
- MOTIVO: el owner quiere operar el BCP desde el móvil. Medición del shell real a 375 px
  ANTES de tocar nada: .main salía 1100 px empezando en x=240, con 965 px de contenido
  RECORTADOS Y INALCANZABLES (html/body llevan overflow-x:hidden ⇒ ni scroll horizontal).
- CAUSA DE FONDO (no era el rail): min-width:auto por defecto en un hijo flex; el ancho
  intrínseco del kanban estiraba .main y nada podía encogerlo. La corrección es
  .main{min-width:0} — sin ella cualquier ajuste de anchos se vuelve a romper.
- NUEVO src/components/panel/PanelChrome.tsx (cliente) envuelve rail + <main>; PanelShell
  sigue siendo de servidor y solo le pasa los children. Estado de TRES valores a propósito:
  null = manda el CSS (visible ≥1024, cajón cerrado por debajo), true/false solo por decisión
  del usuario ⇒ el primer render de servidor y de cliente coinciden sin leer window ni
  localStorage durante el render.
  · <1024 px: barra superior con ☰; el rail es cajón fijo fuera de pantalla; lo cierran el
    fondo, elegir un enlace (delegación, sin efecto) o Escape. Conmutador de consolas subido
    a 44 px de alto (estaba en 36, se falla con el pulgar).
  · ≥1024 px: botón "Minimizar menú" + botón flotante para devolverlo; la preferencia de
    PLEGADO se recuerda (ctc-panel-nav-open). El abierto NO se guarda: haría aparecer el
    cajón encima del contenido al entrar desde un teléfono.
  · Minimizar levanta además el max-width:1100px de .main — sin eso el tablero ganaba 75 px
    (nada); con eso gana 255, una columna entera de kanban.
- Kanban en vertical: scroll-snap x mandatory + columnas al 86% del ancho y encabezado
  pegajoso, para pasar columnas con el dedo y saber siempre en cuál se está. Modales casi a
  pantalla completa bajo 720 px con el botón de cerrar a 40 px. Afecta DICT `panels`,
  `nominados`, `eva` y el nodo n-consoles (las tres consolas comparten el shell).
- ⚠ APRENDIZAJE DE ENTORNO: el panel de vista previa NO ejecuta transiciones CSS, así que una
  propiedad en transición se queda clavada en su valor inicial y getComputedStyle miente — el
  rail parecía no abrirse jamás, incluso forzando !important o estilo en línea. Para medir
  transformaciones aquí hay que neutralizar la transición primero (*{transition:none!important}).
  Medido así: cerrado x=-240, abierto x=0 con los 16 enlaces dentro de pantalla.
- Verificación: tsc + eslint + build limpios; /bcp /bcp/nominados /ecp /ocp siguen devolviendo
  307 a /login (las guardas intactas). El shell se midió montándolo en una ruta sonda temporal
  (el BCP exige 2FA y no se puede conducir automatizado), borrada al terminar.
(commit: ddc6ab6)

## [2026-07-20] Los tableros del owner: TODO el flujo BCP como kanbans + Admin Lock
- ⚠ PRIMERO, EL BUG: /bcp/nominados crasheaba ("This page couldn't load") — TDZ: batchStatusOf
  leía batchById declarado 10 líneas después; tsc no lo ve (referencia dentro de un closure) y
  el build tampoco (página dinámica, nunca se ejecuta). LECCIÓN OPERATIVA: cada página BCP
  tocada se RENDERIZA ahora en una ruta sonda temporal con datos reales antes de commitear.
- NOMINADOS (afecta DICT `nominados`, `sondeo`; escenarios `envio`/`arena`): columnas
  Embotellados (>5 días con pago o muestra pendientes) · Recién Nominados (≤5 días) · En Fila.
  ORDEN DE FASES INVERTIDO: pagado+muestra ⇒ phase='fila' (el POOL); los baches toman ≤30 lotes
  del pool (phase→'sondeo' mientras viajan); el aprobado VUELVE a 'fila' con su puntaje y solo
  con sondeo aprobado se asigna a sesión (assignLotToSession lo exige). Sin valores nuevos de
  phase — solo semántica.
- BACHES DE SONDEO (kanban nuevo bajo Nominados): abierto (Nuevo Sondeo, selección ≤30 desde En
  Fila) → planeado (Cerrar Bache; laboratorio + "Solicitud de Bache de muestras" imprimible,
  src/lib/arena/sondeoRequestPrint.ts, cata a ciegas por código) → pendiente (Bache Enviado
  EXIGE la prueba de confirmación de recibo subida; luego "recibido en Lab" y "pruebas
  entregadas") → registro (VARIAS planillas B2/B3 por lote — sondeo_evaluation pasó a LISTA,
  addSondeoEvaluation appendea, recordSondeoResult da el veredicto). Migración
  boards_v4_batches_seasons_admin_lock: lab_name/lab_contact/proof_*/received_at/delivered_at +
  estados nuevos (cerrado→pendiente, resultados→registro en datos vivos).
- LOTES: secciones Aptos / No aptos con filtro por TEMPORADA DE REGISTRO (lots.season_id +
  arena_inscriptions.season_id, backfill a Mitaca 2026). REGLA: máximo 2 temporadas por lote
  (validada en postularLote/postularOnBehalf via lotSeasonCount; UNIQUE(lot_id) hace que la
  re-postulación real sea segunda pasada — el tope queda de cimiento). Postular-en-nombre vive
  aquí ahora (los Aptos sin postular salieron de Nominados).
- PRODUCTORES como kanban: Marchitando/Nuevos/Primíparos/Establecidos/Activos — lógica PURA en
  src/lib/bcp/producerSegments.ts con criterios documentados (decisiones: "finca pidió
  revisión"≈tiene fincas; "lote pidió EUDR"=intake_step≥2 o enviado; "procesados"=finca
  aprobada+lote enviado; >7d en camino cae en Primíparos; recién llegado completo sigue en
  Nuevos) + scripts/qa-boards-check.mjs (19 checks). Tarjeta con FOTO DE PERFIL; el modal del
  productor enlaza fincas/lotes/Arena en curso/contratos con deep-links.
- FINCAS como kanban: Marchitando/Nuevas/En Proceso/Aprobadas/No Aprobadas (completitud EUDR =
  missingChecks()==0, video excluido; "En Proceso" = completa a cualquier edad — la definición
  del owner dejaba sin columna a la completa de 8 días). La tarjeta/modal de siempre.
- ARENA: SessionFunnel = el diagrama "Competition Structure" del owner (ciegas → R1 Red/Black →
  variedad/proceso → R2 Red/Blue → origen → final 1º/2º/3º) derivado del run_state. A CIEGAS
  POR DISEÑO: las identidades NO viajan al cliente en el render; "mirar bajo el capó" pasa por
  revealSessionIdentities + ADMIN LOCK (platform_settings.admin_lock, sha256, semilla "123",
  se cambia en ECP → Usuarios y Credenciales → tarjeta Admin Lock). Registros B2/B3 por taza en
  LISTA (varios por café) disponibles desde el principio; en jornada viva las tarjetas van por
  etiqueta de taza. Módulo EVALUACIONES eliminado (nav + ruta); la cola de reclamos de
  oficialización quedó embebida al pie de /bcp/arena (reviewEvaluationClaim vive).
- ⚠ SEGUNDA PASADA PENDIENTE (el owner la anunció): reconciliar la JORNADA con los tableros —
  hoy la jornada NO lee cup_registrations ni las planillas del sondeo. No construir encima sin
  esa revisión.
- Verificación: tsc + eslint + build limpios; qa-boards-check 19/19; qa-jornada-check 11/11;
  los 6 tableros renderizados con datos reales vía sonda (borrada); servidor frío sin errores.
  Datos staged: Catia → En Fila (sondeo pendiente), Pacamara → bache "Sondeo Test 1" en Sondeo
  Pendiente, 3 QA en En Fila con sondeo aprobado.
(commit: 450f511 · desplegado como V1.1 en 55702fa)

## [2026-07-20] La barra CANÓNICA del proceso en el stepper del productor (reclamo del owner)
- LotKanbanStepper seguía dibujando el orden VIEJO (EVA después de ARE, sin SON): pasó a la
  barra canónica FT·FT2·EUDR·VID → EVA → MUE·SON·ARE → GAL, con las flechas marcando los DOS
  puntos de equilibrio (EVA habilita el reporte EUDR; GAL compila y entrega la evaluación
  Q-Grader + la información de la Arena). Afecta DICT `stagelifecycle` e `intake`.
- El tramo pagado (MUE/SON/ARE) se deriva ahora de la INSCRIPCIÓN (fase + sondeo), su fuente
  de verdad desde la inversión de fases: MUE hecho = fase más allá de postulacion; SON activo
  en bache/pool-sin-resultado (y "no superado" en retirado); ARE activo con sondeo aprobado /
  sesión / fila_arena; GAL solo con galardonado. Los 4 chips de intake siguen leyendo
  intake_step (el fix anti-stage-empujado se conserva).
- Comentario en el componente: NO reordenar esta barra sin el owner — es cómo el productor y
  el BCP perciben el proceso entero.
- Verificado en vivo (cuenta QA): lotes fila+aprobado pintan …SON:done ARE:ACTIVO → GAL:pend.
(commit: pending)

## [2026-07-20] Herramientas configurables (Default/Plus), procedencia SCA en la Ficha, altura derivada
- HERRAMIENTAS (afecta DICT `herramientas`, nodos n-kaffetal / n-cherry / n-consoles): el reparto
  por superficie salió del código. catalog.ts define ToolsConfig {kr, cp, tier} + el arranque que
  reproduce el reparto histórico; lo administrado vive en platform_settings.tools_config y se
  edita en consola interna → Herramientas → Disponibilidad (ToolsAdmin). Esa página ahora lista
  TODAS las herramientas, no solo las internas.
  · DOS NIVELES: default = cualquier cuenta de la superficie; plus = con estatus.
  · ⚠ La regla de "Plus" es DECISIÓN POR DEFECTO nuestra, no instrucción del owner (se preguntó,
    no hubo respuesta): productor = Pasaporte del Kaffetal Club; comprador = membership_tier por
    encima de 'verde' (la escala verde→pinton→maduro arranca toda cuenta en verde, así que "tiene
    membresía" no distinguiría a nadie). Vive en UN sitio: loadToolAccess (lib/tools/toolAccess).
  · platform_settings es service-role-only ⇒ la superficie NO lee la config: pide su lista ya
    filtrada por server action (hook useToolAccess). Lo Plus que falta se anuncia, no se esconde.
  · Verificado en vivo en los dos sentidos: sin Pasaporte el Agtron marcado plus desaparece y
    sale el aviso; con Pasaporte reaparece. Config y membresía revertidas tras la prueba.
- FICHA (VISTA FINAL) — procedencia de cada puntaje (DICT `intake`, `eva`): la exportación lista
  TODOS los puntajes sensoriales con sus 10 atributos y un rótulo por fila: "Declarado por el
  productor (B2) · sin contrastar" (la autodeclaración nunca es oficial sola), "Evaluación CTC ·
  Contrastado por CTC", "Solicitud de oficialización · pendiente de contrastar / NO validado".
  Nuevo Lot.scaScorings poblado desde lot_evaluations (la consulta ahora trae sca_data,
  q_grader_reference, created_at). Probado en vivo con los tres estados a la vez.
- ALTURA DE FINCA derivada (DICT `intake`, `eudr`): nuevo src/lib/geo/elevation.ts — punto de
  referencia = CENTROIDE del polígono si existe (>4 ha), punto registrado si no (≤4 ha), y
  consulta a google.maps.ElevationService (mismo SDK y clave del mapa, sin dependencias nuevas).
  ⚠ El owner lo enunció al revés ("el punto para >4"): el polígono SOLO existe por encima de
  4 ha, así que el mapeo implementado es el único coherente — queda anotado por si quiere otra
  cosa. Escribir el campo a mano lo congela hasta "Volver a calcularla". Si la Elevation API no
  está habilitada en Google Cloud devuelve null y el campo sigue editable: nunca bloquea guardar.
- ⚠ SIN RESOLVER: el bullet "Kaffetal Klub" del paquete del owner llegó VACÍO. No se tocó nada
  del Club; queda pendiente saber qué iba ahí.
- Verificación: tsc + eslint + build limpios; qa-boards-check 27/27 (añadidos centroide,
  colineales, degenerados y selección punto-vs-polígono); qa-jornada-check 11/11.
(commit: pending · versión V1.3)

## [2026-07-20] Credenciales de socio: separar la identidad de acceso del buzón real
- SÍNTOMA: emitir una credencial en OCP → Socios fallaba con "Ese correo ya es una cuenta de
  productor (Kaffetal Regal)". El mensaje era CIERTO pero el bloqueo no debía existir: el campo
  se llamaba "Correo (su buzón real)" y se usaba a la vez como identidad de auth y como destino
  del envío, así que reutilizar una bandeja de entrada quedaba prohibido.
- ARREGLO (afecta DICT `socios`, `identidades`; nodo n-socios): partner_accounts gana
  delivery_email — el MISMO desdoblamiento que panel_users ya tenía desde 2026-07-15.
  · correo de ACCESO = identidad (fila en auth.users ⇒ única en la plataforma; puede ser una
    etiqueta sin buzón, p. ej. estudio-contenido@ctcexport.com). Sigue validándose contra
    profiles porque profiles.role es de un solo valor: convertir una cuenta pública en socio le
    quitaría su rol y dejaría huérfanas sus fincas/lotes/pedidos.
  · BUZÓN REAL = a dónde llega la invitación y los restablecimientos. NO se valida contra nada:
    se puede repetir entre credenciales y coincidir con una cuenta pública.
  · El formulario pasa a dos campos con su explicación; el error del correo de acceso ahora
    SUGIERE la salida (usar <nodo>@ctcexport.com y poner el buzón abajo).
  · invitePartner y resendPartnerCredential entregan a delivery_email || email; los correos de
    invitación y de restablecimiento dicen explícitamente "este mensaje llegó a X pero tu
    usuario es Y" cuando difieren — si no, el socio intenta entrar con la dirección receptora.
  · La lista de socios muestra "· entregas a <buzón>" solo cuando difiere.
- Migración: partner_accounts_delivery_email (columna + comment).
- Verificación: tsc + eslint + build limpios; SociosClient renderizado en una ruta sonda con
  datos de forma real (los dos campos, sus ayudas, y la lista mostrando/ocultando la entrega
  según difiera), sonda borrada.
(commit: pending · versión V1.4)

## [2026-07-20] Bugs del productor: respuesta de hilo (RLS) + orden Tenencia/Documento
- BUG GRANDE (afecta DICT `identidades`, `intake`; ya estaba en PRODUCCIÓN): responder un hilo en
  "Retroalimentación y ayuda" fallaba SIEMPRE con "No se pudo enviar su respuesta". Causa real:
  la política de INSERT de producer_comm_log (producer_comm_log_insert_own) validaba la
  pertenencia del padre con una subconsulta SOBRE SU PROPIA TABLA
  (parent_id IN (select id from producer_comm_log ...)); Postgres detecta recursión infinita al
  planearla y aborta — y como el WITH CHECK expande las TRES ramas del OR, TODA inserción del
  productor moría, no solo las de respuesta. Reproducido con JWT simulado
  (ERROR 42P17: infinite recursion detected in policy).
  ARREGLO (migración fix_producer_comm_log_insert_recursion): nueva función
  public.producer_owns_comm(comm_id, uid) SECURITY DEFINER + search_path fijo, que lee la tabla
  con RLS ya resuelta; la política usa esa función para la rama del padre (finca/lote no recursan,
  son otras tablas). Verificado: el insert pasa con JWT simulado Y en vivo en el navegador (la
  respuesta se envía, sale "Respuesta enviada a CTC ✓" y aparece en el hilo).
  ⚠ GOTCHA para el repo: una política cuya expresión consulta SU MISMA tabla recursa; envolver la
  subconsulta en una función SECURITY DEFINER rompe el ciclo.
- UI FINCA (FincaModal): "Documento de respaldo (PDF)" pasó a ir JUSTO debajo de "Tenencia de la
  tierra" (respalda esa declaración). "Infraestructura local" bajó debajo del documento.
- ⚠ SIN RESOLVER, pedí aclaración al owner: (a) "el blocker de los certificados no funciona" — el
  camino del código (pendingCertProofs + stripUnprovenCerts, compuerta SUAVE que avisa en FT2 y
  desmarca al envío final) se leyó correcto y no se pudo reproducir un fallo; falta saber si el
  owner espera un bloqueo DURO o ve otro comportamiento. (b) "bug con las áreas de verificación" —
  no se pudo identificar a qué campo/pantalla se refiere.
(commit: pending · versión V1.5)

## [2026-07-21] Diagrama de flujo del proceso completo en la landing de Kaffetal Regal
- Pedido del owner: un diagrama HTML/CSS que mapee el proceso COMPLETO del productor, colgado
  en la landing de KR como acordeón desplegable — entre las 5 tarjetas de pasos (que son un
  resumen simplificado, NO la guía del diagrama) y el acordeón EUDR, como en su boceto.
  El diagrama funciona además como CONTRATO DE ALINEACIÓN: su pie invita a corregir si algo
  no coincide con lo que el productor ve en su panel.
- NUEVO src/components/kaffetal-regal/ProcessFlow.tsx (+ ProcessFlow.module.css): diagrama
  trilingüe (es/en/de vía useLang) en columna única con espina dorsal (timeline), sin librerías.
  3 fases: Documentación (01 Cuenta → 02 Finca+EUDR con ramas Aprobada/No aprobada → 03 Ficha
  Técnica → hito EVA), Kaffetal Regal Arena (04 Postulación con regla máx. 2 temporadas → MUE
  pago+muestra a ciegas → SON Sondeo con ramas Aprobado/No superado(80% cashback) → ARE Sesión),
  Resultado (hito GAL + abanico de grados). Los DOS puntos de equilibrio van en dorado como
  hitos de entrega: EVA ("★ Entrega: su informe EUDR") y GAL ("★ Entrega: la evaluación del
  Q-Grader (perfil de taza + granulometría) y la información de su Arena"). Abanico de destinos
  por grado con los colores globales --t-*: Black (negociación directa), Red·Blue (contrato →
  catálogo Cherry Picked), Gold (contrato premium), Tyrian (subasta).
- ParticiparSection.tsx: nuevo InfoAccordion tone="accent" (flowTitle/flowSub en los 3 idiomas)
  insertado ENTRE la cuadrícula de pasos y el acordeón EUDR, envolviendo <ProcessFlow/>.
- Verificación: tsc + eslint limpios (entidades JSX escapadas en en/de); en navegador:
  posición correcta del acordeón, 9 nodos (01/02/03/EVA/04/MUE/SON/ARE/GAL), 2 hitos dorados,
  4 grados, estilos computados correctos (borde --line, punto de espina 12px --primary, hitos
  --accent, Tyrian #66023C), sin desborde horizontal a 375px (flow 286px), diccionario EN
  cotejado en vivo (fases Documentation/Kaffetal Regal Arena/Result, entregas traducidas).
  ⚠ Gotcha de verificación: al consultar estilos por className en módulos CSS, el formato del
  hash es "ProcessFlow-module__HASH__node" — un regex suelto puede casar el CONTENEDOR .nodes
  y dar lecturas falsas (borde negro, punto 0px); inspeccionar primero el className real.
(commit: pending · versión V1.6)

## [2026-07-21] Toggles de acordeón más grandes + con pulso, y botón a CP que sí navega
- Feedback de usuarios: el símbolo de despliegue de los acordeones es demasiado pequeño en
  todas las UIs y la gente se pierde la información de abajo. Se agrandó el chevrón y se le
  puso un PULSO de atención (escala 1→1.35 + opacidad) que SOLO corre cuando el acordeón está
  cerrado (nunca pelea con el giro-al-abrir) y se apaga con prefers-reduced-motion.
  Chevrones tratados: InfoAccordion (compartido: KR + ProcessFlow), .accordion global
  (globals.css), EcosystemSection y ServicesSection (CTC Home), EnviosSection/MuestrasSection/
  LotCard (Cherry Picked). El Cart de CP solo se agrandó (su giro vive en .closed, así que un
  pulso ahí chocaría con el transform). Al acordeón HIC de TratoSection, que NO tenía símbolo,
  se le añadió un chevrón pulsante.
  ⚠ GOTCHA (Turbopack CSS Modules): referenciar un @keyframes GLOBAL desde un .module.css NO
  funciona — el loader renombra el animation-name a un hash local que no existe, y la animación
  queda muda. `:global(nombre)` en la posición de VALOR tampoco compila ("Missed semicolon").
  Solución: definir el @keyframes ctcChevPulse LOCALMENTE en cada módulo (se renombra igual que
  su referencia, así que casan). En globals.css (que NO es módulo) el keyframe global sirve para
  .accordion. Verificado en navegador: getAnimations() muestra el pulso corriendo (scale ~1.35),
  al abrir animation:none + rotate(180deg).
- BUG (TratoSection, "Lo que hace CTC"): el botón "Conocer Cherry Picked" solo disparaba un
  toast demo y no navegaba. Ahora es un <a class="btn"> a https://cherry-picked.ctcexport.com
  (target _blank, mismo patrón que EcosystemSection). Se quitó useToast (ya no se usa).
(commit: pending · versión V1.7)

## [2026-07-21] El OTRO botón a CP (Hero) + la altura que sí se auto-calcula
- El botón "Conocer Cherry Picked" que seguía muerto NO era el de TratoSection (ese ya se
  arregló en V1.7) sino el del bloque "Lo que hace CTC / ¿Y qué es Cherry Picked?" del HERO
  (Hero.tsx, banda "DE LA MUESTRA AL CONTENEDOR"). Usaba <ToastButton> (toast demo). Ahora es
  <a class="btn btn-sm"> a https://cherry-picked.ctcexport.com (target _blank). Quitado el
  import de ToastButton (ya no se usa en Hero).
  ⚠ Había DOS botones "Conocer Cherry Picked" con el mismo texto en la landing de KR: el del
  Hero y el de TratoSection. Al arreglar uno, revisar el otro.
- ALTURA (Mis Fincas · FincaModal): estaba implementada pero NO auto-llenaba. Causa: lookupElevation
  usaba google.maps.ElevationService, que exige habilitar la Elevation API en Google Cloud —cosa
  que no estaba hecha— así que devolvía null en silencio y el campo nunca se llenaba (por eso el
  helper decía "Marque la ubicación abajo y se calcula sola" pero no pasaba nada).
  ARREGLO (src/lib/geo/elevation.ts): lookupElevation ahora consulta la Elevation API de
  Open-Meteo (https://api.open-meteo.com/v1/elevation?latitude=..&longitude=..) — pública,
  gratis, SIN clave y con CORS abierto, así que corre desde el navegador sin tocar Google Cloud.
  El resto del flujo (fincaReferencePoint → centroide/punto → efecto encadenado → setAlt) queda
  igual. Verificado: la API devuelve {"elevation":[538.0]} para Múnich, el fetch pasa CORS desde
  localhost y el parser da 538. El campo sigue sobrescribible a mano ("Volver a calcularla" resetea).
  ⚠ GOTCHA: features que dependen de una API de Google Cloud (Elevation, Places, etc.) fallan
  mudas si esa API no está habilitada en el proyecto — preferir un servicio keyless con CORS
  (Open-Meteo) cuando solo se necesita el dato y no la integración con el mapa.
(commit: pending · versión V1.8)

## [2026-07-21] Altura: botón explícito "Traer del mapa" en vez de auto-relleno
- Petición del owner: en vez de que la altura se calcule sola, poner un BOTÓN junto al campo
  para traer la altura de la selección actual / centro del polígono; así el productor la trae
  explícitamente o la escribe a mano.
- FincaModal: se quitó el useEffect que auto-derivaba (y el import de useEffect) y el estado
  altManual. Ahora hay una función pullAltitude() que, al tocar el botón «Traer del mapa ⛰»,
  consulta lookupElevation(refPoint.point) y rellena el campo; refPoint sigue siendo el centro
  del polígono si lo hay, o el punto marcado. El botón se deshabilita si aún no hay ubicación.
  Estado nuevo altErr para el caso de fallo ("No se pudo obtener la altura; escríbala a mano").
  Editar el campo a mano limpia altFrom (deja de decir que vino del mapa).
- Verificado con ruta sonda temporal (zz-finca-probe, borrada antes del commit) montando
  FincaModal con un punto real (Pitalito, Huila 1.8534,-76.0508): el botón aparece habilitado,
  al tocarlo rellenó "1270" y el hint pasó a "Traída del punto marcado". tsc + eslint + build limpios.
(commit: pending · versión V1.9)

## [2026-07-21] Consolas internas a ancho completo (se quita el tope de 1100 px)
- El owner reportó (otra vez) que en BCP quedaba una franja MUERTA a la derecha con el rail
  abierto. Causa: .main (panel.module.css) tenía max-width:1100px; en pantallas grandes el
  tablero se quedaba en 1100 y el resto del ancho se desperdiciaba (solo se recuperaba
  minimizando el rail).
- ARREGLO: se quitó el max-width de .main. Las consolas internas (BCP/ECP/OCP) son sobre todo
  kanbans anchos (Fincas, Lotes, Nominados, Arena), así que el tablero ahora usa todo el ancho
  disponible. Los pocos bloques que sí quieren medida de lectura fijan su propio max-width
  (redactor del buzón 640, tarjeta de acceso 380), así que no se afectan. Se limpió el
  .navClosed .main{max-width:none} (ya no hay tope que levantar; minimizar solo esconde el rail).
  Solo lo usan las 3 consolas internas (layouts de bcp/ecp/ocp vía PanelChrome), todas con 2FA
  —no drivables en navegador—; build limpio.
(commit: pending · versión V1.10)

## [2026-07-21] Productores: incluir cuentas con huella de productor (no solo role='producer')
- El owner no veía el productor "CTCx Colombian Trading Company SAS" en BCP → Productores.
  Diagnóstico (SQL): esa es su PROPIA cuenta (ctcexportmain@gmail.com, id 30c49414…), con
  role='bcp_admin' pero con perfil de productor (NIT 9014834257) y 3 fincas + 2 lotes reales.
  El tablero filtraba .eq('role','producer'), así que la excluía — sus fincas salían en
  Fincas/Lotes pero el productor era invisible en Productores (inconsistencia).
- ARREGLO (productores/page.tsx): se quita el filtro por rol en la consulta y se filtra en
  memoria por HUELLA de productor: role='producer' O tiene producer_profiles O posee fincas/lotes.
  Así aparece la cuenta del dueño (admin que además opera como productor de prueba) y quedan
  fuera los administradores y compradores puros sin huella. Validado por SQL: el set incluye los
  10 productores + la cuenta del dueño, y NO incluye a gvb/gvg (admins puros) ni al buyer.
  tsc + eslint + build limpios (BCP con 2FA, no drivable en navegador — verificado por SQL).
(commit: pending · versión V1.11)

## [2026-07-21] Admin Lock: setter resiliente (upsert) + camino de primera vez
- El Admin Lock (candado suave que revela identidades de sesiones de Arena a ciegas, vive en
  platform_settings.value = {hash:"<sha256hex>"}, key='admin_lock') no se podía ESTABLECER en
  un entorno sin la semilla — como pasó hoy en el proyecto sjznkzvefqfcysczllli, donde la fila
  'admin_lock' no existía. setAdminLockPassword hacía .update().eq('key','admin_lock'): sin fila,
  afecta 0 filas en silencio, así que el owner nunca podía crear el candado desde ECP → Usuarios
  y Credenciales, y verifyAdminLock devolvía "El Admin Lock no está configurado."
- ARREGLO (adminLockActions.ts): (1) .update() → .upsert({key,value,updated_at,updated_by},
  {onConflict:'key'}) — mismo patrón que saveToolsConfig sobre esta misma tabla; funciona con o
  sin semilla. (2) Camino de primera vez: si aún no hay hash, se permite establecer el candado
  sin exigir la "actual en mano" (no hay contraseña previa que pedir), gated en is_owner. Si ya
  hay hash, se sigue exigiendo que la actual coincida. (3) Se propaga el error del upsert
  ("No se pudo guardar el Admin Lock.") en vez de asumir éxito.
- Toca el server action del Admin Lock (BCP/ECP, 2FA — no drivable en navegador). tsc limpio.
  Verificación end-to-end pendiente: borrar la fila (delete from platform_settings where
  key='admin_lock') y establecerla como owner desde la UI.
(commit: pending · versión V1.12)

## [2026-07-21] Corrección FUNDAMENTAL del flujo Sondeo → Arena (nueva fase 'arena')
- El owner corrigió mi malentendido: tras «En Fila» un lote NO va directo a la Arena; pasa por
  el Sondeo (Baches). En Fila = SÓLO espera de bache. Al superar el Sondeo y quedar «Registrado»
  + Apto en el Registro de Sondeo, el lote SALE de Nominados y aparece en el módulo Arena, desde
  donde se bloquea en una sesión. (No Apto → retirado + cashback, sin cambios.)
- Antes, recordSondeoResult(aprobado) devolvía el lote a phase='fila' y Nominados mostraba ahí
  mismo un control de "asignar a sesión" — mezclaba "esperando sondeo" con "listo para sesión".
- CAMBIO: nueva fase de inscripción **'arena'** (sondeado y aprobado, esperando sesión):
  · migración arena_inscriptions_add_arena_phase: se amplió el CHECK de phase para incluir 'arena'.
  · nominadosActions.recordSondeoResult(aprobado): phase 'fila' → 'arena' (audit + comm al productor
    "clasificado para la Arena").
  · assignLotToSession: ahora exige phase='arena' (antes 'fila'); se invoca desde el módulo Arena.
  · nominados/page.tsx: En Fila queda SOLO con pendientes; se quitó AssignSessionControls y toda la
    plomería de openSessions; el query no trae 'arena' → los aptos desaparecen de Nominados.
  · arena/page.tsx: nuevo pool «Aptos — listos para sesión» (lee phase='arena') con el control de
    asignación; la pista de «Preparando» ahora apunta a ese pool, no a Nominados.
  · inscriptions.ts (tipo + PHASE_LABEL 'Clasificado a Arena'), data.ts (unión de fase del panel),
    productores ACTIVE_PHASES (+'arena'), AppDashboard (fila = solo espera de bache; rama 'arena' =
    "clasificado"), LotKanbanStepper (ARE activo con phase='arena'/'sesion', no con fila+aprobado).
  · Datos: los 3 fillers ya aprobados se migraron fila → arena.
  ⚠ GOTCHA: arena_inscriptions.phase SÍ tiene CHECK constraint — una consulta multi-statement me
    ocultó el primer resultado y creí que no existía; confirmar constraints con una consulta sola.
- Verificado: tsc + eslint + build limpios; SQL confirma el enrutamiento (Nominados = Catia/Pacamara;
  Arena Aptos = Bourbon/Geisha/Tabi). BCP con 2FA, no drivable en navegador.
(commit: pending · versión V1.12)

## [2026-07-21] Datos colgados de Arena + poder eliminar sesiones y baches
- CONFUSIÓN reportada por el owner: en el popup de Productores, Pacamara y Catia salían "EN SESIÓN
  DE ARENA" (lots.stage='fila_arena') pero no aparecían en ninguna sesión — estaban en Nominados
  (Sondeo / En Fila). Causa raíz: assignLotToSession pone stage='fila_arena' al bloquear en sesión,
  pero NADA lo revierte al desmontar una sesión (no existía "eliminar sesión"); al revertir a mano
  la sesión de prueba quedaron los 5 lotes con stage fantasma.
- AUDITORÍA (SQL): los 5 lotes tenían stage='fila_arena' con in_sessions=0. FIX inmediato: se
  reseteó stage 'fila_arena' → 'apto' para lotes que no están en ninguna sesión. Ahora el popup
  muestra "Apto" (correcto), coherente con su phase (Aptos/En Fila/Sondeo).
  También quedaron a la vista 2 sesiones basura (una vacía d4e27ff8; otra vieja 202aac4a con
  "Caturra Honey") y el bache de prueba "Sondeo Test 1".
- FEATURE: eliminar sesiones y baches, con rollback que impide que se repita el dato colgado:
  · arenaActions.deleteArenaSession: devuelve el roster al pool de Aptos (inscripción sesion/competido
    → 'arena'; lote fila_arena/evaluado/galardonado → 'apto' sin grado), borra arena_scores y el
    roster, elimina la sesión. Botón «Eliminar sesión» (con confirmación) en cada tarjeta de sesión.
  · nominadosActions.deleteSondeoBatch: los lotes SIN veredicto (phase='sondeo') vuelven a «En Fila»
    (bache y planillas limpiados); los ya juzgados solo sueltan el puntero; elimina el bache. Botón
    «Eliminar bache» (con confirmación) en cada tarjeta de bache.
  · Componentes cliente DeleteSessionButton (ArenaBoardClient) y DeleteBatchButton (NominadosClient),
    patrón resultado-inline + window.confirm con resumen de lo vinculado.
- Verificado: tsc + eslint + build limpios; SQL confirma los 5 stages en 'apto'. BCP 2FA — no drivable.
(commit: pending · versión V1.13)

## [2026-07-21] Reorganización de las consolas internas (paso previo a "Mapa de Trabajo")
- Limpieza previa (SQL, con el mismo rollback de los botones nuevos): se borraron 2 sesiones
  basura (vacía + una vieja con "Caturra Honey") y el bache de prueba "Sondeo Test 1"; Pacamara
  volvió a En Fila; Caturra se reseteó a 'apto' sin grado. Y se corrigieron 5 stages fantasma
  ('fila_arena' sin sesión → 'apto'). Estado limpio: En Fila = Catia + Pacamara; Aptos = 3.
- BCP: el menú lateral pasó a TRES submenús sin encabezado, separados por un divisor
  (PanelNavGroup.label ahora opcional; PanelSidebar dibuja el divisor .navGroup; consoles.ts):
  · Comercial: Panel · Kaffetal Club · Catálogo Cherry Picked
  · Cadena: Productores · Fincas · Lotes
  · Competencia: Nominados · Arena · Galardonados (NUEVO)
- NUEVO módulo Galardonados (/bcp/galardonados): todos los lotes stage='galardonado' organizados
  por Grado CTC (Black/Red/Blue/Gold/Tyrian, colores --t-*), con link al lote. Hoy: 1 (Castillo
  Natural · Blue).
- Catálogo Cherry Picked absorbe Contratos y Subastas Tyrian: ya no son entradas del menú, viven
  como PESTAÑAS (nuevo CatalogoTabs, usePathname) al tope de catalogo/contratos/subastas. Las
  rutas y su lógica siguen intactas (contratos/[id], contratos/humedad, subastas=ComingSoon).
- Buzón de entrada → ECP: se movió la carpeta buzon/ + buzonActions.ts a ecp/(app); page usa
  requireConsoleAccess("ecp"); revalidatePath → /ecp/buzon; import de estilos apunta al
  shared.module.css del BCP por ruta absoluta (ECP no tiene el suyo). Entrada nueva en el nav ECP.
- Leads CTC Home → OCP: carpeta leads/ + leadsActions.ts a ocp/(app); revalidatePath → /ocp/leads;
  el Panel BCP sigue mostrando las tareas de leads pero enlaza a /ocp/leads. Entrada nueva en OCP.
  ⚠ GOTCHA: las acciones (buzonActions/leadsActions) usan requireActiveAdmin (agnóstico de consola),
    así que mudarlas no rompe permisos; solo la page del buzón gateaba por consola.
- Verificado: tsc + eslint + build limpios; el manifiesto de build lista /bcp/galardonados,
  /ecp/buzon, /ocp/leads; sin referencias colgadas a /bcp/buzon ni /bcp/leads.
- SIGUIENTE PROYECTO (no incluido aquí): módulo ECP "Mapa de Trabajo" — mapa de datos interactivo
  (ver/editar-cosmético/exportar PDF) al estilo del diagrama FT→GATE→FT2.
(commit: pending · versión V1.14)

## [2026-07-21] NUEVO módulo ECP "Mapa de Trabajo" (mapa de datos/UI interactivo)
- Petición del owner: un mapa visual EXACTO de cómo las tablas se construyen unas sobre otras a
  través de las compuertas del proceso (FT→FT2→EUDR→EVA→MUE→Sondeo→Arena→GAL + comercio/club/leads)
  y EN QUÉ UI(s) se muestra cada tabla; al estilo del boceto (bloques Schema/Table + Decisión,
  bandas de etapa al fondo, flechas). Tres modos: Ver / Editar (cosmético) / Exportar PDF.
- Decisiones (confirmadas con el owner): SVG a mano SIN librería (coherente con el ethos del repo,
  p.ej. ProcessFlow puro CSS); yo pre-autoré una semilla curada precisa que el owner reordena.
- src/lib/workmap/schema.ts: tipos (MapStage/MapNode/MapEdge/WorkMapConfig), toWorkMapConfig
  (normalizador para round-trip seguro), y DEFAULT_WORK_MAP — semilla con ~33 tablas + 4 decisiones
  (compuertas EUDR/EVA/Sondeo/Grado) + ~29 aristas + tags de UI por tabla, organizadas en 13 etapas.
- src/app/ecp/(app)/workmapActions.ts: getWorkMap (requireActiveAdmin) / saveWorkMap (owner-only,
  upsert a platform_settings.work_map, audit) — mismo patrón que toolAccess.ts.
- src/app/ecp/(app)/mapa/{page.tsx, WorkMapCanvas.tsx, mapa.module.css}: lienzo SVG con pan (arrastrar
  fondo) + zoom (rueda/＋－/Ajustar); Ver muestra las UIs de un nodo al tocarlo; Editar (solo owner)
  arrastra nodos, edita etiqueta/tipo/etapa/UIs, agrega Tabla/Decisión, Conectar (origen→destino),
  Borrar, Mapa base, Guardar; Exportar abre una ventana nueva autocontenida (colores concretos, no
  variables CSS) y llama window.print() para "Guardar como PDF" (vista actual o mapa completo).
- consoles.ts: entrada "Mapa de Trabajo" (ownerOnly) en el grupo ECP · IT y Plataforma.
- Verificado con ruta sonda temporal (zz-map-probe, borrada antes del commit) en el navegador:
  render (33 tablas, 4 decisiones, 29 aristas, bandas), Editar, arrastre (x 536→683 finito con
  viewport real), agregar nodo, inspección de UIs en modo Ver; sin errores de consola. tsc+eslint+build
  limpios; el manifiesto lista /ecp/mapa.
  ⚠ GOTCHA: el preview pane puede dar viewport 0×0 (getBoundingClientRect().width=0), lo que hacía que
    scaleX() diera Infinity y el nodo saltara a x=Infinity; se blindó scaleX contra ancho 0. En un
    navegador real (viewport 1400×850) el arrastre da coordenadas finitas correctas.
- MVP: entrega los 3 toggles. Fuera de alcance (futuras iteraciones): auto-diff contra el esquema vivo,
  detalle de columnas por nodo, y múltiples snapshots nombrados.
(commit: pending · versión V1.15)

## [2026-07-21] Mapa de Trabajo · iteración de feedback del owner
- PDF export no funcionaba (ventana nueva bloqueada / SVG en blanco por pérdida de namespace).
  ARREGLO: impresión EN LA MISMA página — se clona el SVG vivo (conserva namespace + colores
  concretos) dentro de un contenedor fijo .printArea que solo se ve con @media print (visibility:
  hidden en body, visible en .printArea); window.print() con @page A4 apaisado. Sin pop-up, sin
  ventana nueva. Verificado: el clon conserva namespaceURI SVG (causa del blanco anterior).
- Conectores desde una decisión ahora permiten escribir el RESULTADO: al seleccionar una arista se
  abre el panel "Conector" con input de rótulo + selector de tono (verde/rojo/gris/neutro) + borrar;
  al crear una arista (Conectar) se auto-selecciona para escribir el resultado enseguida.
- UIs pasa a DROPDOWN: el catálogo UI_CATALOG (agrupado por superficie KR/CP/BCP/ECP/OCP/otros) se
  elige desde un <select> con optgroups; las elegidas se muestran como chips con quitar; además un
  input "otra UI…" para agregar a mano (Enter).
- NUEVO tipo de bloque "Alto del proceso" (stop): pastilla roja suave para los puntos donde un lote
  sale del flujo. Botón "＋ Alto"; también en el selector de tipo del panel.
- CORRECCIÓN de datos (owner): un lote No Apto del Sondeo NO recibe purchase_contract; recibe
  resultado + feedback + 80% de cashback. Se cambió la arista sondeo_gate→purchase_contracts por
  sondeo_gate→retiro_stop (nuevo Alto "No Apto · resultado + feedback + 80% cashback"). Se agregaron
  también los Altos de EUDR no apta y EVA No Apto con sus aristas rojas.
  ⚠ react-hooks/purity marca Date.now() en un handler ("Cannot call impure function"); se cambió la
    generación de ids a un contador en useRef (newId) con prefijo "u" para no chocar con la semilla.
- Verificado con sonda (borrada antes del commit): stops renderizan, panel de conector con
  rótulo+tono, dropdown de UIs (6 optgroups + chips), aristas No Apto→Alto; sin errores de consola;
  tsc+eslint+build limpios.
(commit: pending · versión V1.16)

## [2026-07-21] Mapa de Trabajo · PDF salía en 2 páginas idénticas
- Causa: el contenedor de impresión usaba position:fixed, y en impresión un elemento fixed se
  REPITE en cada hoja; como el resto de la app (oculta con visibility:hidden) seguía ocupando
  alto, el documento medía 2 páginas y el fixed se imprimía en las dos.
- ARREGLO: el .printArea ahora se monta en <body> vía React portal, y en @media print se oculta
  TODO lo demás con `body > :not(.printArea){display:none}` (no visibility, no fixed) — el
  documento colapsa a UNA sola página con solo el mapa. El SVG va width:100% height:auto
  (max-height:100vh) para caber a página completa apaisada.
  Verificado con sonda: printArea es hijo directo de <body>; la app sigue renderizando; el clon
  entra al printArea. tsc+eslint+build limpios.
  ⚠ El patrón "mounted flag" para portales SSR-safe choca con react-hooks/set-state-in-effect;
    se desactivó la regla en esa línea (uso legítimo: SSR y primer render de cliente sin portal,
    el efecto lo habilita tras hidratar).
(commit: pending · versión V1.17)

## [2026-07-21] Mapa de Trabajo v2 · Base autoritativo + Propuestas + reconstrucción fiel
- Reencuadre del owner: el mapa DEBE reflejar el sistema real, y "Guardar" ya no sobrescribe el
  Base — guarda una PROPUESTA (iteración con nombre) que el owner diseña para que CTC la implemente.
  Base = lo que ES (código, fuente de verdad); Propuesta = lo que el owner QUIERE. Lingua franca.
- AUDITORÍA del pipeline real (actions.ts, arenaActions.ts, nominadosActions.ts, arena/run,
  evaluationActions.ts) → correcciones al Base:
  · Se AGREGÓ VID (video del lote: lots.video_asset_id + media_assets), parte del tramo gratis.
  · EVA es un HITO/entrega que lee lots(eva_checklist/eva_verdict)+fincas → apto/no_apto — NO
    lot_evaluations (mi mapa v1 lo cableaba mal). markLotApto/markLotNoApto.
  · lot_evaluations se recolocó como REGISTRO de puntajes SCA (reclamos del productor + planillas
    de jornada), surtido por arena_scores; se muestra en BCP·Arena (reclamos).
  · EVA y GAL ahora son bloques "hito" dorados (los dos puntos de equilibrio/entrega).
  · Bandas tintadas: tramo GRATIS (FT→EVA, verde) vs PAGADO (MUE→GAL, dorado) vs soporte (neutro).
  · UI tags cotejados contra el código.
- Modelo Base/Propuestas:
  · Migración work_map_proposals (id, name, note, config jsonb, created_by, timestamps); RLS
    service-role-only (sin acceso anon/authenticated). Se retiró el guardado único
    platform_settings.work_map.
  · workmapActions: getBaseMap (DEFAULT_WORK_MAP, solo lectura) · listProposals · getProposal ·
    saveProposal (owner, crea/actualiza) · deleteProposal (owner).
  · WorkMapCanvas: barra de FUENTE (Base solo-lectura / propuestas / ＋Nueva propuesta); editar solo
    en borrador o propuesta; Guardar → diálogo nombre+nota (nueva) o actualiza la propuesta; lista
    con cargar/borrar. Nuevo tipo de bloque "Hito/entrega" (＋Hito) y stage.tint.
  ⚠ Un archivo "use server" solo exporta funciones async — el tipo ProposalMeta vive en schema.ts
    (no en workmapActions).
- Verificado con sonda (borrada antes del commit): Base con VID + EVA/GAL hito + bandas tintadas;
  Base solo-lectura; ＋Nueva propuesta → borrador editable; diálogo Guardar; hito dorado (#FBF1DC);
  round-trip de work_map_proposals por SQL (insert→list→delete). tsc+eslint+build limpios.
(commit: pending · versión V1.18)
