Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V19.0(4e0e9ab).html
(CTC Web Platform V2.23, 2026-07-27). Cada entrada se compila al próximo Version Wrap (V20).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio aprendido en el wrap de V19: registrar la entrada AL TERMINAR el trabajo,
no "después". El backend real del Directorio del Café (3 commits del 2026-07-24) nunca
se anotó y V18.0 salió publicado describiéndolo como maqueta.

---
## [2026-07-29] F3 de la reestructura EUDR: la DDS con snapshot congelado (+ cierre de arrastrados)
- Tercera y última fase núcleo de docs/EUDR_RESTRUCTURE_PLAN.md (F4/blends queda
  diferida hasta que existan). La DDS NO es un certificado: nadie la emite y no
  vence — su artefacto es la referencia + código de verificación que genera el
  Information System de la UE al presentar.
- **Migración `f3_lot_dds_columns`**: `lots.dds_reference / dds_verification_code /
  dds_filed_at / dds_snapshot` (jsonb). El snapshot congela lo que el Art. 12
  obliga a poder explicar 5 años después: parcelas aportantes CON GEOMETRÍA,
  claims derivados con cobertura, arquetipo, ventana de cosecha, custodia y el
  nivel de riesgo país FECHADO al presentar (si el benchmarking cambia, el lote
  archivado se explica solo).
- **`registerLotDds(lotId, ref, code)`** (Server Action BCP): exige Sello listo
  (lotEudrGate por aportes) y ventana de cosecha; congela el snapshot y deja
  audit_log (`dds_registered`). El snapshot es INMUTABLE: si ya existe, la acción
  solo corrige referencia/código (`dds_corrected`) — nunca recalcula lo declarado.
  UI: botón "Registrar DDS" en la Lista de /bcp/lotes (lotes en el camino
  post-intake); registrada muestra "DDS ✓ <ref>" y el clic corrige el typo.
- **Sello imprimible**: nueva fila "Declaración de Diligencia Debida (DDS)" con
  referencia, código y fecha de registro, cuando existen.
- **Arrastrados cerrados**: `earthKml` (KML y GeoJSON) emite ahora UNA geometría
  POR PARCELA (nota "un polígono no puede cubrir varias parcelas"; el formato por
  Feature es el que acepta TRACES) con la geometría de la finca como respaldo
  legacy; la ruta KML de BCP y la descarga GeoJSON del editor cargan las
  parcelas. Y el contador "EUDR Aptas" del dashboard del productor usa la misma
  regla por parcelas que el FincaModal y approveFinca (se retiró la regla legacy).
- Verificación: tsc + eslint limpios; QA puro 26/26 sigue verde; panel del
  productor cargado sin errores de consola ni de servidor. El botón de la DDS no
  es navegable en automático (BCP = 2FA real, gotcha documentado): la acción se
  verificó por revisión + tipos; la lógica del snapshot es el motor puro ya
  testeado.
- Con F1+F2+F3, el flujo pedido por el owner queda completo de punta a punta:
  parcelas (Art. 9) → certificados con vigencia en la finca → aportes + arquetipo
  y claims derivados en el lote → DDS registrada con registro congelado de 5 años.
  Material para el wrap V20: nodos n-kaffetal/n-bcp/n-db y entradas DICT nuevas
  (parcela, credencial/claim derivado, DDS/snapshot).
(commit: c9fdab0)

## [2026-07-29] F2 de la reestructura EUDR: el lote como composición (aportes + claims derivados)
- Segunda fase de docs/EUDR_RESTRUCTURE_PLAN.md. El lote pasa a ser un conjunto de
  APORTES PONDERADOS de fincas con VENTANA DE COSECHA; el arquetipo y los sellos se
  DERIVAN — nadie vuelve a marcar una casilla de certificado en la Ficha.
- **Migración `f2_lot_contributions_and_harvest_window`**: tabla `lot_contributions`
  (UNIQUE lote+finca, peso opcional; guard: el productor solo la toca en
  borrador/ficha_completa) y `lots.harvest_from/to`. Backfill desde `finca_id` +
  `datasheet->additional_estate_ids`.
- **`src/lib/lotComposition.ts`** (puro): `deriveArchetype` (conteos |F|/|M|/|D|/|R|/
  |C| — el radio origin_category MUERE; municipio desconocido no se asume vecino;
  tabla mínima de región cafetera para el Eje) y `deriveClaims` (prueba temporal
  cosecha ⊆ vigencia — NO "hoy"; fail closed bajo 100%; finca bloqueante NOMBRADA
  con razón; % incognoscible (null) si faltan pesos en cobertura parcial; custodia
  IP como constante del proceso CTC — decisión del owner). QA:
  `scripts/qa-claims-check.mjs`, 26/26 (incluye el ejemplo 420/380/200 ⇒ 80% del doc).
- **Ficha A2 reescrita**: "¿De qué fincas sale este café?" (kg por finca cuando hay
  más de una) + "¿Cuándo se recolectó?" + chip de arquetipo CALCULADO con ⓘ. La
  finca primaria sigue alimentando estate/país/depto/msnm/geo (lectores legacy
  intactos). `seedContributions` siembra datasheets pre-F2. `saveFicha` espeja a
  columnas harvest_* y sincroniza lot_contributions (diff, no churn).
- **A3/A4**: A3 = Reconocimientos & Narrativa (los premios SÍ son del lote — Cup of
  Excellence es de un café en un año, no de una finca) con puntero a Mis fincas →
  Certificaciones. A4 = "Sellos del Lote (derivados)": panel de solo lectura por
  esquema — ✓ sello (verificado/declarado) o cobertura + finca bloqueante y razón.
  Se retiraron `stripUnprovenCerts`, la compuerta suave de FT2 y la caja de
  soportes pendientes (obsoletos: la prueba vive en la finca con vigencia).
- **`eudrGate` (server)**: lee lot_contributions — TODAS las fincas aportantes deben
  tener Visa, no solo la primaria (fallback finca_id para lotes pre-F2).
- **EVA (BCP /bcp/lotes)**: filas nuevas por lote — "Tipo de lote (calculado)",
  "Recolección" y "Sello X (derivado)" con cobertura y bloqueantes; nada que
  digitar, solo contrastar. Los certItems legacy del datasheet siguen visibles.
- **Sello imprimible (`LotEudrCertDoc`)**: imprime SOLO claims al 100% ("Sellos de
  certificación (derivados)"), + tipo de lote calculado y ventana de recolección;
  el texto legacy eudr_cert_scheme queda como "histórico". La ruta resuelve el
  origen por aportes (fallback primaria).
- Verificación: tsc + eslint limpios; 26/26 del QA puro; e2e con cuenta QA en la
  app real (finca + punto en mapa + cert Rainforest con vigencia → lote → A2:
  elegir finca ⇒ chip SINGLE ESTATE, fechas de cosecha → Guardar ⇒ SQL:
  harvest_from/to en columnas y el aporte espejado en lot_contributions con la
  finca correcta); cascada limpia al borrar la cuenta (0 huérfanos). A4 en vivo no
  se pudo navegar (FT2 se abre al enviar FT — compuerta existente); su lógica es
  el motor puro ya testeado y el mismo patrón de datos de A2.
- Pendientes que siguen para F3: DDS (referencia + código + snapshot congelado),
  KML por parcelas, conteo "EUDR Aptas" del dashboard (regla legacy).
- Es cambio de nodo (`n-kaffetal`, `n-bcp`, `n-db` +1 tabla) y semillas DICT: el
  arquetipo calculado y los claims derivados merecen entrada propia en el wrap V20.
(commit: 68932ec)

## [2026-07-29] F1 de la reestructura EUDR: la finca como titular (parcelas + certificados)
- Primera fase de docs/EUDR_RESTRUCTURE_PLAN.md (aprobado por el owner el 2026-07-29,
  a partir de sus dos notas de deep research "¿Finca o Lote?" y "Los cuatro
  arquetipos"). Principio: el productor declara HECHOS; el sistema deriva
  afirmaciones. Los certificados son CREDENCIALES de la finca (número + vigencia);
  el lote solo derivará claims (F2).
- **Migración `f1_finca_parcelas_and_certificates`**: tablas `finca_parcelas` (el
  átomo probatorio del Art. 9 — punto siempre, polígono donde area>4 ha
  [`requires_polygon` GENERATED]; geometría congelada con la finca aprobada, guard)
  y `finca_certificates` (scheme del catálogo, número, vigencia, soporte,
  `verified_by_ctc` solo-CTC — guard INSERT+UPDATE que además RESETEA la
  verificación si el productor edita el contenido de un cert verificado). RLS
  *_own vía dueño de la finca. Siembra: parcela 1 desde el punto/polígono actual.
- **`certRegistry.ts`** ganó `level` (finca|org|lote|none) y `validate`
  (number|upload|both) por esquema, según el inventario de la nota: IWCA→none
  (membresía, no certificado), EUDR→lote (la DDS es del embarque, F3), BPA/DO/IGP
  →number. `fincaLevelSchemes()` filtra lo registrable en la finca.
- **La Visa se juzga por parcelas**: `fincaEudrStatus()` ganó el parámetro opcional
  `parcelas` (todas localizadas, polígono donde >4 ha; los callers legacy conservan
  la regla vieja) y `approveFinca()` lo re-impone server-side nombrando las
  parcelas que faltan. Nuevos puros `parcelaGeoOk`/`parcelasGeoComplete`.
- **FincaModal (productor), 3→4 pestañas**: la 2 pasa a editar la PARCELA 1 (el
  mapa de siempre ES el cafetal principal — se espeja a `finca_parcelas` al
  guardar, cero doble trabajo) + lista "¿Su café crece en varios cafetales
  separados?" con editor por cafetal (un FincaMapPicker por tarjeta, guardado
  inmediato por fila, FUERA del autosave). La 4 nueva "Certificaciones": esquema
  + número + vigencia + PDF opcional, con la microcopy "no tenerlos no le impide
  exportar"; sin vigencia el cert queda "declarado — no respalda sellos".
- **BCP**: `FincaEudrEditor` ganó la 5ª sub-pestaña "Certificaciones" (declarado
  vs verificado, enlace al registro público del esquema, botón Verificar →
  `setFincaCertVerified`, que exige vigencia y deja audit_log) y la lista de
  parcelas auditada contra el umbral de 4 ha en "Análisis y Evidencia".
- **Dossier de la Visa** (ambas rutas, vía el nuevo `lib/dossierExtras.ts`): tabla
  "Parcelas del predio (Art. 9)" y tabla "Certificaciones de la finca" con número,
  vigencia y estado — citando que son información complementaria del Art. 10(2)(n).
- Verificación: tsc + eslint limpios; e2e con cuenta QA de productor en la app
  real (crear finca → clic real en el mapa → guardar ⇒ parcela 1 espejada en base
  con coords idénticas → registrar Rainforest con número y vigencia ⇒ fila con
  `verified_by_ctc=false` forzado por el guard); cascada FK verificada al borrar
  la cuenta QA (0 huérfanos).
- **Pendiente anotado para F2**: el KML/GeoJSON de `earthKml.ts` sigue exportando
  la geometría de la finca (= parcela 1); el export parcela-por-parcela entra con
  la composición del lote. `AppDashboard` cuenta "EUDR Aptas" por la regla legacy
  (el modal y approveFinca ya usan parcelas).
- Es cambio de nodo (`n-kaffetal`, `n-bcp`, `n-db`: +2 tablas) y semilla de las
  futuras entradas DICT de F2 (claims derivados).
(commit: fd7bbc5)

## [2026-07-28] El autosave perdía en silencio el último cambio al cerrar
- **Bug reportado por el owner**: en el Panel del Productor → Finca → "Ubicación y
  respaldo", el selector "Tipo de documento" mostraba las opciones pero al elegir una
  volvía a "Seleccione…". Confirmado en base: las 3 fincas de gabriel.vasquez92@gmail.com
  (CTC-P-78337EC3) tenían `eudr_support_doc_type` en NULL y `eudr_producer_answers ->
  supportDocType` en "" — el valor no llegaba nunca.
- **No era el selector.** Reproducido con una ruta temporal que montaba `FincaModal`
  con datos sintéticos: el `<select>` fija su valor y el borrador se actualiza bien.
  Tampoco era la guarda `guard_finca_protected_columns` (solo congela declaraciones
  con la finca `approved`, y las tres estaban en `pending_review`).
- **La causa real está en `src/lib/useAutosave.tsx`**: el guardado corre 3,5 s después
  del último cambio, y el cleanup del efecto CANCELABA el temporizador al desmontar.
  Elegir de un desplegable es la interacción más rápida que existe, así que cerrar la
  ficha enseguida le ganaba al debounce y el cambio se perdía sin ningún aviso. Afectaba
  por igual a cualquier campo y a cualquier finca — solo que en los campos que se
  escriben a mano el productor tarda más y el temporizador alcanzaba a disparar.
- **Arreglo**: un efecto con dep array vacío cuyo cleanup corre SOLO al desmontar hace
  *flush* del guardado pendiente si el snapshot difiere del último guardado. Lee de un
  ref (no puede cerrar sobre el render actual), respeta `enabled`, y se abstiene si ya
  hay un guardado en vuelo para no abrir una carrera de dos escrituras sobre la fila.
- **Alcance**: `useAutosave` es compartido, así que la misma pérdida silenciosa existía
  en la Ficha del lote, "Información general" del productor y el editor EUDR de fincas
  del BCP. Quedan arregladas las cuatro.
- Verificado en navegador, tres casos: (1) elegir y cerrar a los 800 ms ⇒ antes SIN
  GUARDADOS, ahora guarda; (2) abrir y cerrar sin tocar nada ⇒ no guarda (sin escrituras
  espurias); (3) elegir, esperar el debounce y luego cerrar ⇒ guarda UNA sola vez.
- No es cambio de nodo ni de escenario del mapa: es una corrección de comportamiento.
(commit: c484c63)

## [2026-07-28] Calculadora de Mermas · CTC — sube a V3
- El owner subió `Calculadora_Mermas_Cafe_CTC_V3.html` a `reference_html_tools/` y
  reemplaza a `Calculadora_Mermas_Cafe_CTC.html` (borrado; la carpeta ya nombra cada
  herramienta con su versión). La V3 cambia la interfaz a un **embudo de mermas**
  interactivo (Cereza → Beneficio → Trilla → Tueste) con merma total y factor de
  conversión en vivo, más carriles laterales de Configurar / Costos / Resultado.
- El **nombre servido no cambia**: sigue siendo `public/tools/mermas-ctc.html`, que es
  a lo que apunta la clave `mermas-ctc` de `src/lib/tools/catalog.ts` — así que ni el
  catálogo, ni la tabla de Disponibilidad, ni los iconos, ni el hub de KR se tocan.
- Se volvió a correr `node scripts/vendor-tool-assets.mjs` (el paso que la nota de
  `herramientas` ya advertía: al actualizar una herramienta hay que recopiar y
  revendorizar): bajó 10 woff2 y jsPDF 2.5.1 a `/tools/assets`. Sin esto la
  herramienta pide fuentes a Google y jsPDF a cdnjs, y la CSP de la ruta interna
  bloquea el script remoto.
- Verificación en el navegador: 0 errores de consola, `window.jspdf` definido, 38
  font-faces cargadas, y el único enlace externo que queda es el de lectura a
  ctcexport.com. Aritmética del embudo comprobada de punta a punta (1.000 kg cereza
  → 200 pergamino → 160 verde → 134,4 tostado; merma total 86,6%; factor 7,44:1).
- No es cambio de nodo ni de escenario: la entrada DICT `herramientas` ya describe el
  mecanismo y sigue siendo correcta.
(commit: 710c1b7)

