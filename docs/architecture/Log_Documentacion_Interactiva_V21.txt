Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V21.0(ad137a8).html
(CTC Web Platform, 2026-07-29). Cada entrada se compila al próximo Version Wrap (V22).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio (lección del wrap de V19): registrar la entrada AL TERMINAR el trabajo,
no "después". El backend real del Directorio del Café (3 commits del 2026-07-24) nunca
se anotó y V18.0 salió publicado describiéndolo como maqueta.

Contexto abierto al cerrar V21:
- **F4 (blends) sigue DIFERIDA a propósito** hasta que los arquetipos Regional/
  Multi-Origin tengan volumen real: tabla completa municipio→región cafetera, recetas
  ponderadas con país y riesgo fechado, y split de un lote en sub-lotes por cobertura.
  Ver docs/EUDR_RESTRUCTURE_PLAN.md §7 y §8.
- Sin verificar en vivo: el botón "Registrar DDS" del BCP (2FA real impide navegarlo
  en automático) — su lógica es el motor puro ya testeado, pero conviene un paso
  manual del owner la primera vez que se registre una DDS de verdad.
- **Decisión pendiente del owner**: las cuatro denominaciones de origen (DOR, DO, IGP,
  Café de Colombia) están atadas a una región, no a una auditoría con vencimiento, pero
  `deriveClaims` les exige vigencia igual que a los demás esquemas — hoy no pueden
  respaldar un sello sin fechas inventadas. Eximirlas toca la semántica de claims de F2
  y `scripts/qa-claims-check.mjs`.
- **Techo a la vista**: el bucket `kaffetal-media` está en 492 MB de 1 GB. El Buzón
  (295 objetos / 144 MB) crece con cada sincronización y la retención de 30 días poda
  Hostinger, NO el bucket. Nadie lo está podando.
- La base quedó SIN fincas ni lotes tras el reset del 2026-07-29 (respaldo fila a fila
  en `CTC Web Platform/backup-reset-2026-07-29.json`, fuera del repo). El próximo
  productor que entre estrena el formulario de 4 pestañas desde cero.

---

## [2026-07-29] Coffeed: el módulo editorial del Estudio de Contenido + su muro en KR/CP/DC
- **Módulo NUEVO completo** (V2.25), adaptado del prototipo `reference_coffeed/`
  (coffeed.html v0.2 + schema.sql + edge function): el muro de noticias y
  anuncios internos de CTC + la línea de producción editorial de 7 etapas
  (ingesta manual → mesa de cata/triaje → extracción con claims trazables →
  3 propuestas → revisión humana → borrador de 5-10 paneles → aceptación →
  guion de vídeo). El nodo **Estudio de Contenido** (socio v3) deja de ser
  puro scaffold: su primer módulo real vive en
  `/socios/estudio-contenido/panel/coffeed` (page gated: slug exacto +
  `requirePartner`; Server Actions re-gateadas por `estudioGate()` en
  `src/lib/coffeed/requireEstudio.ts`).
- **Esquema**: migración `coffeed_editorial_pipeline` — 13 tablas prefijadas
  `coffeed_*` (sources, blocklist_patterns, items, extractions, claims,
  threads [canon], cycles [capítulos], matrix_entries, proposals, drafts,
  panels, video_scripts, announcements), TODAS service-role-only (RLS sin
  policies, patrón Arena/leads). Las reglas del formato (5-10 paneles, máx.
  3 por fuente, ninguno sin trazar) se validan TRES veces: cliente
  (`validateCoffeedDraft`), server action y trigger `coffeed_guard_accept`
  (+ `coffeed_update_canon` actualiza los hilos del canon al aceptar, y
  draft→published exige pasar por accepted). Verificado por SQL en vivo.
- **IA** (`src/lib/coffeed/aiActions.ts`, fetch crudo estilo GVG/advisor):
  triaje con Haiku (`claude-haiku-4-5`), propuestas/expansión/guion con
  Sonnet (`claude-sonnet-5`). ⚠️ Gotcha vivo 2026-07-29: **claude-sonnet-5
  rechaza el prefill de assistant** («This model does not support assistant
  message prefill», 400) que el prototipo usaba para forzar JSON — se quitó
  el prefill y parseJson() rescata el primer bloque JSON. También se añadió
  reintento (x3, backoff) para 529 Overloaded/429, vistos repetidamente en
  vivo. `maxDuration=300` en la page (las actions heredan el segment config,
  lección GVG). Todo el pipeline se corrió EN VIVO de punta a punta con la
  credencial QA: triaje real, 3 propuestas válidas, expansión 6 paneles
  trazados a claims, aceptación, guion de 6 escenas/26 s, publicación.
- **El muro público**: `getCoffeedWall()` (curaduría estilo
  public_lot_catalog: SOLO capítulos `published`, columnas de exhibición;
  los anuncios internos nunca viajan) + componente compartido `CoffeedWall`
  montado en TRES superficies: KR (módulo nuevo "coffeed" del hub del
  productor, AppDashboard), Cherry Picked (CoffeedSection #coffeed con
  QuickNav renumerado 08→11, cabecera EN/ES/DE, contenido en español a
  propósito) y Directorio (pestaña "Coffeed" para miembros verificados,
  AppView). KR y CP verificados en vivo con el capítulo QA; la pestaña del
  DC comparte el mismo componente (no se navegó en vivo — requiere miembro
  verificado).
- Datos QA borrados tras la verificación (el dev apunta a la base real y el
  capítulo de prueba llevaba cifras fabricadas): coffeed_* a cero, cuentas
  QA eliminadas. La base Coffeed queda vacía esperando el primer ciclo real.
- Fase 1 del spec (`reference_coffeed/coffeed-especificacion.md` §10) más
  buena parte de fase 2/3: falta el barrido automático (RSS/YouTube + cron),
  reacciones/comentarios del muro, y el render de paneles a PNG/publicación
  en Instagram. Las listas blanca/negra ya son administrables pero hoy solo
  documentan el criterio.
- Nodos/DICT sugeridos para el wrap: nodo nuevo "Coffeed" colgado del
  Estudio de Contenido; DICT `coffeed` (muro + pipeline + canon),
  cross-link a `estudio-contenido` y al patrón `public catalog view`;
  escenario nuevo "Un capítulo de Coffeed" (estudio produce → acepta →
  publica → KR/CP/DC lo leen).
(commit: c385764)

## [2026-07-29] El loop de login del BCP: la sesión interna gana cookie propia
- **BUG EN PRODUCCIÓN, reportado por el owner**: password + OTP correctos en
  ctcexport.com/login (dos códigos consumidos a las 21:05-21:06 UTC en
  admin_otp_codes) y aun así rebotaba a pedir credenciales. Diagnóstico por
  logs de Auth: una pestaña de Kaffetal Regal abierta, con un refresh token
  ya rotado, reintentaba `/token` cada ~90 s desde la IP del owner (referer
  kaffetal-regal.ctcexport.com), recibía `refresh_token_not_found` y BORRABA
  la cookie compartida `sb-…-auth-token` (Domain=.ctcexport.com desde el
  2026-07-24) — matando la sesión del BCP recién acuñada por el verify. Un
  solo slot de cookie para todas las pestañas de todos los subdominios +
  rotación de refresh tokens = race sin coordinación posible (navigator.locks
  no cruza orígenes).
- **Arreglo (V2.26)**: la sesión de las consolas internas vive ahora en SU
  cookie, `ctc-panel-auth` (`PANEL_AUTH_COOKIE` + `createPanelSessionClient()`
  en `lib/supabase/server.ts`, cookieOptions.name de @supabase/ssr). Cambiados
  al cliente panel: verify/logout del master login, requireConsoleAccess,
  requireActiveAdmin (⇒ todas las Server Actions del BCP/ECP/OCP y GVG),
  /cambiar-contrasena y los requireOwner de ECP usuarios / OCP socios. El
  proxy renueva AMBAS cookies (misma mecánica anti-caducidad de 1 h).
- Colaterales deseados: entrar/salir del BCP ya no pisa la sesión pública
  (productor/socio) del mismo navegador — ambas conviven; verificado en vivo
  (QA admin + QA partner simultáneos, y borrado simulado de la cookie
  compartida con el BCP intacto).
- Bonus del mismo bug-hunt: en ResponseCookies de Next, un segundo `.set()`
  del mismo nombre TRAGA un borrado previo (mapa por nombre, verificado
  contra el Next instalado) — la migración host-only→Domain del proxy del
  2026-07-24 nunca borró nada; ahora la expiración va como header crudo.
- Nota post-deploy: las sesiones internas activas mueren con el cambio de
  cookie — hay que iniciar sesión una vez más en /login. HANDOFF gotcha #13.
(commit: 4bd10c0)

## [2026-07-30] Coffeed v2: del socio al ECP, y un pipeline que sí es una secuencia
- **CAMBIO DE NODO (decisión del owner, un día después de estrenarlo)**: Coffeed
  sale del socio *Estudio de Contenido* y pasa al **ECP**, en `/ecp/coffeed`.
  El argumento manda sobre lo ya construido: la narrativa de la red se DIRIGE
  desde dentro; lo que se delega es su producción, no la decisión editorial.
  Gate nuevo `coffeedGate()` (bcp_admin + panel_user activo + grant ecp, sobre
  la sesión interna); se borran la ruta del socio, su tarjeta en el panel y
  `requireEstudio.ts`. En el mapa: el nodo Coffeed cuelga del ECP, y el Estudio
  de Contenido vuelve a ser scaffold puro.
- **El pipeline se rehace para que sea UNA secuencia legible**, con el estado
  del ciclo (`coffeed_cycles.status`, enum nuevo) como única fuente: es
  literalmente la columna del kanban donde vive la tarjeta, así que una acción
  larga que se corta (529, navegador cerrado) deja su tarjeta con botón de
  reintento en vez de perderse. Migración `coffeed_v2_ecp_pipeline`.
    · **Medios de Consulta** (sustituye Ingesta/Fuentes): se pega la URL de un
      canal o medio y un AGENTE sale a mirarlo con búsqueda web — existencia,
      publicación recurrente y materia. Si no sirve, lo RECHAZA con motivo y lo
      deja en la lista negra (nadie lo vuelve a proponer sin saber por qué).
      Anillo de progreso mientras valida. Lista blanca sembrada con 7 canales
      de YouTube + 7 medios, como pidió el owner. La ingesta manual ya no vive
      aquí.
    · **Selección de Fuentes** (antes «Mesa de cata»): una sesión abierta a la
      vez (índice parcial). Botón nuevo de **barrido de 7 días** que recorre la
      lista blanca y trae titular + sumario + FECHA. Aquí sí entran las URLs
      manuales. El último botón del panel dispara la extracción y salta a
      Propuestas: la extracción es backend, ya no una pantalla.
    · **Propuestas**: kanban de 3 columnas (En extracción · Extraídas ·
      Propuestas). Se abre una tarjeta para leer el material extraído; las
      listas esperan «Generar 3 propuestas (IA)»; de los 3 ángulos se elige uno
      con **Crear Post**. La sección «Borrador» DESAPARECE.
    · **Posts en Fila**: kanban de 2 columnas (Creando · Listos). La ficha del
      post enlaza al render, descargable en **HTML** y **PDF**, con **Re-editar**
      (prompt libre que rehace el post sobre las mismas fuentes) y **Publicar**.
- **Identidad de marca** (tabla `coffeed_brand`, fila única): nombre, slogan,
  logo, paleta de hasta 5 colores (blanco y negro implícitos), tipografía y
  dirección de arte. No es decoración: `postTemplate.ts` la lee para maquetar
  —la maqueta la decide una plantilla determinista, NO un modelo— el logo se
  empotra en base64 para que el HTML descargado sea autosuficiente, y la
  dirección de arte viaja como contexto en extracción, propuestas y redacción.
- **Muro**: sale del grupo «Producción» y sube solo, primero; Canon e Identidad
  quedan debajo. Y los **anuncios ahora VIAJAN** a KR/Cherry Picked/Directorio
  junto a los capítulos: el muro es el mismo dondequiera que se muestre.
- **Bug de contraste corregido**: `.page button { color: inherit }` (0,1,1)
  ganaba a `.btn` (0,1,0), así que ningún botón se leía hasta el hover, que sí
  ganaba con `.btn:hover` (0,2,0). Regla nueva en la hoja: ninguna declaración
  de color por elemento. Medido en vivo: 5.2:1–16.7:1 en todos los botones.
- **Verificado EN VIVO de punta a punta** con credencial QA (OTP inyectado por
  SQL): validación de medio (Wikipedia RECHAZADA con motivo → lista negra),
  barrido real (4 artículos de los últimos 7 días con URL, medio y fecha
  correctos, de Daily Coffee News / Perfect Daily Grind / Global Coffee
  Report), extracción (3 fuentes, 6 claims trazables), 3 propuestas
  genuinamente distintas, post de 5 paneles con 5,4 KB de HTML de marca,
  publicación, y el capítulo + el anuncio visibles en la sección de Cherry
  Picked. Datos QA borrados después; se conservan los 4 artículos reales, la
  sesión abierta y los 14 medios sembrados.
- Gotchas nuevos verificados en vivo:
  · `claude-sonnet-5` **no admite `fallbacks`** (400) — la cabecera de GVG usa
    opus-5, no se copia a ciegas.
  · En un módulo `"use server"` un `export type {…}` compila a export de
    runtime y revienta el módulo entero al evaluarse.
  · **El `fetch` de Node (undici) corta a los 300 s** y devuelve un escueto
    «fetch failed». El barrido en UNA petición con 14 búsquedas web tardaba
    5,1 min y moría ahí; en tandas de 4, igual. Medido aislado: una llamada
    con 2 búsquedas tarda ~41 s. De ahí el diseño final: **una petición por
    medio**, concurrencia 5, timeout explícito de 90 s y **un solo reintento
    para los timeouts** (reintentarlos tres veces multiplicaba la espera sin
    arreglar nada: la consulta lenta seguía siendo lenta).
  · `stop_reason === "max_tokens"` se detecta y se nombra: antes salía como un
    error críptico de posición en el JSON.
- Nodos/DICT para el wrap: mover el nodo Coffeed bajo ECP; DICT `identidad de
  marca` y `medios de consulta`; el escenario «Un capítulo de Coffeed» cambia
  de actor (ECP produce → publica → KR/CP/DC lo leen).
(commit: cd25d8a)
