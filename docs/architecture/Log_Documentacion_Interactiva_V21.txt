Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V21.0(ad137a8).html
(CTC Web Platform, 2026-07-29). Cada entrada se compila al próximo Version Wrap (V22).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio (lección del wrap de V19): registrar la entrada AL TERMINAR el trabajo,
no "después". El backend real del Directorio del Café (3 commits del 2026-07-24) nunca
se anotó y V18.0 salió publicado describiéndolo como maqueta.

Contexto abierto al cerrar V21:
- **F4 (blends) sigue DIFERIDA a propósito** hasta que los arquetipos Regional/
  Multi-Origin tengan volumen real: tabla completa municipio→región cafetera, recetas
  ponderadas con país y riesgo fechado, y split de un lote en sub-lotes por cobertura.
  Ver docs/EUDR_RESTRUCTURE_PLAN.md §7 y §8.
- Sin verificar en vivo: el botón "Registrar DDS" del BCP (2FA real impide navegarlo
  en automático) — su lógica es el motor puro ya testeado, pero conviene un paso
  manual del owner la primera vez que se registre una DDS de verdad.
- **Decisión pendiente del owner**: las cuatro denominaciones de origen (DOR, DO, IGP,
  Café de Colombia) están atadas a una región, no a una auditoría con vencimiento, pero
  `deriveClaims` les exige vigencia igual que a los demás esquemas — hoy no pueden
  respaldar un sello sin fechas inventadas. Eximirlas toca la semántica de claims de F2
  y `scripts/qa-claims-check.mjs`.
- **Techo a la vista**: el bucket `kaffetal-media` está en 492 MB de 1 GB. El Buzón
  (295 objetos / 144 MB) crece con cada sincronización y la retención de 30 días poda
  Hostinger, NO el bucket. Nadie lo está podando.
- La base quedó SIN fincas ni lotes tras el reset del 2026-07-29 (respaldo fila a fila
  en `CTC Web Platform/backup-reset-2026-07-29.json`, fuera del repo). El próximo
  productor que entre estrena el formulario de 4 pestañas desde cero.

---

## [2026-07-29] Coffeed: el módulo editorial del Estudio de Contenido + su muro en KR/CP/DC
- **Módulo NUEVO completo** (V2.25), adaptado del prototipo `reference_coffeed/`
  (coffeed.html v0.2 + schema.sql + edge function): el muro de noticias y
  anuncios internos de CTC + la línea de producción editorial de 7 etapas
  (ingesta manual → mesa de cata/triaje → extracción con claims trazables →
  3 propuestas → revisión humana → borrador de 5-10 paneles → aceptación →
  guion de vídeo). El nodo **Estudio de Contenido** (socio v3) deja de ser
  puro scaffold: su primer módulo real vive en
  `/socios/estudio-contenido/panel/coffeed` (page gated: slug exacto +
  `requirePartner`; Server Actions re-gateadas por `estudioGate()` en
  `src/lib/coffeed/requireEstudio.ts`).
- **Esquema**: migración `coffeed_editorial_pipeline` — 13 tablas prefijadas
  `coffeed_*` (sources, blocklist_patterns, items, extractions, claims,
  threads [canon], cycles [capítulos], matrix_entries, proposals, drafts,
  panels, video_scripts, announcements), TODAS service-role-only (RLS sin
  policies, patrón Arena/leads). Las reglas del formato (5-10 paneles, máx.
  3 por fuente, ninguno sin trazar) se validan TRES veces: cliente
  (`validateCoffeedDraft`), server action y trigger `coffeed_guard_accept`
  (+ `coffeed_update_canon` actualiza los hilos del canon al aceptar, y
  draft→published exige pasar por accepted). Verificado por SQL en vivo.
- **IA** (`src/lib/coffeed/aiActions.ts`, fetch crudo estilo GVG/advisor):
  triaje con Haiku (`claude-haiku-4-5`), propuestas/expansión/guion con
  Sonnet (`claude-sonnet-5`). ⚠️ Gotcha vivo 2026-07-29: **claude-sonnet-5
  rechaza el prefill de assistant** («This model does not support assistant
  message prefill», 400) que el prototipo usaba para forzar JSON — se quitó
  el prefill y parseJson() rescata el primer bloque JSON. También se añadió
  reintento (x3, backoff) para 529 Overloaded/429, vistos repetidamente en
  vivo. `maxDuration=300` en la page (las actions heredan el segment config,
  lección GVG). Todo el pipeline se corrió EN VIVO de punta a punta con la
  credencial QA: triaje real, 3 propuestas válidas, expansión 6 paneles
  trazados a claims, aceptación, guion de 6 escenas/26 s, publicación.
- **El muro público**: `getCoffeedWall()` (curaduría estilo
  public_lot_catalog: SOLO capítulos `published`, columnas de exhibición;
  los anuncios internos nunca viajan) + componente compartido `CoffeedWall`
  montado en TRES superficies: KR (módulo nuevo "coffeed" del hub del
  productor, AppDashboard), Cherry Picked (CoffeedSection #coffeed con
  QuickNav renumerado 08→11, cabecera EN/ES/DE, contenido en español a
  propósito) y Directorio (pestaña "Coffeed" para miembros verificados,
  AppView). KR y CP verificados en vivo con el capítulo QA; la pestaña del
  DC comparte el mismo componente (no se navegó en vivo — requiere miembro
  verificado).
- Datos QA borrados tras la verificación (el dev apunta a la base real y el
  capítulo de prueba llevaba cifras fabricadas): coffeed_* a cero, cuentas
  QA eliminadas. La base Coffeed queda vacía esperando el primer ciclo real.
- Fase 1 del spec (`reference_coffeed/coffeed-especificacion.md` §10) más
  buena parte de fase 2/3: falta el barrido automático (RSS/YouTube + cron),
  reacciones/comentarios del muro, y el render de paneles a PNG/publicación
  en Instagram. Las listas blanca/negra ya son administrables pero hoy solo
  documentan el criterio.
- Nodos/DICT sugeridos para el wrap: nodo nuevo "Coffeed" colgado del
  Estudio de Contenido; DICT `coffeed` (muro + pipeline + canon),
  cross-link a `estudio-contenido` y al patrón `public catalog view`;
  escenario nuevo "Un capítulo de Coffeed" (estudio produce → acepta →
  publica → KR/CP/DC lo leen).
(commit: c385764)
