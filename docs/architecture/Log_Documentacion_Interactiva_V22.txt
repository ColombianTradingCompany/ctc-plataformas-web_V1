Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V22.0(388e043).html
(CTC Web Platform, 2026-08-02). Cada entrada se compila al próximo Version Wrap (V23).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio (lección del wrap de V19): registrar la entrada AL TERMINAR el trabajo,
no "después".

Contexto abierto al cerrar V22:
- **F4 (blends) sigue DIFERIDA a propósito** — ver docs/EUDR_RESTRUCTURE_PLAN.md §7-8.
- "Registrar DDS" del BCP sigue sin verificarse en vivo (la 2FA real impide navegarlo).
- La red V4 está COMPLETA y con TLS en los 16 subdominios; quedan pendientes del
  owner: material de la landing de Varietales, la decisión §5.2 (¿los nodos socios
  pierden su landing pública?), y el UI del enlace CRM Co-Create ↔ Black Stock
  (la FK lead_id ya existe; espera leads y negociaciones reales).
- El vocabulario canónico Specialty/Black (Fase 0) NO se retrofitteó aún a la copy
  vieja de KR (ProcessFlow) y Cherry Picked (BlackSection) — pase de copy pendiente.
- black_negotiations está VACÍA tras el reset: el Black Stock se autopuebla con la
  primera jornada que gradúe un lote Black.
- El bucket de Storage sigue sin poda automática del Buzón.

## [2026-08-02] ECP: diagrama "Estructura del sistema", Herramientas renombrada, Terratalento plantado
- Documentación del sistema (/ecp/documentacion) ganó el botón **"Estructura del
  sistema"**: popup con el diagrama V4 aprobado por el owner (la versión
  corregida — enrutador, módulos internos por consola, 3 clases de E/S), como
  SVG autocontenido en EstructuraModal.tsx (paleta corporativa; se actualiza a
  mano cuando la red cambie). Verificado: 0 elementos fuera del viewBox, los 18
  rótulos esperados presentes.
- Nav del ECP: "Herramientas internas" se RENOMBRÓ a "Herramientas del café" y
  se MOVIÓ de "IT y Plataforma" al grupo Dirección — desde la Fase 4 de V4 es
  un producto de la red, no tooling interno.
- **Terratalento** (nuevo, scaffold en /ecp/terratalento + entrada de nav):
  el servicio del RECOLECTOR — nuevo tipo de usuario con superficie propia
  (landing + login) que crea perfil y se postula; las fincas publicarán
  "Jornadas de Recolecta" desde Kaffetal Regal (módulo nuevo allá) y el ECP
  hace el match. SE DESARROLLA EN UNA SESIÓN APARTE — hoy solo ConsoleScaffold
  con los 3 módulos planeados.
- Para el wrap: nota en n-consoles (nav Dirección: Herramientas del café +
  Terratalento); cuando Terratalento se construya será nodo/superficie propia
  (Clase A) + módulo KR + tablero ECP.
- APP_VERSION 2.33 → 2.34.
(commit: 52b8f6f)

## [2026-08-02] Terratalento CONSTRUIDO — el servicio del recolector, de punta a punta
- Las tres patas del concepto del owner, en un solo paquete (V2.35):
  · SUPERFICIE PÚBLICA /terratalento (subdominio terratalento, DNS pendiente):
    identidad patrón Directorio (misma cuenta del ecosistema, ortogonal a
    profiles.role — verificado en vivo: una cuenta de productor entra y ve
    "completa tu perfil"). Landing + acceso email/contraseña (sin Google: ese
    subdominio no tiene callback) + panel del recolector: perfil (upsert),
    pausa de disponibilidad, jornadas abiertas con postular/retirar y chips de
    estado. Curaduría de lectura: de la finca solo viajan nombre/municipio/
    vereda — nunca geolocalización ni EUDR.
  · MÓDULO KR "Jornadas de Recolecta" (hub del productor, icono canasta,
    JornadasRecolectaModule autocargante estilo CoffeedWall): publicar por
    finca (fechas/cupos/pago/condiciones), cerrar/cancelar, y SOLO conteos —
    el contacto del recolector no viaja al productor; CTC es el puente.
  · TABLERO ECP /ecp/terratalento (sustituye el scaffold): KPIs, popup por
    jornada (LeadModalRow) con roster de postulados + acciones llamar /
    confirmar (tope de cupos) / descartar / volver, control de estado de la
    jornada, y el roster completo de recolectores. audit_log en cada escritura.
- Esquema: migración terratalento_core — terratalento_recolectores /
  _jornadas / _postulaciones (UNIQUE jornada+recolector), service-role-only
  (RLS sin políticas). Lógica en src/lib/terratalento/actions.ts.
- Verificado EN VIVO con cuentas QA (borradas después, roster limpio):
  productor publica jornada desde su hub → recolector entra, guarda perfil,
  se postula → confirmación simulada por SQL (el ECP real es 2FA) → ambos
  lados la reflejan (chip Confirmado + 1/8 cupos). tsc/eslint limpios.
- CTC Home: loseta Terratalento en el índice de la red (10 puertas).
  Nav ECP: la entrada dejó de ser scaffold.
- Nodos/wires para el wrap: nodo nuevo superficie terratalento (Clase A,
  identidad única) + módulo KR + tablero ECP; wires a db; escenario nuevo
  "Una Jornada de Recolecta" (finca publica → recolector se postula → ECP
  confirma). DICT candidato: "Terratalento / Jornada de Recolecta".
- APP_VERSION 2.34 → 2.35.
(commit: db8381d)

## [2026-08-02] Terratalento: la notificación del llamado
- Marcar una postulación como "llamado" o "confirmado" en el ECP ahora envía
  correo al recolector (a su cuenta del ecosistema) vía Resend. Dos variantes
  de texto: «¡Te llamaron!» (CTC te contactará al celular) y «Tu cupo quedó
  CONFIRMADO» — con finca, municipio, rango de fechas, pago y condiciones,
  y el enlace al panel.
- El texto vive en src/lib/email/llamadoEmail.ts — módulo PURO sin imports
  (patrón GVG), testeado por scripts/qa-llamado-email-check.mjs (8/8). El
  envío (terratalentoEmails.ts) usa sendTransactionalEmail con el contrato
  never-throw de leads.
- El cambio de estado NUNCA se bloquea por el correo: el resultado se
  persiste en la fila (notificado_at / notificacion_error — migración
  terratalento_llamado_notification) y el popup del ECP muestra el chip
  «Correo enviado <fecha>» / «Correo sin enviar: <error>» con botón
  Reenviar/Reintentar (reenviarNotificacionLlamado).
- Para el wrap: el escenario futuro "Una Jornada de Recolecta" gana el paso
  Resend; wire n-third ya cubre el envío. Sin nodos nuevos.
- APP_VERSION 2.35 → 2.36.
(commit: pending)
