Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V26.0(5496144).html
(CTC Web Platform, 2026-08-05). Cada entrada se compila al próximo Version Wrap (V27).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio (lección del wrap de V19): registrar la entrada AL TERMINAR el trabajo,
no "después". En el arco de V26 se me acumularon cinco entradas sin registrar y hubo
que reconstruirlas antes de compilar.

Contexto abierto al cerrar V26:
- **Los dos cotizadores y Anclas de mercado NO se han conducido en navegador**: el
  OCP tiene 2FA con OTP por correo. Los motores SÍ están verificados (las apps son
  las del owner; el lector FNC contra la página real). Lo que falta por comprobar a
  mano: guardar un borrador, recargar y ver que la calculadora vuelve donde estaba;
  y la tabla de la lista y el módulo de Anclas pintados.
- `CRON_SECRET` YA está configurado en Vercel (el endpoint responde 401 sin
  cabecera, que es la señal correcta). La primera corrida automática del precio
  cae el 2026-08-06 ~06:10 en Colombia — conviene mirar Settings → Cron Jobs.
- El owner tiene 3 cotizaciones de prueba en producción (COT-G-000001,
  COT-L-000001 y una tercera). Las secuencias quedaron por encima del máximo; NO
  reiniciarlas sin volver a comprobarlo o el siguiente código choca contra el
  índice único de `code`.
- Identity Value Creation (app #3 del Estudio) sigue declarada y sin construir. Su
  pendiente real no es UI: leer finca/lote desde el tier de socios exige vistas
  SECURITY DEFINER estrechas por campo, no un grant ancho.
- Sigue pendiente del owner: material de la landing de Varietales; pase de copy
  Specialty/Black sobre KR ProcessFlow y CP BlackSection.
- El socio multi-nodo sigue exigiendo cambiar el PK de `partner_accounts`.
- El bucket de Storage sigue sin poda automática del Buzón.

## [2026-08-05] Plan de integraciones: Notion, Google y Make
- `docs/INTEGRACIONES_PLAN.md` — plan, sin código todavía. Comprobado con acceso
  real a los cuatro sistemas (Notion, Gmail, Drive y Make, todos bajo
  ctcexportmain@gmail.com).
- Make: plan **Pro activo** con cupón — 750.000 ops/mes, intervalo 1 min, 250 MB
  por archivo, data stores, BYO-LLM-key. Zona **eu2** (UE). Escenarios: cero (se
  borró el único, un "Integration ClickUp" de marzo 2025 con 0 ejecuciones).
- **Hallazgo que ordena todo**: las etiquetas de Gmail y los hubs de Notion son
  la MISMA taxonomía del negocio (0. Admin y Estrategia · 1. Origen y Suministro ·
  2. Tránsito e Importación · 3. Tostadores e Inventario · 4. Ventas y Marketing ·
  5. IT · 6. I+D). Se propone como enum compartido de toda la integración.
- **Regla de arquitectura propuesta**: cada dominio tiene UN dueño y la copia
  viaja en una sola dirección. Postgres manda en lotes/fincas/contratos/
  cotizaciones (ya tienen triggers, RLS y audit_log que Notion no puede honrar);
  Notion manda en doctrina, papers, reuniones y tareas; la frontera se resuelve
  con ESPEJO + `ctc_id`, no con sincronización bidireccional.
- **CONFLICTO YA EXISTENTE, pendiente de decisión del owner**: «Grados de Calidad
  CTC» está definido en Notion (umbrales SCA) y en la plataforma (`lots.grade`,
  otorgado en la Jornada). Hoy coinciden; en cuanto uno se mueva habrá dos
  verdades. Igual con el pitch CaaS y la jerarquía Papagayo Beans, duplicados
  entre Notion y `servicesCopy.tsx`.
- **NODO/MÓDULO NUEVO propuesto**: `/ecp/automatizaciones` en IT y Plataforma —
  registro de escenarios por propósito, dominio, criticidad y ciclo de vida
  (propuesta→piloto→activa→deprecada), con última corrida y ops consumidas
  leídas del API de Make.
- **DICT sugerido**: «Espina de integración» (tabla `integration_events` +
  despachador + una sola puerta `/api/integraciones/[canal]` con secreto por
  canal) y «Make es el bus, no el cerebro».
- Para el TODO de GDPR: la política de privacidad tendrá que declarar a Resend,
  Notion, Google y Make como subprocesadores.
(commit: pending)

## [2026-08-05] F0 + F1 del plan de integraciones: la espina y el registro
- **TABLAS NUEVAS** (migración `integraciones_espina_y_registro`, ambas
  service-role-only): `integration_events` (dominio, tipo, payload, estado
  pendiente|enviado|fallido|descartado, intentos, destino) y `automations`
  (nombre, propósito OBLIGATORIO, dominio, disparador, sistemas[], criticidad,
  etapa propuesta→piloto→activa→pausada→deprecada, make_scenario_id).
- **ENUM NUEVO `integration_domain`** — el vocabulario compartido, que NO se
  inventó: es la taxonomía de las etiquetas de Gmail y los hubs de Notion.
  Espejo en `src/lib/integraciones/dominios.ts` con su etiqueta de Gmail al lado.
- **La regla de la espina**: las Server Actions NO llaman webhooks; insertan una
  fila (`emitEvent`, que nunca lanza) y el despachador la envía. Si Make está
  caído, un fetch dentro de una action revienta la operación del usuario; una
  fila pendiente se reintenta sola. Mismo criterio que el correo de Terratalento.
- **RUTAS NUEVAS**: `/api/integraciones/[canal]` (entrada, secreto POR CANAL,
  503 si falta) y `/api/cron/integraciones` (el despachador, cada 15 min en
  vercel.json). Reintento con techo de 5: un webhook mal configurado no puede
  quemar el presupuesto de operaciones de Make.
- **NODO/MÓDULO NUEVO `/ecp/automatizaciones`** en IT y Plataforma: registro con
  propósito y criticidad (que se contesta con «si esto se cae, ¿qué pasa?») +
  el pulso de la cola de eventos, con ping de prueba y reintento de fallidos.
  El trigger exige `make_scenario_id` para marcar activa/pausada: el registro no
  puede afirmar que algo corre si no existe donde corre.
- **BUG CAZADO EN LA PRUEBA DE VIDA, no leyendo**: el despachador reenviaba a
  Make los eventos ENTRANTES, dejando un bucle. Arreglado filtrando
  `destino is null` — lo entrante nace con el destino sellado.
- Guardián `scripts/qa-integraciones-check.mjs` (11/11): sobre todo que el
  vocabulario de TS y el enum de Postgres no se desalineen en silencio.
- **DICT sugerido**: «Espina de integración» y «Vocabulario compartido».
- PENDIENTE del owner: `INTEGRACIONES_SECRET_MAKE` y `MAKE_WEBHOOK_URL` en
  Vercel; sin ellos la puerta responde 503 y el despachador no manda nada (los
  eventos esperan, no se pierden).
(commit: pending)

## [2026-08-05] Grados de Calidad: UNA definición, en el Control Panel
- **El problema**: los grados estaban definidos en TRES sitios con TRES
  respuestas distintas, y dos de ellas eran material que se le enseña a un
  cliente. Notion «Conceptos Fundamentales» decía Black 80+ / Red 84+ / Blue 85+
  / Gold 87+ / Tyrian 89+; el pitch de Go To Market decía Blue 86+ / Gold 88+ /
  Tyrian 91+; la plataforma no tenía umbrales (el comité asignaba el grado).
  Las dos páginas de Notion se contradecían ENTRE SÍ.
- **El owner fijó los rangos definitivos (2026-08-05)** y ninguna de las
  versiones anteriores coincidía: Black 80–82.99 · Red 83–84.99 · Blue 85–86.99
  · Gold 87–87.99 · Tyrian 88–100. Rangos CERRADOS y CONTIGUOS, sin huecos ni
  solapes. Cada grado lleva además criterios cualitativos: clase de lote
  (nanolote/microlote/macrolote/cosecha), rareza de variedad, y disponibilidad
  por malla (solo de Blue hacia arriba).
- **FUENTE ÚNICA `src/lib/grados/definicion.ts`** — cualquier sitio que hable de
  grados sale de ahí. `bandaPorPuntaje()` se llama así a propósito: el puntaje da
  la BANDA, no el grado entero, porque los criterios cualitativos son parte de la
  definición (un 88 en macrolote de variedad común cumple la banda de Tyrian pero
  no su descripción).
- **NODO/MÓDULO NUEVO `/ecp/grados`** en Dirección: la escalera con sus
  criterios, consulta por puntaje, y una tabla de PROCEDENCIA que enseña las tres
  definiciones viejas junto a la oficial — para que se vea qué se corrigió.
- Guardián `scripts/qa-grados-check.mjs` (33/33): sobre todo la CONTINUIDAD de la
  escala. Un hueco (un café de 82.995) o un solape (dos grados reclamando el 87)
  no da error en ninguna parte: devuelve el grado equivocado y nadie se entera.
- Cazado al verlo: el `.card` compartido es flex con space-between —pensado para
  una fila de cabecera con acciones— y apilar contenido dentro reparte los hijos
  a lo ancho. La página usa su propio bloque.
- **PENDIENTE DEL OWNER, anotado en la propia página**: (1) ¿el puntaje ES el
  grado o solo la banda y el comité decide dentro? (2) «Mix» existe solo en el
  Cotizador Logístico, no en `lot_grade` — ¿grado, empaque, o comodidad de
  cotización? (3) las dos páginas de Notion siguen diciendo lo viejo y hay que
  actualizarlas DESDE aquí.
(commit: pending)
