Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V28.0(e7627dd).html
(CTC Web Platform, 2026-08-10). Cada entrada se compila al próximo Version Wrap (V29).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio (lección del wrap de V19, repetida en el de V28): registrar la entrada
AL TERMINAR el trabajo, no "después". Y sellar el sha en cuanto exista el commit —
en V27 hubo que reconstruir varias con git blame.

Lección propia del wrap de V28: **una entrada del log puede estar ya compilada**.
La del hito V3.0 se registró y se envolvió el mismo día, y al llegar aquí parecía
pendiente. Antes de compilar una entrada, comprobar en el HTML base si ya está
(se comprobó buscando la clave de DICT y la nota que pedía). Compilar dos veces
lo mismo es cómo el mapa empieza a contradecirse.

Contexto abierto al cerrar V28:

- **RT-Scriptor · la imagen real sigue sin salir.** El cableado entero funciona: el
  primer «Acción · imagen» en producción (2026-08-06) compuso el dibujo, lo
  rasterizó, viajó como referencia y Google contestó **429 — la clave no tiene
  cuota de generación de IMAGEN**. No es el modelo ni el prompt: el nivel gratuito
  de Gemini no la incluye. Se resuelve activando facturación en ese proyecto de
  Google AI o apuntando GEMINI_IMAGE_MODEL a un modelo que sí cubra el plan.
- **El escenario de Canva NO está montado en Make** — registrado en
  /ecp/automatizaciones en etapa «propuesta» con el contrato entero; blueprint y
  gotchas en docs/MAKE_RTS_CANVA.md. Al montarlo: pegar el scenarioId y pasarlo a
  piloto.
- **Direccionamiento: nadie ha pulsado «Dame una mano».** La persistencia sí está
  confirmada en vivo por el owner (la insignia «Guardado»); la llamada a Claude y
  —lo más importante— la corrección de la memoria (que el modelo cite la escala SCA
  y no el índice de precio embebido) siguen sin ejercitarse contra la API real.
- **Coffeed**: 5 medios sin feed (FNC, Perfect Daily Grind, Coffee ad Astra —es
  YouTube y no resolvió el channel_id—, Comunicaffe, ICO); calidad editorial por
  afinar; el muro sigue casi vacío (falta producir 2-3 capítulos reales).
- **Integraciones F4 (redes) y F5 (WhatsApp)** sin empezar. F4 ya no está bloqueada
  por el render a imagen: `raster.ts` lo resolvió de rebote.
- **GDPR**: la política de privacidad sigue sin escribirse y la lista de
  subprocesadores no para de crecer (Resend, Notion, Google, Make, Anthropic,
  Gemini). Cada día es más urgente.
- `qa-guard-check.mjs` no corre: necesita una cuenta QA de productor (procedimiento
  en la memoria del roster). Crear Prueba, correr, borrar.
- **Identity Value Creation** (4ª app del taller) sigue declarada y sin construir.
  Su pendiente real no es UI: leer finca/lote desde el tier de socios exige vistas
  SECURITY DEFINER estrechas por campo, no un grant ancho.
- El **socio multi-nodo** sigue exigiendo cambiar el PK de `partner_accounts`.
- Los **dos cotizadores y Anclas de mercado** siguen sin conducirse en navegador
  (el OCP tiene 2FA). Falta a mano: guardar un borrador, recargar y ver que la
  calculadora vuelve donde estaba.
- El owner tiene 3 cotizaciones de prueba en producción; las secuencias quedaron
  por encima del máximo — NO reiniciarlas sin volver a comprobarlo o el siguiente
  código choca contra el índice único de `code`.
- Sigue pendiente del owner: material de la landing de Varietales; pase de copy
  Specialty/Black sobre KR ProcessFlow y CP BlackSection.
- El bucket de Storage sigue sin poda automática del Buzón.

## [2026-08-10] La memoria de Direccionamiento, probada contra la API real
- El boton «Dame una mano» no se puede pulsar en automatico (login maestro con
  OTP), asi que se hizo probable lo que hay DETRAS. La composicion del texto de
  la memoria y el system prompt salieron del Server Action a un modulo PURO
  nuevo, `src/lib/direccionamiento/memoria.ts` (`textoMemoria`,
  `escalaCanonica`, `SISTEMA_REDACCION`); el action se queda con la compuerta.
  Patron de la casa: modulo puro + guardian (terminos.ts, llamadoEmail.ts,
  model.ts de RT-Scriptor).
- Guardian nuevo `scripts/qa-direccionamiento-check.mjs`, 20 comprobaciones.
  Lo que lo hace util: LEE EL PARRAFO EN CONFLICTO DEL .jsx REAL —el que cita
  los grados como indice de precio sobre base 100— y lo mete en el prompt tal
  cual, igual que hace el componente. No es un conflicto de laboratorio.
  Con `--live` hace la llamada REAL al modelo.
- RESULTADO DE LA CORRIDA EN VIVO (2026-08-10, 13 s): la memoria GANA. Las tres
  redacciones citaron la escala SCA (Black 80–82.99, Gold 87–87.99, Tyrian
  88–100) y NINGUNA cito el indice de precio que tenia delante en el mismo
  prompt. El contrato de vuelta tambien: parseJson, array, cada item con
  titulo y texto.
- Tres comprobaciones vigilan el acuerdo con el autor: que el .jsx SIGA
  trayendo su parrafo, que siga estando mal, y que NADIE lo haya editado para
  arreglarlo (si aparece «82.99» ahi dentro, alguien toco el archivo del autor
  en vez de la memoria). Si el autor publica una version nueva y el conflicto
  cambia, el guardian se entera.
- Lo que SIGUE sin probarse: `requireActiveAdmin()` (hace falta cookie de panel)
  y el viaje completo por el navegador. La persistencia si esta confirmada en
  vivo por el owner.
- Nodo/DICT al wrap: una linea en `direccionamiento` — la memoria ya no es una
  promesa, esta medida contra la API real.
- APP_VERSION 3.8 -> 3.9.
(commit: e43f272)
