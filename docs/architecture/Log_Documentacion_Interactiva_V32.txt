Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V32.0(5f8163e).html
(CTC Web Platform, desde 2026-08-14). Cada entrada se compila al próximo Version Wrap (V33).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio: registrar la entrada AL TERMINAR el trabajo, no «después». Y sellar
el sha en cuanto exista el commit.

Lección heredada del wrap de V28: **una entrada del log puede estar ya compilada**
si se registró y se envolvió el mismo día. Antes de compilar, comprobar en el HTML
base si ya está (buscar la clave de DICT o la nota que pedía).

⚠️ LECCIÓN DEL WRAP DE V31 — cómo se buscan las claves del DICT.
El DICT tiene DOS estilos de escritura conviviendo: `clave:{k:"…"` (compacto, una
línea) y `clave: {` (con espacio, en varias líneas). Buscar con `^  clave:{` SOLO
encuentra las del primer estilo — eso hizo creer que tres claves no existían y
estuvieron a punto de quedar DUPLICADAS (JS se queda con la última y la ficha
habría sido la vieja, en silencio). Busca siempre con espaciado flexible:
    grep -nE '^[[:space:]]*"?clave"?[[:space:]]*:[[:space:]]*\{'
y confirma en el navegador que `Object.keys(DICT).length` sube EXACTAMENTE lo que
esperas. En el wrap de V32 se hizo así: 95 → 99, cuatro claves nuevas, cuadra.

⚠️ LECCIÓN NUEVA DEL WRAP DE V32 — el mapa de archivos delata los borrados.
Al regenerar FILETREE, la comprobación de ANN huérfanos encontró
`src/app/robots.ts`, que este mismo pase había borrado. El script de FILETREE lee
el disco, pero ANN es a mano: **cuando un wrap borra o renombra un archivo
anotado, hay que repuntar su ANN** o el mapa apunta a un fichero que ya no está.
La comprobación que lo caza:
    orphanAnn = Object.keys(ANN).filter(k => !pathsDeFILETREE.has(k))
debe volver VACÍA antes de guardar.

Contexto abierto al cerrar V32 (2026-08-14):

- **Los dos vídeos apuntan al MISMO id provisional** (`Yird1_j6yqo`): el de la
  cabecera de «Tres ofertas» en CTC Home y el de «Bienvenidos al Kaffetal Regal».
  Cuando exista el vídeo real de KR, es cambiar `BIENVENIDA_VIDEO_ID`.
- **Los sitemaps no se anuncian solos**: los buscadores no sabrán que existen
  hasta que vuelvan a leer el `robots.txt` de cada subdominio. Enviarlos a mano
  en Search Console es cuenta del owner, no código.
- **La deuda del loop de iconos**: ~2,6 MB por 240 px y 8 fps. Si hay que
  adelgazarlo más, lo que queda es recortar la duración o el número de iconos —
  decisión de contenido, no de codificación. Receta anti-fantasma en
  `components/ctc-home/Hero.module.css` y en la ficha «WebP animado con alfa».
- **Co-Create sigue renombrado a medias**: el hub dice «Cherry Picked Co-Create»,
  su landing (Clase B) sigue diciendo «CTC Co-Create». La tarjeta de enlace dice
  lo que dice la página, no lo que dirá — si se adelanta, el enlace compartido y
  la página a la que lleva se llamarían distinto.
- Todo lo demás heredado de V31 sigue vivo: la cola diferida de la auditoría (83
  índices FK, poda del Buzón, carreras de `useAutosave`, tableros sin tope),
  `/api/kaffetal-regal/next-step` sin llamador POR DECISIÓN, la robusta de
  Londres y los inventarios ICE sin fuente gratuita, el hub de Cherry Picked sin
  captación de correo (el CHECK solo admite roast|x|ctc-home), el escenario de
  Canva sin montar en Make, Gemini sin tarifa en `precios.ts` (📌 el 2026-08-31
  caduca el precio de lanzamiento de Sonnet 5), Coffeed con 5 medios sin feed,
  la política de privacidad GDPR sin escribir, `qa-guard-check.mjs` sin cuenta QA,
  Identity Value Creation declarada y sin construir, el socio multi-nodo pendiente
  del PK de `partner_accounts`, los tres cotizadores sin conducir por 2FA, y la
  protección de contraseñas filtradas bloqueada por plan.

## [2026-08-14] Datos estructurados (JSON-LD) en las tres superficies clave
- La red no declaraba NINGUN schema.org — cero en todo el repo. Es lo que
  convierte una pagina en ficha para Google, y lo mas parecido a hablarle en su
  idioma a un modelo de lenguaje: un hecho declarado sin ambiguedad.
- NUEVO: `src/lib/seo/jsonLd.ts` — constructores de las fichas. REGLA: aqui no
  se escribe ningun dato. Todo sale de la fuente unica que ya lo posee:
  `lib/legal.ts` (razon social, NIT, sede, correo), `lib/grados/definicion.ts`
  (los cinco grados y sus rangos SCA) y `lib/kaffetal/faq.ts` (las 12 preguntas).
- NUEVO: `src/components/JsonLd.tsx` — componente de SERVIDOR (sin "use client"
  a proposito: el rastreador tiene que verlo en la primera respuesta). Escapa
  `<` a \u003c POR CONSTRUCCION: dentro de un <script> el navegador busca el
  cierre en texto crudo, asi que un dato con `</script>` cerraria la etiqueta
  antes de tiempo. Hoy todo sale de constantes de la casa, pero la garantia
  tiene que sobrevivir al dia que un campo sea administrable desde el panel.
- REFACTOR HABILITANTE: el diccionario de las 12 preguntas SALIO de
  `FaqSection.tsx` a `src/lib/kaffetal/faq.ts` (modulo puro, patron de
  `harvestYear.ts`/`definicion.ts`). Ahora tiene DOS lectores —la seccion que
  las pinta y el constructor de FAQPage— y duplicarlas habria garantizado que
  la respuesta publicada y la visible se separaran al primer cambio de cifra.
  Habilita ademas el paso siguiente del plan: URL propia por pregunta.
- QUE EMITE CADA SUPERFICIE (un solo `@graph` por pagina, para que las
  referencias por `@id` resuelvan dentro del documento):
  · CTC Home    → Organization + WebSite. Es la que DECLARA la empresa.
  · Kaffetal Regal → Organization + FAQPage (12) + DefinedTermSet (5 grados).
  · Cherry Picked  → Organization + DefinedTermSet.
  La Organization se repite en cada subdominio A PROPOSITO: Google los trata
  como sitios aparte, asi que una referencia `@id` suelta a otro dominio se
  quedaria sin nada a lo que apuntar.
- DECISION: los grados son `DefinedTermSet`, NO `Product`/`Offer`. No son cinco
  cosas a la venta con precio: son los cinco terminos de una escala que la casa
  define. Es la figura que schema.org tiene para un vocabulario propio y la que
  un modelo puede citar sin inventarse un precio.
- `organizationLd()` lleva `knowsAbout` (7 temas) — la propiedad que mas rinde
  de cara a un modelo: la lista explicita de en que es competente esta empresa.
- Verificado en vivo: las tres emiten JSON valido en el HTML del servidor
  (parseado, no grepeado), 0 referencias `@id` colgantes, 12 preguntas, los
  cinco grados con su rango leido de definicion.ts, y el FAQ sigue pintando sus
  12 fichas tras la extraccion.
- COBERTURA COMPLETA el mismo dia: las 19 superficies publicas emiten
  Organization. Las 16 restantes via el componente de conveniencia
  <OrganizationLd /> (una linea por pagina; el dinamico socios/[partner] cubre
  los 5 nodos de golpe). En los socios se declara a CTC, NO al socio: los cinco
  nodos son puertas de esta casa, y la ficha del socio como organizacion propia
  no la podemos afirmar nosotros. Verificado 19/19 contra el HTML del servidor,
  con JSON parseado y taxID correcto en cada una.
- PENDIENTE del plan SEO (artifact «Visibilidad de la red CTC»): la decision del
  canonical (subdominio vs dominio raiz) y Search Console — las dos son del
  owner. Candidato inmediato de UNA linea: anadir gradosLd() a Cherry Picked
  Green, que es la tienda donde el grado ES la taxonomia del producto.
(commit: pending)

## [2026-08-14] El rastro del cacao: venia del indice, no del codigo
- SINTOMA: buscar «Colombian Trading Company» devolvia a CTC descrita como
  exportadora de «specialty coffee or cocoa». El cacao fue producto en una
  iteracion temprana de la compania y ya no lo es.
- DIAGNOSTICO (investigado, no supuesto): el sitio VIVO esta limpio — se
  descargo ctcexport.com y no tiene ni una mencion de cacao/cocoa/chocolate.
  Pero los buscadores siguen sirviendo copy de posicionamiento ANTIGUO de la
  propia casa: «transforma en origen, reduce intermediarios y entrega cafe y
  cacao premium al mundo», atribuido a ctcexport.com en dos busquedas
  independientes. La pagina cambio; el indice no. Y si esa frase entro al
  entrenamiento de un modelo, ahi no hay boton de borrar.
  El titubeo del propio resultado —«coffee or cocoa, DEPENDING ON the specific
  agricultural assessments»— es un modelo reconciliando una fuente vieja que
  dice cacao con una nueva que dice cafe.
- LO UNICO QUE SE TOCO: el <title> de public/tools/mermas-rapida.html, que
  decia «Calculadora de Rendimiento para Cafe y Cacao». Esos 12 HTML son URLs
  INDEXABLES (el matcher del proxy excluye /tools y robots.txt no lo cubre) y
  la herramienta es web:true/tier:default, o sea visible para anonimo — asi que
  ese titulo sembraba la palabra desde el propio dominio. Ahora dice
  «Calculadora de Rendimiento · CTC», igual que su propio <h1>.
- LO QUE NO SE TOCA, Y ES UNA REGLA: la HERRAMIENTA conserva su modo cacao
  ENTERO y funcionando (verificado: se pulsa 🍫 Cacao, se meten 1000 kg y da
  Crudo 1000 → Seco 505 → Grano Tostado 255 → Nibs 128.8, 0 errores). Y las
  menciones de cacao/cocoa/chocolate en rueda-catacion.html, viaje-cafe.html y
  fichaData.ts son VOCABULARIO DE CATA — «Frutos secos / Cacao» es categoria de
  la rueda SCA y las notas achocolatadas describen variedades. Son
  INDEPENDIENTES del producto y no se tocan jamas. El producto se fue; el
  descriptor se queda.
- LECCION DE PROCESO (mia, vale anotarla): el encargo era AVERIGUAR de donde
  venia la mencion. La primera pasada retiro el modo cacao entero de la
  herramienta — mucho mas de lo pedido. Se cerro sin fusionar (PR #18) y se
  rehizo como el cambio de una linea que era. Investigar != amputar.
- QUE SI PUEDE MOVER LA AGUJA, y no es codigo: (1) Search Console, para que se
  re-rastree y se reemplace la descripcion cacheada; (2) el JSON-LD ya juega a
  favor — `knowsAbout` nombra cafe, trazabilidad, EUDR y SCA, y NO nombra
  cacao; (3) buscar donde mas vive ese copy viejo fuera de la web (LinkedIn de
  empresa, ProColombia, directorios, algun pitch subido) — eso si es
  actualizable por el owner.
- HALLAZGO COLATERAL, no arreglado: de los 12 tools indexables solo DOS llevan
  meta description y sus titulos son inconsistentes (uno dice «Cool PDF — give
  your pages a body»). Superficie SEO que nadie gobierna — es el argumento mas
  concreto a favor del modulo «Manejo de Plataformas».
(commit: pending)

## [2026-08-14] «Co-Create» pasa a llamarse CaaS · Coffee as a Service
- ENCARGO DEL OWNER: cambiar el TERMINO, no lo que representa. El subdominio
  caas.ctcexport.com ya estaba creado en Hostinger y en Vercel antes de tocar
  codigo. De paso se cerro un renombrado a MEDIAS que la casa arrastraba desde
  hacia tiempo: el hub de Cherry Picked decia «Cherry Picked Co-Create» y la
  landing decia «CTC Co-Create». Ahora las dos dicen CaaS.
- RUTAS: la nueva es /caas (git mv de app/co-create → app/caas, para que el
  historial siga al archivo). La vieja /co-create SIGUE VIVA y reenvia con un
  308 permanente — hay enlaces sueltos, tarjetas OG ya indexadas y un
  subdominio que el owner no quiere apagar. La consola interna tambien se
  movio: /bcp/co-create → /bcp/caas.
- SUBDOMINIOS (src/lib/red/subdominios.ts): las DOS entradas —«caas» y
  «co-create»— quedan, y apuntan a rutas DISTINTAS a proposito. El mapa inverso
  ROUTE_SUBDOMAIN se DERIVA de ese objeto, asi que si las dos apuntaran a
  /caas la ultima ganaria y las tarjetas OG de la superficie se firmarian con
  el dominio viejo. Queda anotado en el archivo mismo, no solo aqui.
- EL REDIRECT ES ABSOLUTO EN PRODUCCION, y esto es la trampa: en
  co-create.ctcexport.com el proxy antepone la base del subdominio, asi que un
  redirect("/caas") relativo se resuelve a /co-create/caas → 404. En dev si es
  relativo. Verificado en local: 308 → /caas.
- ⚠️ LO QUE NO SE RENOMBRO, Y ES DELIBERADO: el pilar del lead sigue siendo
  `cocreate`, en minuscula. Es la clave interna de leads.pillar, vive bajo un
  CHECK de Postgres ('general','tech','cocreate','varietales') y ya tiene
  filas reales. Renombrarla exigiria migracion + reescritura de historico para
  cambiar algo que ningun usuario ve. LA MARCA ES CaaS; LA CLAVE ES cocreate.
  Anotado en los tres archivos que lo tocan (app/caas/page.tsx,
  lib/leads/actions.ts, lib/email/leadEmails.ts) para que la proxima sesion no
  «arregle» la inconsistencia.
- FORMULARIO (components/ctc-home/ContactModal.tsx), tres cosas nuevas:
  (1) «Otros» en Mercado — al elegirlo aparece un campo de texto libre
  (mercadoOtro); (2) acordeon «Necesidades particulares», opcional y plegado:
  Incoterm (En Colombia / En destino), Periodicidad (Mensual, Bi-Mensual,
  Trimestral, Semestral, Anual) y Certificaciones en seleccion MULTIPLE
  (Organico, Fairtrade, Rainforest Alliance, UTZ, Bird Friendly, Kosher, Halal,
  Denominacion de origen, Sin certificacion). Todos viajan en el mismo `fields`
  JSON del lead: no hubo migracion. Lo unico que decide que se guarda es la
  lista blanca FIELD_KEYS.cocreate en lib/leads/actions.ts — un campo que no
  este ahi se descarta en silencio, asi que ese array es el sitio a tocar.
- LANDING: seccion nueva «Modelos de oferta comunes» (components/services/
  CaasLanding.tsx, ex CoCreateLanding.tsx), trilingue. Tres arquetipos que
  desactivan tres objeciones distintas de un comprador de marca: «Lujo del
  Origen» (no canibalizar el portafolio actual), «Piso Firme» (dejar de estar
  expuesto al vaiven del mercado) y «La Historia Completa» (tener con que
  contar la historia y con que cumplir EUDR sin pedir dos veces el dato). El
  primero lo redacto el owner; los otros dos se escribieron aqui.
- DOC: nueva seccion en HANDOFF.md y la fila de la tabla de superficies pasa a
  «CaaS · Coffee as a Service | /caas (/co-create → 308) | caas.* (+
  co-create.*)». Para el mapa interactivo: el nodo de la superficie cambia de
  nombre y de ruta, y el DICT gana la distincion marca-vs-clave.
- LECCION DE PROCESO: un script de insercion POR INDICE metio el bloque
  castellano DENTRO de la definicion del tipo y corrio los idiomas (el ingles
  cayo en el dict `es`). tsc lo caso al vuelo (TS2300/TS2717/TS2322) y se
  recupero con `git show HEAD:...`. En un Record<Lang, Dict> con tres bloques
  casi identicos, insertar por indice es apostar; hay que anclar en una cadena
  UNICA de cada idioma.
- Gate: tsc limpio, eslint 27 warnings (base), build exit 0. APP_VERSION 4.4 → 4.5.
(commit: pending)
