Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V36.0(48bac75).html
(CTC Web Platform, desde 2026-08-17). Cada entrada se compila al próximo Version Wrap (V37).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio: registrar la entrada AL TERMINAR el trabajo, no «después». Y sellar
el sha en cuanto exista el commit — de UNO en UNO, no en bloque al final (en V33 se
sellaron tres asientos con el sha equivocado por hacerlo en bloque, y en V35 tres se
quedaron en «pending»; los dos fallos se corrigieron a mano el 2026-08-17).

════════════════════════════════════════════════════════════════════════════════
CÓMO SE VALIDA UN WRAP — la lista que de verdad hay que correr
════════════════════════════════════════════════════════════════════════════════

✅ LA BATERÍA YA ES UN ARCHIVO (lección de V36): `docs/architecture/validate_snapshot.mjs`.
Se corre en NODE, no en la consola del navegador (el panel de vista previa de esta
máquina no navega a un archivo suelto). Lee el HTML, extrae los literales
`DICT`/`WIRES`/`SCENARIOS`/`CTX`/`ANN`/`FILETREE` casando corchetes y los evalúa
con `new Function`. Uso, desde `ctc-platform/`:

    node docs/architecture/validate_snapshot.mjs docs/architecture/<nuevo>.html --prev docs/architecture/<anterior>.html [--dump]

`--prev` imprime los contadores del snapshot anterior al lado de los del nuevo, para
comprobar que suben EXACTAMENTE lo esperado; `--dump` lista nodos, claves de DICT,
ids de escenarios y CTX. Correrlo también contra el snapshot ANTERIOR como control:
si pasa en los dos, no es que sea permisivo.

Las NUEVE comprobaciones, y las nueve tienen que volver VACÍAS:
  1. pasos de traza (`q.n` y `q.from`) contra los `.node[id]` reales
  2. `DICT[k].a[]` contra las claves de DICT
  3. `WIRES` contra los nodos reales
  4. `CTX` — nodo real Y claves reales
  5. `ANN` huérfanas: rutas que ya no existen en el FILETREE recién regenerado
  6. `ANN[...].info` contra las claves de DICT
  7. `.ibtn[data-info]` del marcado contra DICT
  8. `.chip[data-info]` — SOLO sobre el marcado ANTERIOR a `<script>` (después
     están las plantillas que GENERAN chips y siempre saldrían como falso positivo)
  9. claves REPETIDAS en el texto de DICT/CTX (JS se queda con la última en silencio;
     lección de V31 y V35) — la novena, nueva en V36

Y los contadores tienen que subir EXACTAMENTE lo esperado
(V32: 95→99 · V33: 99→102 · V34: 102→104 · V35: 104→107 · V36: 107→113).

Además, después de `gen_filemap_tree.mjs`, un `node --check` del bloque `<script>`
extraído (el validador evalúa los literales, no el resto del motor).

⚠️ EL MAPA NODO→FICHAS SE LLAMA `CTX`, NO `NODEDICT` (lección de V34). **Ningún
chequeo de un wrap debe ir envuelto en un guard de existencia**: si el símbolo no
existe, tiene que romper.

⚠️ EL SELECTOR DE TRAZAS VA POR ÍNDICE (lección de V33): los `option.value` son
"0","1","2"…; la comprobación buena es `select.options.length === SCENARIOS.length`
y que el `id` esté en `SCENARIOS.at(-1)`.

⚠️ EL DICT TIENE DOS ESTILOS DE ESCRITURA conviviendo (lección de V31):
`clave:{k:"…"` y `clave: {` en varias líneas. Nunca contar por regex: el número
bueno es `Object.keys(DICT).length` evaluado de verdad (el validador lo hace).

⚠️ EL MAPA DE ARCHIVOS DELATA LOS BORRADOS (lección de V32): `gen_filemap_tree`
lee el disco, pero ANN es a mano — al borrar o mover un archivo anotado hay que
repuntar su ANN (en V36: `docs/PROXIMA_SESION.md` → `docs/archive/…`).

⚠️ ESTE DOCUMENTO GUARDA HISTORIA, NO SOLO ESTADO (lección de V33): al renombrar
algo se cambia solo el texto en PRESENTE; las notas fechadas conservan el nombre de
entonces. Donde una nota vieja cita una RUTA que ya se movió, se anota entre guiones.

Y la de V28: una entrada del log puede estar YA compilada si se registró y se
envolvió el mismo día. Antes de compilar, comprobar en el HTML base si ya está.

⚠️ EL SHA DEL NOMBRE ES EL DEL CONTENIDO (lección de V35): si la tanda no está
comiteada, envuelve igual con HEAD y renombra después; tras renombrar, VOLVER a
correr `gen_filemap_tree.mjs` (el snapshot se lista a sí mismo) y el validador.
V36 salió con `48bac75` = HEAD al envolver; el commit del propio wrap + el barrido
de docs de ese día quedan como los primeros asientos de este log.

════════════════════════════════════════════════════════════════════════════════

Contexto abierto al cerrar V36 (2026-08-17):

- **Lo que V36 compiló**: el módulo OCP · Transcripciones entero (V4.8→V4.15: archivo,
  cola de audio, nube AssemblyAI probada, latido/tirón, .bat, instalador, herramienta en
  el repo) como nodo `n-transcripciones` + nodo `n-transcriptor` (la herramienta local,
  en la zona de Clientes: es lo único del sistema que corre en un PC), escenario
  `notavoz` con la bifurcación equipo|nube, seis fichas nuevas (`transcripciones`,
  `transcriptor`, `modelotiron`, `transcribirnube`, `repopublico`, `infrapro`), el atajo
  del rail en `direccionamiento`/`manejoplataformas`, BCP = **Base** Control Panel,
  CaaS ≠ CommaaS en la ficha `caas`, Vercel a 19 subdominios + Pro, la máquina local
  con sus guardianes y `navActivo.ts`, y la batería como archivo.
- **La reorganización de consolas del board del owner (2026-08-17) NO está en el mapa
  como estado — está como aviso** en `direccionamiento` y en la ANN del PDF: es
  PLANEADA. Cuando se construya, cambian `PanelDiagram` (EstructuraModal.tsx), la ficha
  `panels`, la de `bcp`/`consoles`, y este mapa entero de módulos por consola. Plan y
  preguntas abiertas: `docs/ESTADO_Y_PREGUNTAS_2026-08-17.md` (§2.2, §5).
- **Pendientes del owner que este log arrastra**: Search Console (www + sitemap nuevo,
  urgente desde el canonical), política GDPR (subprocesadores: Resend, AssemblyAI,
  Anthropic, Google), número Nequi de la Arena, material de Varietales, la puerta gris
  de Terratalento, los dos vídeos con id provisional `Yird1_j6yqo`, Gemini sin tarifa
  (📌 2026-08-31 caduca el precio de lanzamiento de Sonnet 5), Canva en Make, Coffeed
  con 5 medios sin feed, y activar la protección de contraseñas filtradas (ya no
  bloqueada por plan).
- **Deuda consciente que sigue viva**: la cola diferida de la auditoría (83 índices FK,
  poda del Buzón, carreras de `useAutosave`, tableros sin tope); `/api/kaffetal-regal/
  next-step` sin llamador POR DECISIÓN; robusta de Londres e inventarios ICE sin fuente
  gratuita; `qa-guard-check.mjs` sin cuenta QA; Identity Value Creation declarada y sin
  construir; socio multi-nodo pendiente del PK de `partner_accounts`; los cotizadores
  sin conducir por 2FA; la credencial estrecha del worker (RPC con token propio) en el
  backlog hasta que alguien que no sea el owner corra uno; los `<title>`/meta de las 12
  herramientas de `public/tools/` dentro de cada archivo; el riesgo aceptado del
  registro de herramientas (`allow-same-origin`); el loop de iconos ~2,6 MB + vídeo del
  hub 826 KB; el pilar del lead sigue `cocreate` (marca CaaS) y el hub sin captación de
  correo, los dos a propósito.
- **Infra**: $45/mes (Supabase Pro CTC $25 + Vercel Pro $20; el hub de CommaaS sigue en
  Free). Repo PÚBLICO por decisión hasta que se arregle el acceso de la GitHub App de
  Vercel. Vercel y Supabase bajo ctcexportmain@gmail.com.
- **Siguiente en el orden acordado**: (2) la mudanza del workspace fuera de OneDrive
  (`_WORKSPACE-MOVE-PLAN.md`, sesión fresca) → (3) la reorganización de consolas con
  plan propio → hub CommaaS en sus propias sesiones.

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V36 y el barrido de docs, comiteados; el owner cierra la segunda ronda de preguntas
- `e526708` lleva el snapshot `Documentacion_Interactiva_V36.0(48bac75).html`, el Log V36, el
  validador, el barrido de HANDOFF/logs/NUBE, el archivo de PROXIMA_SESION y
  `docs/ESTADO_Y_PREGUNTAS_2026-08-17.md`. El sha del nombre (`48bac75`) es el HEAD al envolver
  = el último commit de CONTENIDO (todo lo de hoy es doc); no se renombra.
- El owner respondió «defaults on all follow-ups»: F1–F14 quedan decididas como sus valores por
  defecto (registro en §0 del doc de estado). Para la doc, cuando se construya: ECP sigue siendo
  *Executive*; las URLs se mueven con los módulos; «hub» se reserva para CommaaS (la portada de
  Cherry Picked y el selector de consolas dejan de llamarse así — repuntar las fichas `cphub`,
  `panels` y `masterlogin` cuando toque); HC pasa a membresía con concha propia y grants por
  herramienta; CTC Selection = paraguas, Black Stock = su rama Black.
- Nada de eso está construido: este asiento solo deja constancia de la decisión.
- Verificado tras el push: Vercel construyó `aa7e521` en producción (la tubería sigue sana tras
  el incidente del repo privado) y la sonda del callback de AssemblyAI responde 401 → el secreto
  rotado está configurado en producción (F11 cerrada).
Sin cambio de APP_VERSION.
(commit: aa7e521)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] Plan de la reorganización escrito: `docs/V5_CONSOLAS_PLAN.md` (+ el módulo «Sneak Peek» al frente)
- Paso (i) de F14 cumplido: el plan PR por PR de la reorganización de consolas ya existe como
  documento propio. Se escribió desde el workspace nuevo (`C:\dev\ctc-platforms`) y se contrastó
  contra el código real, no contra el recuerdo: `consoles.ts`, el árbol de rutas de las tres
  consolas, `qa-nav-check.mjs` + `navActivo.ts`, las tres compuertas de `lib/panel/`,
  `next.config.ts`, el esquema vivo de Supabase (definición de la vista `public_lot_catalog`,
  columnas de `lots`/`lot_listings`/`fincas`, enums) y `MarketTicker` como patrón de cinta.
- **Novedad del owner del 2026-08-17**: un módulo reutilizable nuevo, la **«Active Catalogue Sneak
  Peek»** — cinta pública de tarjetas de lote SIN datos comerciales para Home y las landings — pasa
  al FRENTE de la lista, antes de la reorganización, con **7 lotes mock** rotulados «Temporada
  anterior» mientras ningún lote ha terminado el camino KR → Arena (hoy: 3 lotes en borrador, 0
  galardonados, 0 publicaciones, 0 filas en `public_lot_catalog`).
- El owner aceptó **todos los defaults D0.1–D0.8** y precisó que el módulo es **el mismo en todas
  las landings de Cherry Picked y REEMPLAZA el catálogo directo** que hoy se ve sin sesión: el
  Catálogo Activo completo queda solo detrás del login (D0.5 opción (a), extendida a la familia CP).
  Consecuencia: la parrilla de Green deja de ser rastreable y los precios salen de la internet
  pública — que es justamente lo que se buscaba.
- Los 7 mock se construyeron con **referencias REALES** de la base de Notion «📋 Fichas Técnicas de
  Café» (11 fichas, 7 con datos), con origen desde su base «Fincas» y la escalera desde «Grados».
  **El grado se DERIVA del puntaje SCA, no se copia de Notion**: la relación `Grado CTC` de Notion
  contradice su propia columna `SCA` en 6 de 7 fichas (84.25 → «Tiryan», 87.0 → «Black»…), y la
  regla 1 de `grados/definicion.ts` es que el puntaje manda. Queda registrado en el §9 del plan
  como higiene de datos pendiente del owner, junto con «Tiryan» mal escrito y su `Definicion` que
  dice SCA+89 mientras su propio `Min SCA` dice 88.
- El «Borbón Rosado - Natural [La Pradera]» (SCA 88.5 → **Tyrian**) queda FUERA de la cinta: Tyrian
  es solo de subasta y `publishLot` lo rechaza en el catálogo. La escalera final es 2 Gold · 2 Blue
  · 2 Red · 1 Black, lo que sustituye a la inventada del default D0.7.
- Los mock quedan marcados para poder borrarlos de un tirón (petición del owner): un solo archivo
  `sneakPeekMock.ts` con receta de retirada en su cabecera, `mock: true` en cada entrada, ids en el
  espacio reservado `mock-lote-NN`, el rótulo de temporada guiado por el dato y un guardián nuevo
  (`qa-sneak-peek-check.mjs`) que falla si algo de eso se rompe. Además se retiran solos: en cuanto
  haya 7 lotes vivos, los mock dejan de aparecer.
- Nada de esto está construido — el plan es documento. El orden queda: paso 0 Sneak Peek (V4.16) →
  (i) congelar nombres (4.17) → (ii) mudanza de rutas PR-A/B/C (4.18/4.19/4.20) + **Wrap V37** →
  (iii) módulos nuevos (4.21–4.25) → (iv) HC → (v) submódulos en KR → el owner declara **V5.0**.
- `docs/HANDOFF.md` apunta al plan desde su cabecera. Radio de la mudanza ya medido para no
  descubrirlo tarde: 127 rutas `/bcp/` + 57 `/ecp/` + 34 `/ocp/` escritas a mano y 20 archivos con
  `revalidatePath` de consola.
Sin cambio de APP_VERSION (documentación).
(commit: 00c9721)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.16 — «Active Catalogue Sneak Peek»: la cinta del catálogo, y el catálogo detrás del login
- Paso 0 del plan (`docs/V5_CONSOLAS_PLAN.md` §1) CONSTRUIDO. Módulo reutilizable nuevo
  `components/catalogo/SneakPeek.tsx` (+ su hoja): una cinta de tarjetas de lote SIN nada
  comercial, montada en SEIS superficies — CTC Home (tras el hero), la landing de Kaffetal
  Regal, la portada de Cherry Picked, la tienda Green, Roast y X.
- **Cambio de cara pública** (decisión del owner, D0.5 extendida a toda la familia CP): en la
  tienda Green la cinta SUSTITUYE a la parrilla para quien no ha entrado. `loadCatalog()` ya no
  corre sin sesión, así que los lotes y sus precios dejan de ser públicos; `GradosSection` y
  `BlackSection` solo se pintan con sesión y la cinta hereda el ancla `grados`. Al cerrar sesión
  se vacían los lotes. El índice rápido deja de ofrecer «Black Selection» a quien no ha entrado
  (esa sección no existe sin sesión). Consecuencia asumida: la parrilla deja de ser rastreable;
  la portada, las fichas de grado y la propia cinta siguen siéndolo.
- El dato sale SOLO de la vista estrecha `public_lot_catalog` (la de la tienda) cruzada con los
  `lot_listings` en `published` — la vista incluye `sold_out` y un teaser no debe anunciar lo
  agotado. El tipo `SneakPeekLot` NO TIENE dónde poner precio, MOQ, kilos, anticipo ni fecha de
  llegada: la garantía es estructural, y el guardián nuevo la comprueba campo por campo.
- `api/catalogo/sneak-peek` con el mismo montaje que la cinta de mercado (`force-dynamic` +
  `s-maxage=900`): las seis páginas siguen siendo ESTÁTICAS y un lote publicado aparece sin
  desplegar. Si la petición falla, la cinta no se dibuja.
- **7 lotes mock** (`lib/catalogo/sneakPeekMock.ts`) con referencias REALES de la base de Notion
  «📋 Fichas Técnicas de Café» del owner: 11 fichas, 7 con datos. Escalera 2 Gold · 2 Blue ·
  2 Red · 1 Black. El grado se DERIVA del puntaje (`gradoPorPuntaje`), porque la relación
  `Grado CTC` de Notion contradice su propia columna `SCA` en 6 de las 7 fichas — anotado en el
  §9 del plan como higiene pendiente del owner. El lote de 88.5 (Tyrian) queda fuera: es solo de
  subasta. Dos huecos rellenados por mí y dos discrepancias de la fuente van marcados en el
  archivo (`// GAP:` y `// ⚠`), no arreglados en silencio.
- Retirada de un tirón, como pidió el owner: un solo archivo con la receta en su cabecera,
  `mock: true` en cada entrada, ids en el espacio reservado `mock-lote-NN`, rótulo de temporada
  en el DATO (no en el componente) y un guardián que falla si algo de eso se rompe. Y se retiran
  SOLOS: el relleno solo llega a siete tarjetas, así que cada lote real desplaza a un mock.
- Guardián nuevo `scripts/qa-sneak-peek-check.mjs` — **56 comprobaciones**: nada comercial en el
  tipo, solo la vista pública, Tyrian fuera, la cinta no se cae ni se pinta vacía, movimiento
  reducido respetado, los 7 mock marcados/rotulados/coherentes con su puntaje, un solo sitio con
  datos mock, y la compuerta de la tienda (`loadCatalog` después de la guarda de sesión).
- `NarrativaSection` recibe `loggedIn`: su vacío tenía UNA causa («aún no hay lotes publicados») y
  ahora tiene dos. Decirle eso a quien no ha entrado sería mentirle, así que el rótulo se elige.
- Verificado además con Chrome headless, que es lo que destapó dos cosas que ninguna aserción
  habría visto: el sello del grado de 420×420 reducido a 36 px era una mancha gris ilegible (se
  sustituyó por el nombre del grado sobre su color oficial) y el puntaje aparecía sin escala (se
  le puso la etiqueta «SCA»). El rótulo largo de temporada se partía cortando el año por el
  guion: se acortó a «Temporada anterior · 2025-26».
- Compuerta: `tsc` limpio · `eslint src` 0 errores y 27 avisos, TODOS preexistentes (los archivos
  nuevos no añaden ninguno) · `npm run build` exit 0 con las seis superficies aún estáticas ·
  guardianes `qa-sneak-peek-check` 56/56, `qa-nav-check` 18/18, `qa-grados-check` 44/44.
- ⚠️ **Encontrado al correr la compuerta, NO introducido aquí**: `node_modules/pino` estaba como
  carpeta VACÍA (daño de la mudanza desde OneDrive) y tumbaba el build desde `imapflow` →
  `lib/buzon/mailClient.ts`; se reparó con `npm install` sin tocar los manifiestos. Y `npm audit`
  ya no está en 0: 3 vulnerabilidades altas por un aviso NUEVO sobre `deepmerge-ts` ←
  `html-to-text` ← `mailparser` (dependencia directa del Buzón). El arreglo que ofrece npm es una
  bajada rompedora de `mailparser`, así que se deja anotado para decisión del owner, no se fuerza.
APP_VERSION 4.15 → 4.16.
(commit: 62cb377)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.17 — la tarjeta del Sneak Peek se voltea y abre su ficha técnica
- Petición del owner sobre lo construido en V4.16: que la tarjeta sea de VOLTEO. Delante,
  **foto, nombre, grado y variedad**; al pulsarla, el reverso con el puntaje, el origen
  completo, el proceso, las notas de cata y un botón **«Ver ficha técnica»** que abre el
  archivo del lote.
- **Regla de casa que el owner fijó explícitamente y que ahora está escrita donde se aplica**:
  si el dato que viene de fuera —Notion, una hoja, un PDF de un socio— contradice una
  DEFINICIÓN del repo, manda el repo y lo que se corrige es la fuente. Queda como regla 5 del
  §0 del plan, en la cabecera de `sneakPeekMock.ts` y comprobada por el guardián (el grado sale
  del puntaje, y el puntaje tiene que ser válido en la escala de `grados/definicion.ts`).
- **Las fichas técnicas**: `scripts/build-fichas-mock.mjs` (mismo espíritu que
  `build-og-cards.mjs`) arma un PDF A4 por lote con los MISMOS datos de la tarjeta, sin nada
  comercial, y con un aviso de «documento de referencia» que dice que es de una temporada
  anterior y no es oferta. Salida en `public/docs/fichas-mock/`.
  ⚠️ Van bajo `docs/` A PROPÓSITO: es una de las tres carpetas que el matcher del proxy excluye
  (`images/`, `docs/`, `tools/`). Una carpeta nueva en la raíz habría dado 404 en los 18
  subdominios — el error clásico de esta casa, evitado por lectura y no por suerte.
- **Los lotes vivos todavía no tienen ficha**: no hay columna para ella en `lots` ni en
  `lot_listings`, así que `datasheetUrl` viene vacío y el botón no se dibuja. Anotado como
  to-do de desarrollo 2 en el §9 del plan; el día que exista, el botón se enciende solo.
- **Las fotos**: siete `.webp` de 660×440 (382 KB el conjunto) sacadas de `reference_images` con
  Pillow —`sharp` no carga en este equipo— y elegidas para que ninguna se repita. NO son fotos de
  ese lote (no existen): son fotos de CTC que ilustran el proceso o el paisaje, y la tarjeta
  entera va rotulada como temporada anterior. Sin foto, la tarjeta cae al sello del grado.
- Detalles de la mecánica que importan: la cinta **se para** con una tarjeta abierta (si no, el
  detalle recién abierto se va de la pantalla); la copia del bucle sigue siendo pulsable con el
  ratón pero no recibe foco ni la lee un lector (media cinta inerte sería peor, y contenido
  enfocable dentro de `aria-hidden` es un fallo de accesibilidad); Escape cierra; el enlace de la
  ficha no voltea la tarjeta al pulsarlo; con `prefers-reduced-motion` el giro desaparece y las
  caras simplemente se cambian.
- Guardián ampliado a **112 comprobaciones** (de 56): además de lo anterior, que cada mock declare
  foto y ficha Y QUE AMBAS EXISTAN EN EL DISCO, que la ficha cuelgue de `/docs/`, que el puntaje
  sea válido en la escala, que el grado exista en la definición única, y las cinco de la mecánica
  del volteo (aria-expanded, Escape, pausa, copia sin foco, stopPropagation, noopener).
- ⚠️ **El panel de vista previa no sirve para verificar un volteo 3D**: además de no pintar
  fotogramas, devuelve el valor INICIAL de una transición, así que `getComputedStyle(...).transform`
  dice «identidad» aunque la tarjeta esté volteada — y un `transform` en línea tampoco cambia la
  lectura. Se verificó con Chrome headless conducido por CDP (receta de la memoria), que sí
  compone: el reverso se ve entero y legible. Cuesta tiempo descubrirlo dos veces; queda escrito.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos, todos preexistentes) ·
  `npm run build` exit 0 con las seis superficies aún estáticas · guardián 112/112 ·
  las fichas y las fotos responden 200 en el servidor de desarrollo.
APP_VERSION 4.16 → 4.17.
(commit: dd57675)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.18 — el último residuo del cacao-producto, y la rama `fix/rastro-cacao` cerrada
- **Rescatado de la rama y aplicado**: la tarjeta de «Calculadora rápida de mermas» en el hub del
  productor (`AppDashboard.tsx`) seguía diciendo «Rendimiento de café **y cacao**». Es copy de
  producto, no nota de cata, y era el último resto de aquella iteración temprana que el arreglo del
  `<title>` (`4a7e1e2`, 2026-08-14) no alcanzó — está detrás del login, así que no era SEO.
  La CALCULADORA NO SE TOCA: conserva su modo cacao entero y funcionando, como decidió el owner.
- **Rescatado también como to-do**: de los 12 HTML de `public/tools/` —que son URLs INDEXABLES,
  porque el matcher del proxy excluye `/tools/` y `robots.txt` solo cubre `/bcp /ecp /ocp /lab`—
  **solo DOS llevan `meta description`**. Superficie SEO que nadie gobierna; queda como to-do de
  desarrollo 3 en el §9 del plan y candidata natural del módulo «Manejo de Plataformas», que ya
  administra `platform_surfaces` (ruta, título, descripción, `en_sitemap`).
- **Rama `fix/rastro-cacao` (`6ff5f5b`) BORRADA, local y remota, por decisión del owner.** Era la
  OTRA respuesta al mismo problema —quitar el modo cacao entero de `mermas-rapida.html`— y es la que
  NO se desplegó: `main` lo resolvió el mismo día con una línea de `<title>` y la herramienta intacta,
  después de que el owner parara el borrado («Wow wow no!!!! ten mucho cuidado»). La causa real estaba
  fuera del código (copy de posicionamiento viejo cacheado en el índice), así que amputar la
  herramienta no habría arreglado el síntoma. Queda constancia en el §9 del plan para que nadie
  repita el análisis; el commit se conserva en el reflog local mientras no se recoja la basura.
- **La regla de dominio que salió de aquello sigue siendo la de la casa**: `cacao`/`cocoa`/`chocolate`
  como PRODUCTO ya no existe; como NOTA DE CATA es vocabulario estándar del café («Frutos secos /
  Cacao» es categoría de la rueda SCA) y vive legítimamente en `rueda-catacion.html`,
  `viaje-cafe.html` y `ficha/fichaData.ts`. **Eso no se toca nunca.** Anotado en el archivo mismo.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos preexistentes) · `npm run build` exit 0.
APP_VERSION 4.17 → 4.18.
(commit: 2a599d8)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.19 — la cinta gana flechas, rueda de catación, ficha de 3 páginas y ventana; y entra en CaaS
Cinco peticiones del owner sobre el módulo, todas construidas:
- **Flechas en los extremos**: al pasar el ratón (o al recibir el foco) aceleran la cinta hacia
  ese lado —150 s a 24 s por vuelta—, la izquierda invirtiendo el sentido. Es un ACELERADOR, no
  un carrusel. Al pulsarlas además empujan el scroll, que es lo que las hace útiles con
  `prefers-reduced-motion`, donde no hay animación que acelerar.
- **La rueda de catación en el reverso**: el extracto del Reporte de Catación del lote, con sus
  descriptores encendidos sobre la rueda SCA y el resto atenuado al 10 %.
  ⚠️ La dibuja `scripts/build-ruedas-mock.mjs` CONDUCIENDO la herramienta de la casa
  (`public/tools/rueda-catacion.html`), no una rueda paralela: una segunda rueda se separaría de
  la primera al primer cambio de taxonomía — el mismo error que costó meter los grados en un solo
  archivo. Dos variantes por lote: con rótulos para la ficha, sin una letra para la tarjeta (a
  222 px un rótulo de 11 px es ruido).
  Nota para quien vuelva: las funciones internas de la herramienta (`toggleNeedle`,
  `buildSearchIndex`) NO son globales, así que desde CDP no se ven; se trabaja sobre el SVG ya
  dibujado, donde cada gajo es un `.wedge` con `data-level|fam|sub|leaf`.
- **La ficha técnica pasa a TRES páginas y va «red taped»**: resumen · Ficha de café verde ·
  Rueda de catación, las tres con cinta roja diagonal («Muestra · no es oferta comercial») y
  marca de agua MUESTRA. Lo que no se sabe dice «— sin dato» y NO se inventa: una humedad
  inventada en un documento con pinta de oficial es justo el número que alguien acaba citando.
- **«Ver el catálogo completo» abre una ventana** en vez de navegar sin más: explica que la cinta
  es un vistazo, que el catálogo entero vive dentro de Cherry Picked y que **registrarse es
  gratis**. Dos acciones —crear cuenta o entrar—; en las superficies CP la segunda abre el login
  sin navegar. El enlace conserva `href` real para que se pueda abrir en otra pestaña y para que
  un buscador lo siga.
- **La landing de CaaS monta el módulo**, entre «Las dos clases de café» y «Dónde encaja». El
  orden es el argumento: primero qué son Specialty y Black, después lotes de verdad, y solo
  entonces los modelos de oferta. Séptima superficie del módulo.
- La tarjeta crece a 462 px de alto para que quepa la rueda sin apretar el resto.
- Guardián de 112 a **140** comprobaciones: rueda declarada Y EXISTENTE en disco para cada mock,
  que la rueda salga de la herramienta de la casa, flechas con rótulo accesible que aceleran con
  ratón Y foco y sueltan al salir, la izquierda invirtiendo, la ventana en tres idiomas diciendo
  que es gratis, y que CaaS la monte ENTRE las dos secciones (medido sobre los usos del JSX, no
  sobre la primera aparición del nombre — la declaración del tipo aparece antes y engañaba).
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos preexistentes) · `npm run build`
  exit 0 · guardián 140/140 · verificado en Chrome headless (el volteo, la rueda) y en el DOM
  vivo (flechas 150s→24s y `reverse`, ventana con sus tres botones, `/caas` con una sola banda
  entre las dos secciones).
APP_VERSION 4.18 → 4.19.
(commit: 60bc834)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.20 — la cinta cambia de motor, y la ficha gana su Análisis Intrínseco
Cinco correcciones del owner sobre lo de V4.19:
- **El sentido por defecto es hacia la DERECHA.**
- **Y la cinta ya no es una animación CSS.** Era la causa del segundo defecto que reportó: al
  pasar el ratón por una flecha, la cinta «volvía a empezar». Con `@keyframes` no se puede
  cambiar duración ni sentido sin que el navegador REINICIE la animación. Ahora la posición la
  lleva un bucle de `requestAnimationFrame` sobre `translate3d`, con la velocidad PERSIGUIENDO a
  su objetivo (17 px/s en reposo, 155 con flecha, negativa hacia la izquierda): acelerar y soltar
  son continuos y el bucle no tiene costura.
- **Al pulsar una tarjeta, la cinta la CENTRA antes de voltearla y la tarjeta crece un 15 %.** El
  motor acepta un destino en vez de una velocidad, elige la copia más cercana (la tira está
  duplicada) y solo entonces voltea.
  ⚠️ Trampa que costó un ciclo: el envoltorio del bucle (mantener la posición en [-w, 0]) PELEABA
  con el centrado — envolvía mientras se perseguía el destino, la tarjeta no llegaba nunca y por
  tanto no se volteaba. El envoltorio ahora se salta mientras hay destino.
- **Página 2 de la ficha: «Análisis Intrínseco»**, la telaraña de los diez atributos del
  formulario SCA, con los atributos físicos compactados en dos columnas encima.
- **Los datos que faltaban se rellenaron POR ENCARGO del owner** («make it up for the sake of
  completeness»): humedad, actividad de agua, densidad, factor, malla, defectos y los diez
  atributos, en `scripts/lib/analisis-intrinseco.mjs`. La cabecera de ese archivo dice que son
  INVENTADOS y por qué se puede: los diez suman EXACTAMENTE el puntaje real del lote —lo único
  que un catador comprueba de un vistazo—, uniformidad/taza limpia/dulzor van a 10 como en una
  taza sin defectos, y el resto se reparte según el carácter del lote. No inventado: puntaje
  total, variedad, proceso, finca y notas de cata. El sello MUESTRA es lo que mantiene la
  diferencia legible, y la página lo dice con todas las letras.
- Dos arreglos de dibujo que salieron al verificar: los rótulos de la telaraña se cortaban contra
  el panel (lienzo ahora más ancho que alto) y la cinta roja se salía de la hoja y se leía a
  medias («…A · NO ES OFERTA COMER»); geometría recalculada y texto acortado.
- ⚠️ Y una trampa del lenguaje, anotada donde toca: los comentarios CSS dentro del template
  literal del generador de fichas NO pueden llevar tildes invertidas — una sola cierra la cadena
  y el script deja de parsear.
- Guardián de 140 a **153**: que no vuelva a haber `@keyframes` para la cinta, que la mueva rAF
  con la velocidad persiguiendo, sentido por defecto a la derecha, la izquierda invirtiendo, el
  centrado con su copia más cercana y su volteo AL LLEGAR, que el bucle no envuelva mientras
  centra, el 15 % y el z-index, los diez atributos del formulario y que el archivo de datos
  inventados AVISE de que lo son.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos preexistentes) · `npm run build`
  exit 0 · guardián 153/153 · medido en Chrome real (el panel de vista previa ni siquiera corre
  rAF, así que ahí todo salía a cero): sentido DERECHA, base 17 px/s, rampa sin salto, vuelta a
  17 al soltar, −159 px/s con la flecha izquierda, desviación del centro 0 px y escala 1.15.
APP_VERSION 4.19 → 4.20.
(commit: c82a87d)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] V4.21 — las dos caras de la tarjeta, según las maquetas del owner
- El owner mandó las maquetas de cara y reverso: lo que faltaba de la tanda anterior era la
  TARJETA en sí. Reconstruida, y el reparto no es estético — delante IDENTIFICA, detrás EXPLICA.
- **CARA**: la foto con «Ver detalle +» ENCIMA, arriba a la derecha (antes vivía abajo, robándole
  una línea a los datos) y el rótulo de temporada abajo a la izquierda; el nombre; variedad ·
  altitud; las notas de cata; y un pie con el **sello del grado a 72 px** enfrentado al puntaje
  SCA, la finca y el municipio. El sello VUELVE porque a ese tamaño se lee: la mancha gris
  ilegible era la de 36 px de la primera versión.
- **REVERSO**: nombre y ×, la telaraña del **Análisis Intrínseco** (los diez atributos del
  formulario SCA), el botón «Ver ficha técnica» CENTRADO entre las dos figuras, y el extracto de
  la **rueda de catación** al pie. El puntaje, el origen y las notas se fueron a la cara.
- Los diez atributos VIAJAN ahora en el payload (`SneakPeekLot.intrinseco`) para que la telaraña
  se dibuje en el navegador. Son datos de CATA, no comerciales.
- La telaraña se dibuja **en React** y no como SVG generado (al contrario que la rueda) porque sus
  rótulos son texto y la tarjeta habla tres idiomas: un SVG estático serían 21 archivos que habría
  que rehacer al tocar una palabra. Comparte `ATRIBUTOS_SCA` con la versión del PDF, que es lo que
  hace que las dos figuras sean la misma.
- ⚠️ **LA TRAMPA QUE ESTO DESTAPÓ, y conviene no volver a pisarla**: `RadarIntrinseco` es un
  componente de CLIENTE, e importar de `lib/catalogo/sneakPeek.ts` un VALOR —esa lista— metió
  `server-only`, `supabase/server` y `next/headers` en el paquete del navegador y tumbó la página
  entera con un 500. Importar un TIPO de ahí es gratis (se borra al compilar); importar un VALOR,
  no. La lista vive ahora en `src/lib/catalogo/atributosSca.ts`, sin `server-only`.
  **`tsc --noEmit` NO lo ve** —para TypeScript el import es válido—: lo cazó el servidor de
  desarrollo. El guardián ahora lo vigila.
- Dos ajustes de dibujo: el lienzo de la telaraña es más ancho que alto (con uno cuadrado
  «Residual» y «Taza limpia» se cortaban contra el borde de la tarjeta) y la tarjeta crece a
  318×520 px para que quepa todo sin apretar.
- Guardián de 153 a **177**: la anatomía de las dos caras, que los diez atributos sumen el puntaje
  de su lote, y que la lista de atributos siga viviendo fuera de `server-only`.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos preexistentes) · `npm run build`
  exit 0 · guardián 177/177 · las dos caras verificadas en Chrome real.
APP_VERSION 4.20 → 4.21.
(commit: b8e2673)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-17] Cierre de sesión: el plan rebasado sobre la realidad y listo para ejecutarse en frío
- El paso 0 (la cinta) se planeó con UNA versión y usó SEIS (V4.16→V4.21). El mapa de versiones del
  §7 del plan estaba, por tanto, mal desde el paso (i) en adelante: **rebasado sobre V4.21**. El
  paso (i) es ahora V4.22, la mudanza de rutas 4.23/4.24/4.25, los módulos nuevos 4.26–4.30, HC
  4.31+ y los submódulos de KR 4.32+, con el V5.0 al final como siempre.
  Queda dicho en el propio §7: el mapa es una comodidad, la regla es **una versión por tanda
  desplegada**; si un paso vuelve a comerse varias, se rebasa otra vez.
- El plan abre ahora con un **«▶ EMPIEZA AQUÍ»** (qué está construido, qué toca, qué leer) y gana un
  **§0.1 con el bucle de cada paso** — leer, hacer, subir versión, correr la compuerta, verificar
  como se pueda verificar esa cosa, escribirlo, y PARAR. Está pensado para que la siguiente sesión
  ejecute en frío sin volver a deducir nada.
- El §1 se cierra con un **§1.0 «lo que se construyó»**: inventario de archivos, las siete
  superficies y las dos preguntas que siguen abiertas (D0.9 la variedad de la tarjeta 2, D0.10 la
  finca de la 3), que no bloquean nada.
- El §2 (paso siguiente) deja de ser una tabla de vocabulario y pasa a ser **lista de tareas medida
  hoy**: los tres taglines de `consoles.ts` con su texto exacto, **58 «hub» en `src/`** y **48 en
  `docs/`**, el DICT con su gotcha, y una definición de hecho comprobable. Con un aviso: tras el
  paso (i) los taglines describirán módulos que aún no se han movido — es a propósito.
- Traspaso de la sesión entera en **`docs/SESION_2026-08-17_SNEAK_PEEK.md`**, enlazado desde la
  cabecera del HANDOFF: qué se hizo, las tres trampas que costaron un ciclo cada una, el estado de
  la compuerta y los seis frentes abiertos.
Sin cambio de APP_VERSION (documentación).
(commit: 582269a)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.22 · Las catorce portadas públicas recuperan su codificación
- **Lo que estaba roto, y llevaba tres días en producción.** El commit `10c9016` (2026-08-15, V4.7)
  guardó catorce `src/app/**/page.tsx` con su UTF-8 leído como cp1252 y vuelto a escribir como
  UTF-8. No es un problema de comentarios: lo que se sirvió roto fue el **`<title>`, la meta
  description y el `siteName`** de las **catorce superficies públicas a la vez** — la pestaña del
  navegador, el resultado de Google y la tarjeta de Open Graph de cada enlace compartido.
  Comprobado en producción antes de tocar nada:
      `CTC Control Panel Â· La sala de mÃ¡quinas de la red`   (bytes c3 83 c2 a1)
- **Por qué no lo vio nadie.** Porque no rompe nada. `tsc`, `eslint` y `next build` pasan felices
  con la mojibake dentro: para el compilador «mÃ¡quinas» es una cadena tan válida como «máquinas».
  El daño sale exactamente por donde la compuerta no mira. Es el tipo de fallo que solo aparece si
  se leen los BYTES de lo que sirve producción, y por eso ahora hay un guardián.
- **Cómo se arregló.** Deshaciendo la doble codificación **tramo a tramo** (cp1252 → UTF-8), no
  archivo a archivo: tres de los catorce estaban PARCIALMENTE corruptos —commits posteriores les
  añadieron texto sano— y un pase global sobre el archivo entero los habría roto. Verificación
  contra el original: **89 de las 96 líneas no-ASCII vuelven a ser idénticas byte a byte** a su
  versión en `10c9016^`; las 7 restantes son líneas que ese mismo commit escribió de nuevas y
  quedan en español correcto.
- **Guardián nuevo: `scripts/qa-encoding-check.mjs`.** No busca caracteres «raros» —eso daría
  falsos positivos en todo el español del repo—: busca tramos que se **dejan deshacer** y cuyo
  resultado cae en el repertorio que este repo realmente escribe. Sobre el árbol corrupto canta
  **233**; sobre el arreglado, **0** en 667 archivos. El repertorio hubo que añadirlo por un falso
  positivo real: «AQUÍ»» en `docs/HANDOFF.md`, donde la Í pegada a la » da bytes UTF-8 válidos (la
  letra griega ͻ). Corre sin `ts-resolve`: `node scripts/qa-encoding-check.mjs`.
- **Alcance medido, no supuesto**: la corrupción estaba confinada a esos 14 archivos. Los 38
  snapshots `Documentacion_Interactiva_*.html` y el resto de `docs/` y `public/` se escanearon con
  el mismo criterio y dan **0** — incluido `V35.0(10c9016)`, el snapshot del propio commit culpable.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos, la línea base) · `npm run build`
  exit 0 · qa-nav 18/18 · qa-grados 44/44 · qa-sneak-peek 177/177 · qa-docs 14/14 · qa-encoding
  667/0. Verificado en el HTML **construido**, no solo en el fuente: el `<title>` de
  `/control-panel` ya sale `c2 b7` / `c3 a1`.
APP_VERSION 4.21 → 4.22.
(commit: bde465d)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.23 · Paso (i) del plan V5: se congela el vocabulario
- **Qué cambió**: palabras, no comportamiento. Ni una ruta movida, ni un archivo renombrado.
  **39 reescrituras en `src/` y 30 en `docs/`**. Los tres `tagline` de `src/lib/panel/consoles.ts` pasan a
  las frases del owner (BCP «El negocio: dirección, configuración y red de socios» · OCP «La operación: del
  productor al catálogo» · ECP «La ejecución: plataformas, contacto y caja de herramientas») y la cabecera
  del archivo al reparto **Business / Operation / Execution**. La sigla del ECP conserva *Executive* (F1).
- ⚠️ **Los taglines describen la casa del final del paso (ii), no la de hoy.** Quien lea un tagline y luego
  el `nav` de esa misma consola verá que no cuadran. Está avisado dentro del archivo para que el siguiente
  lector no los "corrija" de vuelta.
- **La palabra «hub» queda reservada para CommaaS.** Medirla destapó dos cosas que el plan no sabía:
  el conteo «58 en `src/` / 48 en `docs/`» estaba inflado (contaba el «hubo» de *haber*, el «github» y, sobre
  todo, IDENTIFICADORES), y la palabra no tenía dos sentidos sino **cinco**: la **portada de Cherry Picked**,
  el **selector de consolas** (`/panel`), el **panel del productor** de Kaffetal Regal (su rejilla pasa a ser
  «la rejilla»), la **casa matriz** (CTC Home como centro de la red, término que `src/app/page.tsx` ya usaba)
  y los **espacios de Notion**. El sexto, CommaaS, es el que se queda.
- **Lo que NO se tocó, y por qué** (decisión **D2.2**, nueva en el §8 del plan): los IDENTIFICADORES
  (`styles.hubTile`, `HUB_ICON`, `kind="hub"`, la clave i18n `hub`, `hub.module.css`, `backToHub`) siguen la
  lógica de D2.1 —renombrarlos es refactor, mueve archivos y chocaría con el paso (ii)— y esperan al paso (v);
  y los `Log_Documentacion_Interactiva_V*.txt` **sellados** no se reescriben, porque son el registro histórico.
  La razón de lo primero queda escrita DENTRO de `HubLanding.tsx` y `AppDashboard.module.css`, que es donde la
  buscará el próximo barrido — no solo en esta bitácora.
- **Para el DICT (pendiente del Version Wrap V37, que el §7 sitúa tras el paso (ii)).** No se edita el HTML a
  mano. Términos a añadir/ajustar, con su definición congelada:
  · **BCP · Base Control Panel** — «Business». El negocio: dirección, configuración y red de socios.
  · **OCP · Operational Control Panel** — «Operation». La operación: del productor al catálogo.
  · **ECP · Executive Control Panel** — «Execution». La ejecución: plataformas, contacto y toolbox interna.
  · **selector de consolas** — `/panel`, la landing neutra tras el login maestro (antes «hub»).
  · **portada de Cherry Picked** — `/cherry-picked` (antes «hub»); `HubLanding.tsx` conserva su nombre.
  · **hub** — reservado a **CommaaS** y a nada más.
  · **CaaS · Coffee as a Service** vs **CommaaS** vs **CommaaS-OG** — el outlet de CTC · el hub personal del
    owner · la app original como tenant.
  · **CTC Selection** (paraguas: todo lote comprado en firme) vs **Black Stock** (su rama de grado Black).
  · **Catálogo Cherry Picked (Contratos Vigentes)** — los lotes pre-vendidos que alimentan la tienda Green.
  · **Active Catalogue Sneak Peek** — la cinta pública del paso 0.
  · **Value Ecosystem** — las seis plataformas del ECP como 4.ª unidad de Definición de contexto (F7).
  ⚠️ Al aplicarlos, leer antes `reference_dict_wrap_gotcha`: el DICT mezcla `clave:{` y `clave: {`, y un grep
  ingenuo "demuestra" que una entrada falta y acaba metiendo un duplicado silencioso (JS se queda con el último).
- **Aviso para el paso (ii)**: los avisos «planned» del mapa de arquitectura (`direccionamiento`, el ANN del
  PDF) siguen sin apuntar a `docs/V5_CONSOLAS_PLAN.md`. Es edición del HTML, así que va también al Wrap V37.
- De paso: el §2.2 del plan arrastraba caracteres de control (`\x08`) pegados a la palabra «hub» desde que se
  escribió el documento. Fuera.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos, la línea base) · `npm run build` exit 0 ·
  qa-nav 18/18 · qa-grados 44/44 · qa-sneak-peek 177/177 · qa-docs 14/14 · qa-encoding 668/0.
  `qa-direccionamiento` sigue en 13 ok / 2 fail, **idénticos a los de antes de esta tanda** (comprobado
  corriéndolo sobre el árbol limpio) — no los introdujo el paso (i).
APP_VERSION 4.22 → 4.23.
(commit: 38f5799)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.24 · Paso (ii), PR-A: el OCP recibe el pasaporte del lote
- **La mudanza**: doce módulos dejan el BCP y pasan al OCP — Productores, Fincas, Lotes, Nominados, Arena,
  Galardonados, Club, Catálogo, Contratos, Subastas, Black Stock y el CRM de CaaS, que además **se anida**:
  `/bcp/caas` → **`/ocp/crm/caas`**, el primero de los cuatro tableros de Cherry Picked (los otros tres nacen
  en el paso (iii), F5). El tablero de KPIs de `/bcp` viaja con ellos y es ya el panel del OCP; el BCP estrena
  un scaffold de dirección que enlaza, marcados como construidos, los módulos que PR-B le traerá.
- **El rail del BCP se queda solo con «Panel» hasta PR-B.** Previsto por el §3.3 del plan y avisado DENTRO de
  `consoles.ts` y de `bcp/(app)/page.tsx`, para que no se lea como avería ni se rellene con enlaces a `/ocp/*`.
- **Nodo nuevo del mapa: `src/lib/panel/rutasMovidas.ts`**, fuente única de las mudanzas (`RUTAS_MOVIDAS` +
  `destinoDe()`). Los talones leen de ahí, el guardián la comprueba, el HANDOFF la nombra. Regla F2 hecha
  código: si un módulo se vuelve a mover se **reapunta** su entrada, nunca se encadena un talón contra otro —
  y el guardián rechaza que un `de` sea también el `a` de otra entrada.
- **Los talones van FUERA del grupo `(app)`.** Dentro, el layout corre `requireConsoleAccess("bcp")` y un
  marcador viejo daría «no tiene acceso» sobre una URL que ya no existe. Un `[[...resto]]` opcional por módulo
  cubre la ruta y todas sus sub-rutas. **Verificado en servidor de producción real**, no solo en el build:
  `/bcp/fincas` → 308 → `/ocp/fincas` · `/bcp/arena/abc123` → 308 → `/ocp/arena/abc123` ·
  `/bcp/caas` → 308 → `/ocp/crm/caas` · `/bcp/club/campanas/7` → 308 → `/ocp/club/campanas/7`.
- **Guardián nuevo `scripts/qa-rutas-consolas.mjs`** (164 comprobaciones, se queda para PR-B y PR-C): rail →
  página, mudanza → talón + destino, sin colisiones de prefijo, sin cadenas, y **sin un solo literal de ruta
  vieja vivo en `src/`**. Probado inyectando uno: canta y sale con 1.
  ⚠️ Esa última es la que importa: de las ~300 rutas escritas a mano, **108 son `revalidatePath`**. Un enlace
  roto se ve al primer clic; un `revalidatePath` a una ruta muerta **no lanza, no avisa y no rompe el build**,
  y deja al operador mirando datos rancios después de guardar.
- **Radio re-medido antes de empezar** (§3.2 del plan): los números de 2026-08-17 se habían quedado cortos —
  **304 ocurrencias reales** (no 218) y **29 archivos con `revalidatePath`** (no 20). El dato que faltaba era
  el útil: **47 rutas DISTINTAS en 66 archivos**; el resto es repetición.
- **Dos trampas nuevas, ya en el §3.4 bis del plan**: (1) un `git mv` de módulo rompe imports que `tsc` NO ve
  —los `*.module.css` no se type-chequean, así que un `../shared.module.css` inexistente pasa `tsc --noEmit`
  limpio y muere en el build—; (2) `shared.module.css` salió de `bcp/(app)/` a **`src/components/panel/`**
  (62 imports reapuntados): lo importan las tres consolas y `src/components/`, y dejarlo en una consola que se
  vaciaba era una señal falsa de propiedad. `adminLockActions.ts` sí se queda en el BCP — PR-B le trae Usuarios
  al lado.
- **Para el DICT y el mapa (Version Wrap V37, tras PR-C)**: cambia el FILETREE entero de `bcp/` y `ocp/`;
  `EstructuraModal.tsx` sigue dibujando «BCP · Base — identidad y pasaporte del lote» y «OCP · Operación —
  despacho, relevos, recepción», que ya no es cierto; y hace falta una entrada de DICT para **`rutasMovidas`**
  (la fuente única de las mudanzas) y otra para **«talón»/stub 308**.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos, la línea base) · `npm run build` exit 0 ·
  qa-rutas-consolas 164/164 · qa-nav 18/18 · qa-grados 44/44 · qa-sneak-peek 177/177 · qa-docs 14/14 ·
  qa-boards 27/27 · qa-guard OK · qa-encoding 667/0.
APP_VERSION 4.23 → 4.24.
(commit: 486d160)
