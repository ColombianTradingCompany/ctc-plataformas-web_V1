Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V37.0(a3cfe82).html
(CTC Web Platform, desde 2026-08-18). Cada entrada se compila al próximo Version Wrap (V38).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio: registrar la entrada AL TERMINAR el trabajo, no «después». Y sellar
el sha en cuanto exista el commit — de UNO en UNO, no en bloque al final (en V33 se
sellaron tres asientos con el sha equivocado por hacerlo en bloque, y en V35 tres se
quedaron en «pending»; los dos fallos se corrigieron a mano el 2026-08-17).

════════════════════════════════════════════════════════════════════════════════
CÓMO SE VALIDA UN WRAP — la lista que de verdad hay que correr
════════════════════════════════════════════════════════════════════════════════

✅ LA BATERÍA YA ES UN ARCHIVO (lección de V36): `docs/architecture/validate_snapshot.mjs`.
Se corre en NODE, no en la consola del navegador (el panel de vista previa de esta
máquina no navega a un archivo suelto). Lee el HTML, extrae los literales
`DICT`/`WIRES`/`SCENARIOS`/`CTX`/`ANN`/`FILETREE` casando corchetes y los evalúa
con `new Function`. Uso, desde `ctc-platform/`:

    node docs/architecture/validate_snapshot.mjs docs/architecture/<nuevo>.html --prev docs/architecture/<anterior>.html [--dump]

`--prev` imprime los contadores del snapshot anterior al lado de los del nuevo, para
comprobar que suben EXACTAMENTE lo esperado; `--dump` lista nodos, claves de DICT,
ids de escenarios y CTX. Correrlo también contra el snapshot ANTERIOR como control:
si pasa en los dos, no es que sea permisivo.

Las NUEVE comprobaciones, y las nueve tienen que volver VACÍAS:
  1. pasos de traza (`q.n` y `q.from`) contra los `.node[id]` reales
  2. `DICT[k].a[]` contra las claves de DICT
  3. `WIRES` contra los nodos reales
  4. `CTX` — nodo real Y claves reales
  5. `ANN` huérfanas: rutas que ya no existen en el FILETREE recién regenerado
  6. `ANN[...].info` contra las claves de DICT
  7. `.ibtn[data-info]` del marcado contra DICT
  8. `.chip[data-info]` — SOLO sobre el marcado ANTERIOR a `<script>` (después
     están las plantillas que GENERAN chips y siempre saldrían como falso positivo)
  9. claves REPETIDAS en el texto de DICT/CTX (JS se queda con la última en silencio;
     lección de V31 y V35) — la novena, nueva en V36

Y los contadores tienen que subir EXACTAMENTE lo esperado
(V32: 95→99 · V33: 99→102 · V34: 102→104 · V35: 104→107 · V36: 107→113).

Además, después de `gen_filemap_tree.mjs`, un `node --check` del bloque `<script>`
extraído (el validador evalúa los literales, no el resto del motor).

⚠️ EL MAPA NODO→FICHAS SE LLAMA `CTX`, NO `NODEDICT` (lección de V34). **Ningún
chequeo de un wrap debe ir envuelto en un guard de existencia**: si el símbolo no
existe, tiene que romper.

⚠️ EL SELECTOR DE TRAZAS VA POR ÍNDICE (lección de V33): los `option.value` son
"0","1","2"…; la comprobación buena es `select.options.length === SCENARIOS.length`
y que el `id` esté en `SCENARIOS.at(-1)`.

⚠️ EL DICT TIENE DOS ESTILOS DE ESCRITURA conviviendo (lección de V31):
`clave:{k:"…"` y `clave: {` en varias líneas. Nunca contar por regex: el número
bueno es `Object.keys(DICT).length` evaluado de verdad (el validador lo hace).

⚠️ EL MAPA DE ARCHIVOS DELATA LOS BORRADOS (lección de V32): `gen_filemap_tree`
lee el disco, pero ANN es a mano — al borrar o mover un archivo anotado hay que
repuntar su ANN (en V36: `docs/PROXIMA_SESION.md` → `docs/archive/…`).

⚠️ ESTE DOCUMENTO GUARDA HISTORIA, NO SOLO ESTADO (lección de V33): al renombrar
algo se cambia solo el texto en PRESENTE; las notas fechadas conservan el nombre de
entonces. Donde una nota vieja cita una RUTA que ya se movió, se anota entre guiones.

Y la de V28: una entrada del log puede estar YA compilada si se registró y se
envolvió el mismo día. Antes de compilar, comprobar en el HTML base si ya está.

⚠️ EL SHA DEL NOMBRE ES EL DEL CONTENIDO (lección de V35): si la tanda no está
comiteada, envuelve igual con HEAD y renombra después; tras renombrar, VOLVER a
correr `gen_filemap_tree.mjs` (el snapshot se lista a sí mismo) y el validador.
V36 salió con `48bac75` = HEAD al envolver; el commit del propio wrap + el barrido
de docs de ese día quedan como los primeros asientos de este log.

════════════════════════════════════════════════════════════════════════════════

Contexto abierto al cerrar V36 (2026-08-17):

- **Lo que V36 compiló**: el módulo OCP · Transcripciones entero (V4.8→V4.15: archivo,
  cola de audio, nube AssemblyAI probada, latido/tirón, .bat, instalador, herramienta en
  el repo) como nodo `n-transcripciones` + nodo `n-transcriptor` (la herramienta local,
  en la zona de Clientes: es lo único del sistema que corre en un PC), escenario
  `notavoz` con la bifurcación equipo|nube, seis fichas nuevas (`transcripciones`,
  `transcriptor`, `modelotiron`, `transcribirnube`, `repopublico`, `infrapro`), el atajo
  del rail en `direccionamiento`/`manejoplataformas`, BCP = **Base** Control Panel,
  CaaS ≠ CommaaS en la ficha `caas`, Vercel a 19 subdominios + Pro, la máquina local
  con sus guardianes y `navActivo.ts`, y la batería como archivo.
- **La reorganización de consolas del board del owner (2026-08-17) NO está en el mapa
  como estado — está como aviso** en `direccionamiento` y en la ANN del PDF: es
  PLANEADA. Cuando se construya, cambian `PanelDiagram` (EstructuraModal.tsx), la ficha
  `panels`, la de `bcp`/`consoles`, y este mapa entero de módulos por consola. Plan y
  preguntas abiertas: `docs/ESTADO_Y_PREGUNTAS_2026-08-17.md` (§2.2, §5).
- **Pendientes del owner que este log arrastra**: Search Console (www + sitemap nuevo,
  urgente desde el canonical), política GDPR (subprocesadores: Resend, AssemblyAI,
  Anthropic, Google), número Nequi de la Arena, material de Varietales, la puerta gris
  de Terratalento, los dos vídeos con id provisional `Yird1_j6yqo`, Gemini sin tarifa
  (📌 2026-08-31 caduca el precio de lanzamiento de Sonnet 5), Canva en Make, Coffeed
  con 5 medios sin feed, y activar la protección de contraseñas filtradas (ya no
  bloqueada por plan).
- **Deuda consciente que sigue viva**: la cola diferida de la auditoría (83 índices FK,
  poda del Buzón, carreras de `useAutosave`, tableros sin tope); `/api/kaffetal-regal/
  next-step` sin llamador POR DECISIÓN; robusta de Londres e inventarios ICE sin fuente
  gratuita; `qa-guard-check.mjs` sin cuenta QA; Identity Value Creation declarada y sin
  construir; socio multi-nodo pendiente del PK de `partner_accounts`; los cotizadores
  sin conducir por 2FA; la credencial estrecha del worker (RPC con token propio) en el
  backlog hasta que alguien que no sea el owner corra uno; los `<title>`/meta de las 12
  herramientas de `public/tools/` dentro de cada archivo; el riesgo aceptado del
  registro de herramientas (`allow-same-origin`); el loop de iconos ~2,6 MB + vídeo del
  hub 826 KB; el pilar del lead sigue `cocreate` (marca CaaS) y el hub sin captación de
  correo, los dos a propósito.
- **Infra**: $45/mes (Supabase Pro CTC $25 + Vercel Pro $20; el hub de CommaaS sigue en
  Free). Repo PÚBLICO por decisión hasta que se arregle el acceso de la GitHub App de
  Vercel. Vercel y Supabase bajo ctcexportmain@gmail.com.
- **Siguiente en el orden acordado**: (2) la mudanza del workspace fuera de OneDrive
  (`_WORKSPACE-MOVE-PLAN.md`, sesión fresca) → (3) la reorganización de consolas con
  plan propio → hub CommaaS en sus propias sesiones.

════════════════════════════════════════════════════════════════════════════════


════════════════════════════════════════════════════════════════════════════════
LO QUE EL WRAP V37 ENSEÑÓ (léase antes del V38)
════════════════════════════════════════════════════════════════════════════════

· LA BATERÍA CAZÓ UNA CLAVE DUPLICADA DE DICT, otra vez. `vocabulario` ya existía
  (los dominios de integración, en estilo compacto) y el wrap añadió otra con el
  mismo nombre para el vocabulario congelado del paso (i). JS se queda con la
  ÚLTIMA, así que la ficha vieja habría desaparecido sin un solo error. Se renombró
  a `vocabcongelado`. Es EXACTAMENTE la trampa de la nota `reference_dict_wrap_gotcha`,
  y la comprobación 9 existe por ella: no basta con que el DICT cargue.

· UNA MUDANZA DE CARPETAS DEJA ANOTACIONES HUÉRFANAS, y son 30 de golpe. `ANN` se
  indexa por RUTA, así que mover un módulo de consola desconecta todas sus notas del
  mapa de archivos sin romper nada visible. La comprobación 5 las encuentra; hay que
  correrla DESPUÉS de regenerar el FILETREE, no antes.

· EL FILETREE CAMBIÓ DE RAÍZ. El workspace salió de OneDrive a `C:\dev\ctc-platforms`
  el 2026-08-17, así que el generador ahora escribe `ctc-platforms` como carpeta raíz.
  Las claves de `ANN` empiezan por `ctc-platform/` (el repo, dentro), así que siguen
  casando — pero si alguna vez se anota algo FUERA del repo, ojo con el nombre.

· LA VALIDACIÓN EN NAVEGADOR SIGUE VALIENDO PARA UNA COSA que el archivo no ve:
  `Object.keys(DICT).length` en tiempo de ejecución contra el conteo por texto. Si
  bajan, hay duplicados comiéndose entradas. En V37: 121 = 121. Servir la carpeta con
  `npx serve . -l 4311` y abrirla; las capturas no funcionan en este panel, pero el
  DOM sí se puede interrogar.


════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.27 · Paso (iii)-1: «CTC Selection», el paraguas de lo comprado en firme
- **Black Stock deja de ser módulo suelto** y pasa a ser la pestaña Black de **«CTC Selection»**
  (`/ocp/ctc-selection`), el paraguas de todo lote que CTC compra EN FIRME para venderlo como productor. La
  segunda pestaña, **«Selección»**, lleva Red · Blue · Gold. Cambio de nodo: el rail del OCP pierde «Black
  Stock» y gana «CTC Selection».
- **Una columna, no una segunda tabla** (F4): `black_negotiations.grade` (`lot_grade`, not null default
  `'black'`, índice `(grade, status)`), más un CHECK que **rechaza `tyrian`** — va a subasta, no se compra en
  firme, y eso vive en la BASE y no en la interfaz. La tabla estaba vacía: el default no reescribió nada.
- **Primer uso real de la regla F2 de reapuntado**: el talón de `/bcp/black-stock` se cambió para apuntar
  directo a `/ocp/ctc-selection`, en un solo salto, en vez de encadenarse contra `/ocp/black-stock`.
  Comprobado en servidor real. Es exactamente el caso que `rutasMovidas.ts` anticipaba en su cabecera.
- ⚠️ **PARA EL DICT / LA FICHA DEL NODO — la mitad que NO se construyó, y por qué.** La rama «Selección» no
  publica: `public_lot_catalog` entra por `JOIN fincas` sobre `lots.finca_id`, así que el catálogo público
  enseña el nombre de la **finca real de origen**. El plan daba por hecho que bastaba crear una identidad de
  productor de CTC «+ fincas para que la vista una», pero aplicarlo obligaría a repuntar `lots.finca_id` a una
  finca ficticia — y eso **borra el origen del lote, su pasaporte y su rastro EUDR**, que son el activo del
  negocio. Es la decisión **D3.1**, devuelta al owner con una recomendación (separar la cara COMERCIAL del
  origen: un indicador en `lot_listings` + un nombre a mostrar calculado en la vista, dejando el pasaporte
  intacto). Cuando se resuelva, esto merece entrada propia de DICT: es la primera vez que el proyecto tiene
  que distinguir **quién cultivó** de **quién vende**.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos, la línea base) · `npm run build` exit 0 ·
  qa-rutas-consolas **253/253** (30 rutas mudadas) · qa-nav 16/16 · qa-encoding limpio.
APP_VERSION 4.26 → 4.27.
(commit: cdc7b22)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.28 · D3.1 resuelta: el lote comprado en firme tiene DOS caras
- **La decisión del owner**, literal: «All the lots that CTC buys will be first registered in KR, which means
  that the real farm is shown in the documentation but is replaced as the Finca in the showcase cards (Not
  changing the official finca, just how it looks in the UI)». Con eso, la rama «Selección» de CTC Selection ya
  publica y el módulo del paso (iii)-1 queda COMPLETO.
- **Cambio de arquitectura, y merece entrada propia de DICT** («la vitrina y el registro», o similar): es la
  primera vez que la plataforma distingue **quién CULTIVÓ** de **quién VENDE**. El REGISTRO —pasaporte, ficha,
  rastro EUDR— conserva la finca real; la VITRINA —tarjetas del catálogo y cinta del Sneak Peek— enseña a CTC.
- **Cómo**: `public_lot_catalog` deja de devolver `finca_name` cuando el lote está comprado y expone
  `ctc_selection`. Tres decisiones que conviene que la ficha explique:
  · **Se anula en la VISTA, no en el componente** — la vista la lee `anon`, así que taparlo en la interfaz
    habría dejado el nombre de la finca a un `curl` de distancia.
  · **El rótulo lo pone la app** desde `CTC_RAZON` (`lib/legal.ts`), no el SQL: una segunda definición de la
    razón social sería una contradicción esperando su turno.
  · **Se DERIVA de la compra** (`black_negotiations.status = 'comprar'`), sin interruptor manual que olvidar.
- **Ni una fila de `lots`/`fincas` cambia.** El default original del plan (crear una identidad de productor de
  CTC y repuntar `finca_id`) se descartó porque borraba el origen del lote.
- Guardián `qa-sneak-peek-check` 177 → **182**: comprueba que las dos superficies pidan `ctc_selection` y que
  el rótulo salga de `legal.ts` y no esté escrito a mano en ningún sitio.
- Verificado en una transacción con ROLLBACK sobre datos reales: antes de comprar, `finca_name = "Palmas"` y
  `ctc_selection = false`; después, `finca_name = null` y `ctc_selection = true`. Nada quedó escrito.
- ⚠️ **Para el copy público (anotado, no urgente)**: el Manifiesto promete «finca, personas, proceso y
  evaluación, verificables lote a lote» y la Historia, no perder «el nombre de quien los cultivó». Para un lote
  de CTC Selection eso sigue siendo cierto en la FICHA pero no en la TARJETA. Si el próximo wrap toca los nodos
  de Cherry Picked, conviene decidir si el texto necesita un matiz.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos) · `npm run build` exit 0 · qa-sneak-peek 182/182 ·
  qa-rutas-consolas 253/253 · qa-nav 16/16 · qa-docs 14/14 · qa-encoding 700/0.
APP_VERSION 4.27 → 4.28.
(commit: 4de00ab)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.29 · Paso (iii)-2: CRM CP Green, el tablero de los compradores
- **Módulo nuevo** `/ocp/crm/green`: los compradores de la tienda Cherry Picked Green en un embudo de tres
  etapas (nuevo · activo · recurrente). Segundo de los cuatro CRM del OCP; Roast y X llegan en iii-3.
  Nodo/DICT: encaja como hermano de `n-club` en «Lógica de negocio», o como nota del nodo de las consolas.
- **La decisión que merece la ficha, y que iii-3 debería copiar: la etapa se DEDUCE y NO se guarda** (D3.2).
  0 pedidos = nuevo, 1 = activo, 2 o más = recurrente. En `buyer_profiles.crm_stage` (nullable) vive **solo el
  anulado manual**, y `null` significa «sigue la regla», no «sin etapa». Persistir la etapa calculada es el
  error clásico: en cuanto entra un pedido la fila conserva la etapa vieja, **nada falla** y el tablero miente
  en silencio. Evaluándola al leer no hay nada que recalcular en ningún camino que toque `orders`.
- **La regla vive en un módulo PURO** (`src/lib/crm/etapaComprador.ts`) por lo mismo que `navActivo.ts`: la
  consola está tras 2FA y no se puede conducir en un navegador, así que la lógica tiene que poder comprobarse
  sin levantarla. Guardián nuevo `qa-crm-green-check.mjs` (**21** comprobaciones), verificado saboteando la
  página a propósito para confirmar que canta en vez de pasar de largo.
- **Matiz de interfaz que vale la pena recordar**: fijar a mano la MISMA etapa que dicta la regla NO se marca
  como excepción. Si contara, el tablero se llenaría de avisos «a mano» que no informan de nada.
- **No reutiliza `LeadsBoard`** y es deliberado: aquél está sobre `leads` y sus pilares — otra tabla y otro
  ciclo de vida. Un lead es alguien que escribió; un comprador ya tiene cuenta y puede tener pedidos.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos) · `npm run build` exit 0 · qa-crm-green 21/21 ·
  qa-rutas-consolas 256/256 · qa-nav 16/16 · qa-sneak-peek 182/182 · qa-boards 27/27 · qa-encoding 700/0.
APP_VERSION 4.28 → 4.29.
(commit: 71bd712)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.30 · Paso (iii)-3: CRM CP Roast y X — los cuatro tableros de Cherry Picked ya existen
- **Módulos nuevos** `/ocp/crm/roast` y `/ocp/crm/x`. Con ellos el grupo «OCP · Cherry Picked» queda completo:
  CaaS · Green · Roast · X. Para el nodo/DICT: los cuatro NO son la misma cosa — CaaS y Green son embudos
  reales (proyectos y compradores), Roast y X son **listas de espera** de programas que abren en 2027.
- **Un componente para los dos** (`InteresBoard`), parametrizado por fuente sobre `newsletter_subscribers`.
- **Solo se persiste lo que no se puede deducir** (la regla que dejó CRM CP Green): aquí, `contacted_at`, y
  nada más. Idioma, antigüedad, «este mes» y recuentos se calculan al leer. Se puede **desmarcar**, porque en
  una jornada de envíos marcar de más es tan fácil como marcar de menos.
- ⚠️ **TRAMPA NUEVA, y merece DICT propio junto a las otras del mismo tipo (las que no fallan): una clase de
  CSS module que no existe sale `undefined`.** `styles.btn` sobre un `shared.module.css` que no define `.btn`
  pinta el botón SIN estilo, y ni `tsc`, ni `eslint`, ni `next build` dicen una palabra. Los botones de las
  consolas usan las clases GLOBALES `btn btn-sm`. Guardián nuevo `qa-crm-interes-check.mjs` (**17**), que
  compara las clases usadas contra las declaradas — y al que hubo que enseñarle a **ignorar los comentarios**,
  porque se delató a sí mismo sobre el comentario que explica la trampa.
- La acción revalida **los dos** tableros: una fila es de una sola fuente, pero saber cuál exige leerla.
  Revalidar de menos deja el otro rancio sin avisar — mismo fallo mudo que persigue `qa-rutas-consolas`.
- ⚠️ **Anotado en el §9 del plan**: `newsletter_subscribers` tiene una TERCERA fuente, `ctc-home` (desde
  2026-08-10), que **no tiene tablero**. Hoy 0 filas, pero es una lista que se recoge y nadie mira. El
  componente ya está parametrizado; falta decidir en qué consola vive (el ECP parece su sitio).
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos) · `npm run build` exit 0 · qa-crm-interes 17/17 ·
  qa-crm-green 21/21 · qa-rutas-consolas 262/262 · qa-nav 16/16 · qa-sneak-peek 182/182 · qa-boards 27/27 ·
  qa-docs 14/14 · qa-encoding 705/0.
APP_VERSION 4.29 → 4.30.
(commit: 9890659)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.31 · Paso (iii)-4: una ficha por nodo partner, y 13 compuertas mal apuntadas
- **Módulo nuevo** `/bcp/socios/<nodo>` (×5). Cambia el nodo `n-socios` del mapa: la Red de Socios deja de ser
  solo un tablero de credenciales y gana una ficha por nodo — estado de cada credencial, último acceso, sus
  tres puertas (landing · acceso · panel) y lo que sella en el pasaporte, tomado de `partners.ts`.
- **Es ficha de ESTADO, no panel de operación**: no escribe nada. Alta, baja y reenvío siguen en el tablero.
  Lo que cada nodo HACE se construirá nodo a nodo en su propia interfaz de socio.
- **KPI propio para las invitaciones fallidas**: un correo de invitación que no sale es el fallo mudo de este
  módulo — la credencial existe, el socio no sabe que existe. Sacarlo del detalle de la fila a la cabecera es
  la diferencia entre descubrirlo y no descubrirlo.
- ⚠️ **HALLAZGO DE LA TANDA, y merece nota en el DICT junto a las otras «no fallan»: 13 compuertas apuntaban a
  la consola equivocada.** Siete módulos que PR-B trajo al BCP seguían llamando `requireConsoleAccess("ecp")`,
  dos redirigían a `/ecp`, y uno del OCP pedía `"bcp"`. Lo dejó la mudanza del paso (ii): **las claves de
  consola no llevan barras**, así que la reescritura masiva de rutas no las tocó. Mismo agujero que
  `PILLAR_CONSOLE` — tercera vez que esta familia aparece en la reorganización.
  **No habría fallado nunca para el owner** (tiene las tres consolas); habría esperado a un colaborador con
  una sola credencial. Cerrado con la comprobación **(f)** de `qa-rutas-consolas.mjs` (263), verificada
  saboteando un archivo.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (27 avisos) · `npm run build` exit 0 ·
  qa-rutas-consolas 263/263 · qa-nav 16/16 · qa-crm-green 21/21 · qa-crm-interes 17/17 ·
  qa-sneak-peek 182/182 · qa-boards 27/27 · qa-docs 14/14 · qa-encoding 711/0.
APP_VERSION 4.30 → 4.31.
(commit: 403f550)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.32 · Paso (iii)-5: Definición de contexto deja de ser una herramienta de guion — PASO (iii) COMPLETO
- **Cambio de nodo grande**: `n-direccionamiento` ya no describe un módulo vendorizado de guion de vídeo. La
  pantalla es ahora **las tres preguntas (Producto · Cliente · Contexto) por cada una de CUATRO unidades**
  —CTCX · KR · CHP · **Value Ecosystem**, la nueva (F7)— más tres pestañas placeholder: Misión y Visión,
  Modelo Económico y Mercado Global. Con esto el paso (iii) del plan V5 queda completo (V4.27 → V4.32).
- ⚠️ **PARA EL DICT — el módulo DEJÓ DE SER VENDORIZADO, y su ficha lo dice al revés hoy.** La entrada
  `direccionamiento` explica que el componente llega verbatim del autor y no se edita. Ya no: el rework retiró
  justo lo que lo hacía suyo (formatos de vídeo, derivables, moodboard) y el archivo ya traía dentro las
  unidades, colores y dominios de CTC — la resincronización era teórica. Hay que reescribir ese párrafo.
  La herramienta de guion sigue existiendo FUERA de la plataforma.
- **La decisión que el plan no había visto**: D3.3 preguntaba por el moodboard, que estaba VACÍO (28 bytes). El
  problema real era que las respuestas de Producto y Contexto vivían dentro de la rama «Video largo» — con la
  promesa de marca, el CTA y el objetivo ahí dentro. Borrar por prefijo se habría llevado 3.000 caracteres.
  Owner: «Strip it, I'll rescue the text». Respaldo de los 20 campos en `docs/archive/`, migración que LEVANTA
  las respuestas, y retirada solo de los cinco campos de planos de cámara. Quedan 15 campos / 4.202 caracteres.
- **Guardián nuevo `qa-definicion-check.mjs` (33)**: lista las 15 claves ya escritas y exige que sigan teniendo
  casilla. Renombrar un `id` deja su texto huérfano **sin que nada falle** — la clave deja de casar y el campo
  sale vacío.
- **`serverActions.bodySizeLimit` volvió al defecto** de Next (era 8 MB por los data-URI del moodboard).
- **Dos efectos laterales que conviene registrar**: la línea base de `eslint` bajó de **27 avisos a 8** (19 eran
  del `.jsx` retirado), y **`qa-direccionamiento-check` volvió a 14 ok / 0 fail** — llevaba en 13/2 desde antes
  del 2026-08-18 porque su sección 2 comprobaba que el `.jsx` siguiera trayendo un párrafo que ya no traía.
  Vigilaba una premisa muerta y nadie lo miró: **un guardián que falla y se ignora enseña a ignorar los fallos.**
- Compuerta: `tsc` limpio · `eslint src` 0 errores (**8** avisos, nueva línea base) · `npm run build` exit 0 ·
  qa-definicion 33/33 · qa-direccionamiento 14/14 · qa-rutas-consolas 263/263 · qa-nav 16/16 ·
  qa-crm-green 21/21 · qa-crm-interes 17/17 · qa-sneak-peek 182/182 · qa-docs 14/14 · qa-encoding 712/0.
APP_VERSION 4.31 → 4.32.
(commit: 9028ed7)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.33 · Paso (iv-a): Herramientas del Café gana su modelo de acceso
- **Primera de las dos tandas del paso (iv)**: quién puede abrir qué. La concha in-app con vuelta segura (A5)
  es la segunda, V4.34.
- **`herramientas` es un OBJETIVO de la matriz de identidad, no una identidad nueva.** Se entra con la cuenta
  que ya se tiene: productor de KR **o** comprador de CP. Crear una tercera identidad habría roto la exclusión
  productor ⊕ comprador sobre la que se sostiene `identidad/matriz.ts` entera. Merece línea en el DICT de la
  matriz: es la primera entrada de esa lista que no es una identidad sino una PUERTA.
- **Permisos por PERSONA y por HERRAMIENTA** (`tool_user_grants`). Antes `tools_plus_grants` concedía por
  AUDIENCIA y abría todas las Plus de golpe — lo que vaciaba de sentido el nivel Plus, cuya gracia es que una
  herramienta esté visible pero bloqueada. `source` (manual|payment) sobre la misma tabla, para que el
  disparador de pago no obligue a migrar nada.
- ⚠️ **`tools_plus_grants` NO se retiró**: 3 filas vivas, se lee como **comodín heredado**. El veredicto de
  acceso dice POR QUÉ se abrió (`via: "permiso"` vs `"comodin-heredado"`) — sin ese dato no hay forma de saber
  a quién migrar antes de retirar la tabla. `quienDependeDelComodin()` es esa lista.
- ⚠️ **Trampa anotada, de la familia «no falla»**: la caducidad se filtra en código y no con un
  `.lt("expires_at", …)`. `expires_at` nulo = no caduca, y un filtro por fecha en SQL se comería esas filas
  salvo que se escriba el `or(...is.null)`. Quitaría permisos **en silencio**.
- Guardián nuevo `qa-herramientas-acceso-check.mjs` (**26**), verificado saboteando la regla para confirmar que
  una Plus sin permiso deja de abrirse. Los tres rechazos conservan texto propio: entrar, registrarse y
  solicitar son tres salidas distintas.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-herramientas-acceso 26/26 · qa-terminos 19/19 · qa-definicion 33/33 · qa-rutas-consolas 263/263 ·
  qa-nav 16/16 · qa-direccionamiento 14/14 · qa-docs 14/14 · qa-encoding 719/0.
APP_VERSION 4.32 → 4.33.
(commit: 266fc9f)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-18] V4.34 · Paso (iv-b): la concha in-app y la vuelta segura — PASO (iv) COMPLETO
- **Módulos nuevos**: `/kaffetal-regal/herramientas/<slug>` y `/cherry-picked-green/herramientas/<slug>`. Cada
  herramienta se abre DENTRO de la webapp, en una ruta de su propia superficie. Cambio para el nodo de las
  Herramientas: deja de ser «un panel con iframes» y gana conchas propias por superficie.
- **Por qué la ruta es de la SUPERFICIE y no de una consola**: bajo `kaffetal-regal.ctcexport.com` el proxy
  antepone la base del subdominio, así que la ruta es correcta por construcción; cualquier cosa colgada de
  `/ecp/…` se reescribiría y daría 404. Es la gotcha 12 otra vez. El HTML sigue intacto en `/tools/h/<slug>`.
- ⚠️ **PARA EL DICT — merece entrada propia: «la vuelta segura».** El owner la pidió como USABILIDAD (A5:
  «botones seguros que le permitan volver a lo que estaba haciendo») y resultó ser SEGURIDAD. Obedecer el
  `?volver=` a ciegas es un **redirect abierto**: `…?volver=https://sitio-falso/login` pondría, dentro del
  dominio de CTC, un botón «Volver a Kaffetal Regal» que lleva a una copia del login — phishing servido por la
  casa, sin que falle nada. `vueltaSegura()` es lista blanca estrecha (solo rutas relativas de ESA superficie,
  por frontera de segmento) y **nunca devuelve vacío**. Guardián `qa-concha-herramientas-check.mjs` (**42**),
  once vectores de ataque, verificado en servidor real: destino hostil → `/kaffetal-regal`; destino legítimo →
  `/kaffetal-regal?m=lotes`.
- **«Pedir no es poder»**, y también merece DICT: las solicitudes viven en `tool_access_requests`, **tabla
  aparte** de `tool_user_grants`. Una fila de grants significa «puede abrir» sin más lectura; con un `status`
  compartido, un filtro olvidado convertiría una petición en un permiso **sin fallar**. La tabla vieja
  `tools_plus_grants` sí mezcla las dos cosas — es justo lo que no se replicó.
- **D4.1 con su default**: «Solicitar» avisa a `info@` y **el resultado del envío se guarda en la fila**
  (`aviso_email_at` / `aviso_email_error`). Lección del OTP del BCP: ningún remitente se traga un fallo.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-concha-herramientas 42/42 · qa-herramientas-acceso 26/26 · qa-terminos 19/19 · qa-rutas-consolas 263/263 ·
  qa-nav 16/16 · qa-definicion 33/33 · qa-direccionamiento 14/14 · qa-docs 14/14 · qa-encoding 722/0.
APP_VERSION 4.33 → 4.34.
(commit: c2519d8)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.35 · Paso (v): «Mis solicitudes» en el panel del productor — EL PLAN V5 QUEDA COMPLETO
- **Módulo nuevo en Kaffetal Regal**: «Mis solicitudes» (CTC Tech · Varietales · CaaS), con tarjeta propia
  junto a «Más allá de la exportación», contador de respuestas sin leer e icono de sobre. Cambio del nodo de KR
  en el mapa: el panel del productor pasa de un módulo de conversación a dos.
- ⚠️ **PARA EL DICT — la premisa del plan era falsa, y merece decirse**: el §6 daba por hecho que los leads son
  «capturas anónimas» y que había que emparejarlos con una cuenta al entrar (D5.1). **Ya se vinculan en la
  CAPTURA** (`leads.profile_id` + `account_provisioning`): 13 de 15 lo estaban. Y las respuestas ya llegaban al
  productor, espejadas en `producer_comm_log`. La tanda fue de PRESENTACIÓN, no de fontanería, y D5.1 murió.
- ⚠️ **EL FALLO QUE CASI SALE, y es el mejor ejemplo de la familia muda**: partir el feed por `leadId` a secas
  parecía correcto, pero **solo la nota de CTC lleva el lead** — la RESPUESTA del productor se guarda con
  `parentId` y `leadId` nulo. La conversación habría salido partida en dos pantallas (el mensaje en «Mis
  solicitudes», la respuesta en «Retroalimentación») **sin un solo error**. Eran 2 de 15 notas. Encontrado
  consultando los datos reales antes de dar la tanda por hecha.
- **La partición es por CAMPO y no por el texto de la etiqueta**: una partición basada en copy se rompe cuando
  alguien mejora el texto, y se rompe en silencio.
- **Dos migraciones de `context_label`**, cosméticas pero necesarias porque esa cadena AGRUPA los hilos: se
  recortó «Solicitud CTC Home ·» (falso para quien entra por la landing de un servicio) y se normalizó
  «CTC Co-Create» → «CTC CaaS». La CLAVE `cocreate` no se toca.
- **El formulario de contacto gana su puerta**: creaba la cuenta y terminaba en «Entendido», que solo cerraba.
  Ahora ofrece «Entrar a …» por pilar (CaaS → Cherry Picked Green; resto → Kaffetal Regal), en es/en/de.
- ℹ️ Los COMPRADORES siguen fuera por diseño de A3 (el espejo solo dispara para perfiles productor).
- **EL PLAN V5 QUEDA COMPLETO** (V4.23 → V4.35). Lo siguiente NO es trabajo de plan: el owner declara **V5.0** y
  toca el **Version Wrap V38**, que tendrá que compilar todo el Log V37 — es largo y trae muchos nodos nuevos.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 · qa-solicitudes-kr 23/23 ·
  qa-rutas-consolas 263/263 · qa-concha-herramientas 42/42 · qa-herramientas-acceso 26/26 · qa-nav 16/16 ·
  qa-boards 27/27 · qa-definicion 33/33 · qa-direccionamiento 14/14 · qa-docs 14/14 · qa-encoding 731/0.
APP_VERSION 4.34 → 4.35.
(commit: 642796a)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.36 · `npm audit` vuelve a 0 sin degradar el Buzón
- La cadena era `deepmerge-ts <8.0.0` (GHSA-ggr8-5vv4-36mx) ← `html-to-text` ← **`mailparser`**, dependencia
  directa del Buzón. Llevaba en 3 altas desde el 2026-08-17.
- **NO se aplicó `npm audit fix --force`**: instalaba `mailparser@3.9.8`, bajada rompedora sobre el módulo que
  lee el correo real. En su lugar, `overrides: { "deepmerge-ts": "^8.0.1" }` — `html-to-text` la pide como
  `^7.1.5`, así que sin override no sube; con él, `mailparser` se queda en 3.9.14 y la vulnerable sale.
- **Verificado comparando la SALIDA y no la instalación**: se capturó el resultado completo de `simpleParser`
  sobre dos correos (solo-HTML y `multipart/alternative`, con acentos, lista, enlace y asunto codificado)
  antes y después del cambio → **byte a byte idéntico**. El camino HTML → texto es el que atraviesa
  `html-to-text` → `deepmerge-ts`, que es lo que se tocaba.
- ⚠️ Nota para el DICT/gotchas: `simpleParser` **no** sintetiza `text` desde el HTML en un `multipart/mixed`
  con adjunto (`m.text` sale `undefined`) — es normal, no una regresión. La primera prueba lo dio por fallo.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 · **`npm audit` 0**.
APP_VERSION 4.35 → 4.36.
(commit: 1ff089c)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.37 · Las 12 herramientas dejan de anunciarse solas
- `public/tools/*.html` son **archivos estáticos e indexables**: NO los pinta Next, así que no pasan por
  `generateMetadata` ni por ningún layout. Lo que no esté escrito a mano en su `<head>` no existe. **Diez de
  las doce no tenían `meta description`**, y cuando falta, Google recorta un trozo del cuerpo y lo pone de
  titular — en una calculadora, normalmente una etiqueta de formulario. Nada falla, nada se registra.
- **El texto no se inventó**: sale de `tools.descripcion`, que el owner ya escribió. Recortado a longitud de
  resultado (139–160) y sin las notas de administración («Se ofrece a productores», «Reemplazada por…»), que
  dicen a QUIÉN se le muestra y no QUÉ hace.
- ℹ️ **Diez añadidas, una corregida, una intacta**: `viaje-cafe.html` tenía una descripción corta pero correcta
  del owner y se deja como está. Solo se tocó la rota.
- ⚠️ **Una estaba peor que vacía**: `mermas-ctc.html` sí tenía descripción, y describía la calculadora
  **Rápida** — pero la base dice que ese archivo es la **Detallada**.
- **Guardián nuevo `qa-tools-seo-check.mjs` (193)**, que no se conforma con «existe»: largo útil, sufijo de la
  casa, sin frase de admin, **ninguna repetida** (Google colapsa duplicados) e **idioma acorde al `<html
  lang>`**. Verificado saboteándolo por tres caminos — quitar una, duplicar otra, cambiarle el idioma a una
  tercera; los tres lo hacen fallar.
- ⚠️ La comprobación de idioma existe **porque el fallo pasó en esta misma tanda**:
  `green-coffee-datasheet.html` es `lang="es"` con la interfaz entera en español y se le escribió primero una
  descripción en inglés.
- ⚠️ **CORREGIDO EN V4.38**: aquí se dijo que `tools.meta_description` era DECORATIVA. Nada la sirve —cierto—,
  pero **sí la lee el tablero de «Manejo de Plataformas»** para la píldora «sin descripción». Es el INVENTARIO.
- Solo se tocó el `<head>`: son herramientas vendorizadas con JS vivo, y el cuerpo no se rozó.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 · `npm audit` 0 ·
  qa-tools-seo 193/0 · qa-encoding 732/0 · qa-docs 14/14.
APP_VERSION 4.36 → 4.37.
(commit: e1fbe30)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.38 · El espejo del inventario, y una retirada que seguía publicada
- **CORRECCIÓN DE V4.37.** `tools.meta_description` no es decorativa. Nada la SIRVE —y no puede:
  `/tools/h/[slug]` redirige al archivo estático si la versión es del repo, y para una subida responde con
  `X-Robots-Tag: noindex`— pero **sí la LEE `cargarHerramientasSeo` → `PlataformasBoard`**, que pinta con ella la
  píldora «sin descripción» y el contador de indexables. Es el **INVENTARIO**. Con 11 NULL + un «Test
  descripcion 1», tras V4.37 el tablero mentía al revés.
- **Se llenó la columna** con lo que sirve cada archivo (12 filas) y **se corrigió `tools.lang` de
  `green-datasheet`** (`en` → `es`): el archivo declara `lang="es"` y la interfaz es española, y ese campo sale
  como píldora «ES»/«EN» en la tarjeta.
- **LA REGLA, para el DICT**: en una herramienta del repositorio manda **el ARCHIVO**; la columna es su
  **espejo**. El campo del ECP ahora lo dice en pantalla, con la ruta del fichero al lado.
- **Guardián nuevo `qa-tools-seo-espejo.mjs` (68)**, que toca la base. Verificado saboteando la columna de `qr`
  y el `lang` de `green-datasheet` desde SQL: denunció los dos.
- ⚠️ **NODO NUEVO PARA EL MAPA: ARCHIVAR NO RETIRA DE LA WEB una herramienta del repositorio.** `archivado_at`
  la saca del ECP y hace que `/tools/h/<id>` dé 404, **pero el archivo sigue en `public/`** — ruta que el proxy
  ni mira. `mermas-detallada` («Reporte de proceso de café», reemplazada el 2026-08-15) seguía viva e indexable,
  y en V4.37 se le escribió sin saberlo una descripción nueva. Ahora lleva `noindex, follow`. Borrar el archivo
  es decisión del owner. El guardián exige `noindex` en toda archivada y su AUSENCIA en toda viva.
- ⚠️ **IDS CRUZADOS**: «Calculadora de mermas · Detallada» = id `mermas-ctc`; el id `mermas-detallada` es la
  vieja «Reporte de proceso de café», retirada. Esa confusión dejó a `mermas-ctc.html` describiendo la Rápida.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-tools-seo 193/0 · qa-tools-seo-espejo 68/0 · qa-encoding · qa-docs 14/14.
APP_VERSION 4.37 → 4.38.
(commit: 419aeae)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.39 · La lista de espera de CTC Home gana su tablero
- Cierra el **punto 6 del §9** del plan V5. `newsletter_subscribers` tiene TRES fuentes; el paso (iii)-3 dio
  tablero a `roast` y `x`. La tercera, `ctc-home`, nació el **2026-08-10** —cuando el índice de la red, en la
  portada, dejó de anunciar la puerta del Control Panel— y estuvo **nueve días recogiendo correos sin tablero**.
  No fallaba nada: el formulario guardaba y la base guardaba, sencillamente no había dónde mirarlos. Sigue en 0
  filas, así que no se perdió ninguna dirección — suerte, no diseño.
- **NODO NUEVO: `/ecp/ctc-home` · «Lista de espera · CTC Home»**, en «ECP · Dirección», junto a Leads ·
  Recepción. **En el ECP y NO en el OCP a propósito**: Roast y X son programas de Cherry Picked y por eso
  cuelgan de «OCP · Cherry Picked»; ésta es de la red entera, y ahí el sitio habría contradicho el contenido.
- ⚠️ **MUDANZA: `InteresBoard` + `InteresRow` + `interesActions` salen de `src/app/ocp/(app)/crm/` a
  `src/components/panel/interes/`.** Sirven a DOS consolas; bajo el árbol del OCP, la siguiente mudanza de
  módulos se habría llevado por delante una página del ECP. Misma lección que `shared.module.css` en PR-B.
- ⚠️ `marcarContactado` revalidaba dos rutas y ahora revalida **tres** — y la tercera está en otra consola, así
  que olvidarla dejaría una lista que se llena y no se refresca, en silencio.
- El estado vacío se parametriza (`origen`): «desde la landing del programa» es falso para la lista de la
  portada.
- ⚠️ **PARA EL DICT — la comprobación que faltaba**: `qa-crm-interes-check.mjs` (37) ya no lleva las fuentes a
  mano, las **lee de `SOURCES`**, y exige por cada una página + rail + `revalidatePath`. Una fuente nueva sin
  tablero rompe el guardián el día que se escribe. Verificado con una cuarta fuente falsa y un `revalidatePath`
  borrado: denunció las dos.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-rutas-consolas 266/0 · qa-crm-interes 37/0 · qa-nav 16/16 · qa-boards 27/27 · qa-encoding 734/0.
APP_VERSION 4.38 → 4.39.
(commit: 353b49e)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.40 · D0.9 y D0.10 cerradas, sin cambiar un solo valor
- Las dos discrepancias que el paso 0 dejó marcadas en `sneakPeekMock.ts`. **Las tarjetas ya mostraban lo
  correcto**, así que el diff del mock es SOLO comentarios y no hubo que regenerar ruedas ni fichas PDF.
- **D0.9 · tarjeta #2: es BOURBON** (owner, 2026-08-19). El título dice Bourbon y el campo `Variedad` dice
  Castillo. Costó preguntarlo bien porque un campo estructurado suele ganarle a un título: la ficha hermana
  `Tabi - Honey [La Pradera]` SÍ lleva `Variedad: Tabi` (el campo no está roto por sistema) y `Variedades / %`
  de La Pradera está vacío (la finca no desempata). **Desempata la taza**: 87.00 floral/mandarina/cardamomo
  frente a los dos Castillo de la MISMA finca a 84.25 y 84.50 con chocolate y especias.
- **D0.10 · tarjeta #3: es LA FORTALEZA, y por prueba, no por criterio.** La relación apunta a «La Floresta» —y
  **La Floresta no cultiva Gesha**: su `Variedades / %` dice «Castillo 90%, colombia 10%». El RUT adjunto a La
  Fortaleza es de **Wilmer Rodríguez**, que es el `Supplier Name` del lote, y el datasheet se llama
  `La_Fortaleza_Wilmer_R_Gesha_Ragonvalia…`. **Las dos fincas cuelgan del MISMO proveedor**: así se escoge la
  equivocada en un desplegable.
- ⚠️ **PARA EL DICT / owner: aguas arriba los dos errores SIGUEN.** Notion conserva `Variedad: Castillo` en el
  Bourbon y la relación del Gesha con La Floresta. Hoy da igual porque el mock está a mano — **pero el mock es
  temporal**, y el día de la importación real los dos valores volverían a entrar sin que falle nada.
- **`qa-sneak-peek-check.mjs`: 177 → 189.** Clava variedad, finca, municipio y altura, y exige que el archivo
  siga explicando por qué. Verificado simulando la reimportación (Castillo + La Floresta + 1 300 m): 3 fallos.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-sneak-peek 189/0 · qa-grados · qa-docs 14/14 · qa-encoding.
APP_VERSION 4.39 → 4.40.
(commit: 7dfd8bb)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.41 · El Manifiesto dice DÓNDE se verifica la trazabilidad
- Cierra la consecuencia que dejó abierta **D3.1** (V4.28). El pilar 01 prometía «finca, personas, proceso y
  evaluación, **verificables lote a lote**», a secas.
- **Fallaba en DOS puntos, no en uno.** La **finca**, por D3.1 (el lote comprado se muestra a nombre de CTC); y
  las **personas** — que **no lo trajo D3.1**: la tarjeta no ha mostrado NUNCA al productor, de ningún lote (el
  tipo `Lot` de `data.ts` no tiene campo). La promesa ya sobrepasaba antes de V4.28.
- **Decisión del owner (2026-08-19): decir DÓNDE, sin retirar la promesa**, porque es cierta. En los tres
  idiomas: «en la ficha técnica y en la DDS» · «on the datasheet and the DDS» · «im Datenblatt und in der DDS».
- **No es rebaja**: apunta al mismo sitio que ya nombraba la sección de EUDR (`eudrP2d`: el número de DDS
  «queda visible en tu factura y en la ficha técnica del lote») y al que dijo el owner en D3.1 («se muestra en
  la documentación»).
- **La Historia NO se toca**: «sin perder el nombre de quien los cultivó» sigue siendo verdad — el nombre vive
  en la ficha.
- ⚠️ **PARA EL DICT: la promesa y la vitrina quedan ATADAS.** `qa-sneak-peek-check.mjs` 189 → **194**: falla si
  se le quita el «dónde» al pilar y falla si se devuelve la finca a la tarjeta. Por separado ninguna rompía
  nada — por eso se habían separado sin que nadie se enterara. Verificado saboteando las dos.
- ℹ️ `public_lot_catalog` devuelve **0 filas** hoy: no cambia nada que un visitante esté viendo.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-sneak-peek 194/0 · qa-terminos 19/19 · qa-docs 14/14 · qa-encoding 735/0.
APP_VERSION 4.40 → 4.41.
(commit: 3c72ca5)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.42 · La ficha pública de un lote vivo, sobre lista blanca
- Cierra el **§9 to-do 2** del plan V5 — pero **NO como decía la nota, porque la nota era peligrosa**. Mandaba
  «generar el PDF desde `lots.datasheet` y el botón se enciende para todo el catálogo».
- ⚠️ **Eso publicaba el expediente entero.** `lots.datasheet` son **110 claves**: `nit_rut`, `razon_social`,
  `productor`, `geo_ref`, `qgrader_*` y todo el bloque `eudr_*` de evaluación de RIESGO del proveedor. Y
  `estate` — que por D3.1 la tarjeta tapa, y el PDF habría dejado a un clic de esa misma tarjeta.
- ⚠️ **PARA EL DICT: dos documentos del repo se contradecían.** La auditoría del 2026-07-10 (HANDOFF) ya decía
  que `public_lot_catalog` existe para que la Ficha privada y la geolocalización NO lleguen a un comprador, y
  advertía de no deshacerlo. La nota del §9, posterior, mandaba deshacerlo. **Gana la que explica por qué.**
- **NODO NUEVO: `/docs/ficha/[lotId]`**, la ficha pública de un lote vivo. ⚠️ Cuelga de `/docs` por la **gotcha
  12**: el matcher del proxy excluye `docs/`, y una ruta no excluida se reescribiría en subdominio → 404. El
  enlace se abre desde las SIETE superficies de la cinta.
- **`src/lib/catalogo/fichaPublica.ts`**, módulo puro con **lista BLANCA**: una clave nueva del formulario nace
  PRIVADA. Solo escalares (un anidado arrastra fincas o adjuntos). D3.1 se respeta dentro de la ficha.
- **Migración**: `public_lot_catalog` gana `tiene_ficha` — **booleano, nunca el contenido**. Misma forma que
  `ctc_selection`.
- **La compuerta es la vista**: sin fila publicada, 404, y el `datasheet` ni se toca. **Verificado contra la
  base**: los dos lotes reales con ficha (y NIT dentro) están sin publicar → 404.
- **Guardián `qa-ficha-publica-check.mjs` (105)** contra **las 110 claves REALES**. Cuatro sabotajes: recorrer
  la entrada, tumbar D3.1, meter `nit_rut` en la lista blanca (revienta al cargar), sacar la URL de `/docs`.
- ℹ️ **Sin verificar**: no hay lotes publicados, así que la página no se ha visto renderizada con datos reales.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-ficha-publica 105/0 · qa-sneak-peek 194/0 · qa-rutas-consolas 266/0 · qa-docs 14/14 · qa-encoding 735/0.
APP_VERSION 4.41 → 4.42.
(commit: 84bea49)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.43 · Los grados de Notion cuadran con su propio puntaje
- Higiene de datos en «📋 Fichas Técnicas de Café» (Notion). Releído antes de tocar: los 6 descuadres del
  2026-08-17 seguían idénticos.
- **Seis relaciones `Grado CTC` corregidas**: Borbón Rosado 88.50 Red→Tiryan · Tabi Honey 87.00 Black→Gold ·
  Gesha 86.25 Gold→Blue · Tabi Doble Ferm. 85.00 Gold→Blue · Castillo Doble Ferm. 84.50 Tiryan→Red · Castillo
  Lavado 84.25 Tiryan→Red. Bourbon Honey (87.00 → Gold) ya estaba bien.
- ⚠️ **CORRECCIÓN AL PROPIO PLAN: la escala numérica de Notion nunca estuvo mal.** `Min SCA`/`Max SCA` de los
  cinco grados ya coincidían EXACTAMENTE con `definicion.ts`. Lo roto eran las RELACIONES, no los rangos.
- ⚠️ **Dos fichas que el plan no vio**: Cenicafe 1 y Castillo [La Hacienda] llevaban grado **sin ningún SCA** —
  un grado afirmado sin nada detrás. Por decisión del owner se rellenaron (material de muestra): 81.50 en
  Cenicafe (dentro de Black, y **el mismo número que usa `sneakPeekMock.ts`** en la tarjeta 7) y 83.75 en
  Castillo [La Hacienda] (dentro de Red). **Marcados en `Notas de Perfil` como PUNTAJE DE RELLENO, no de
  laboratorio** — misma regla que las fichas PDF selladas «MUESTRA».
- **Verificado con `gradoPorPuntaje()`**: las 9 fichas con puntaje cuadran. Chiroso y Caturra siguen sin
  puntaje y sin grado, que es coherente.
- ℹ️ **El owner decidió NO tocar**: la grafía «Tiryan» (la plataforma usa *Tyrian*; nada del código lee esa
  cadena) ni su `Definicion` «SCA+89» contra su propio `Min SCA` 88.
- ⚠️ **PARA EL DICT: no hay guardián posible hoy.** No existen credenciales de Notion en el repo, así que un
  espejo ejecutable no se puede escribir. Es el requisito que falta para el «espejo Notion» de
  `INTEGRACIONES_PLAN.md` §1; sin él, esto se vuelve a descuadrar en silencio.
- Sin cambios de comportamiento en la aplicación: el mock ya llevaba 81.50/Black en la tarjeta 7.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 ·
  qa-sneak-peek 194/0 · qa-grados OK · qa-docs 14/14 · qa-encoding.
APP_VERSION 4.42 → 4.43.
(commit: 6398217)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.44 · La escala de grados es DE DOS EN DOS (corrección del owner)
- ⚠️ **NODO CRÍTICO PARA EL DICT.** `definicion.ts` afirmaba desde el 2026-08-05: Black 80–82.99 · Red 83–84.99
  · Blue 85–86.99 · Gold 87–87.99 · Tyrian 88–100. **La escala real es**: **Black 80–82 · Red 82–84 · Blue
  84–86 · Gold 86–88 · Tyrian 88+** → con la convención del archivo, **80–81.99 · 82–83.99 · 84–85.99 ·
  86–87.99 · 88–100**. **El límite es siempre del grado de ARRIBA**: 84.00 es Blue, 88.00 es Tyrian.
- ⚠️ **LA LECCIÓN, y duele**: en V4.43 (horas antes) se anotó que «la escala de Notion nunca estuvo mal» porque
  coincidía EXACTAMENTE con `definicion.ts`. Coincidía, **y las dos estaban mal**. Se comprobó la consistencia
  entre dos copias y se dio la cifra por buena; ninguna de las dos era la fuente. **Dos copias de acuerdo no son
  una verificación.**
- **Tres lotes de la cinta suben**: 86.25 Blue→**Gold**, 84.50 Red→**Blue**, 84.25 Red→**Blue**. Cambian sus
  códigos (RD-8B15→BL-8B15, RD-3D62→BL-3D62) y sus fichas PDF, regeneradas; las `RD-*` se borraron.
- ⚠️ **La escalera de la cinta pasa de 2 Gold · 2 Blue · 2 Red · 1 Black a 3 Gold · 3 Blue · 1 Black: SIN NINGÚN
  RED.** Es lo que dicta el puntaje, pero cambia lo que ve un visitante.
- **`GradosBoard` dejó de mentir**: LA página de referencia ESCRIBÍA A MANO la fila «Oficial (esta)» mientras su
  cabecera dice que sale de `definicion.ts`. Se habría quedado enseñando la escala vieja bajo ese rótulo. Ahora
  se pinta desde `GRADOS`.
- **Copias reapuntadas**: `public/ocp-apps/cotizador-logistico.html` (rótulo con la escala entera), y los
  comentarios de `sneakPeekMock.ts`, `jsonLd.ts`, `OportunidadSection.tsx` y `definicion.ts`.
- **Notion alineado del todo**: cinco `Min SCA`/`Max SCA`, cinco `Definicion` reescritas (SCA+80 · +82 · +84 ·
  +86 · +88), la página **renombrada «Tiryan» → «Tyrian»**, y tres relaciones movidas otra vez. **Las 9 fichas
  con puntaje verificadas contra `gradoPorPuntaje()`: las 9 cuadran.**
- **`qa-grados-check.mjs` 44 → 48**: los cuatro límites enteros (82 · 84 · 86 · 88), uno por uno — es donde un
  punto de diferencia convierte un Red en Blue y cambia lo que se cobra.
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 · qa-grados 48/48 ·
  qa-sneak-peek 194/0 · qa-ficha-publica 105/0 · qa-definicion 33/33 · qa-docs 14/14 · qa-encoding 738/0.
APP_VERSION 4.43 → 4.44.
(commit: a934249)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V4.45 · `/control-panel` fuera del sitemap · el cacao fuera del índice
- Cierra los DOS últimos puntos de la lista de nueve. Los dos son de indexación.
- **`/control-panel` sale del sitemap.** Es la puerta del EQUIPO (el selector de las tres consolas). Desde el
  2026-08-10 el índice de la red ya no la enlaza —la sustituyó `NetNewsletter`—, así que **el sitemap era lo
  único que seguía nominándola a Google**. `platform_surfaces` → `en_sitemap = false`, con el motivo en `notas`.
  **La página sigue en pie** en `panel.ctcexport.com`. Verificado: el sitemap pasa de 19 a **18** URLs.
- ⚠️ **AÑADIDO TRAS EL SELLADO — el atajo tuvo precio.** (a) se aplicó con un `update` directo a
  `platform_surfaces`, no por el módulo. `guardarSuperficie` hace TRES cosas y el SQL hizo una: escribe la fila
  ✅ · **invalida la caché** (`unstable_cache` + `TAG_SUPERFICIES`) ❌ · deja rastro en `audit_log` y
  `actualizado_at/por` ❌. **El sitemap en producción sirve la copia vieja hasta `revalidate: 3600`** — una hora
  como mucho. La verificación local no lo vio: `next start` recién construido arranca con caché fría. Bien en
  local, viejo en producción — la trampa de siempre, esta vez en el arreglo. **Cierre**: Guardar esa fila desde
  ECP → Manejo de Plataformas invalida al instante y firma el cambio.
- **`mermas-rapida.html` gana `noindex, follow`: la SEGUNDA MITAD del arreglo del 2026-08-14.** Aquel día se
  cambió el `<title>` («para Café y Cacao») porque buscar «Colombian Trading Company» devolvía cacao. Arregló el
  TITULAR — pero un buscador indexa el CUERPO, y la página conserva el conmutador «Café / Cacao», «Diferencias
  Clave: Café vs. Cacao» y el «Proceso del Cacao» entero. **Verificado en vivo el 2026-08-19.**
- ⚠️ **LA HERRAMIENTA NO SE TOCA**, igual que entonces: el modo cacao queda entero (el owner paró la rama que
  quería amputarlo). `follow`, responde 200, se sigue compartiendo. Se quita la candidatura, no la vida.
- **`qa-tools-seo-espejo.mjs` aprendió la excepción**: exigía que una viva NUNCA llevara `noindex`. Ahora hay
  `FUERA_DEL_INDICE`, lista corta con el porqué de cada entrada, y para ellas **invierte** la comprobación.
  *Una regla sin puerta se salta por la ventana.* Verificado por los dos lados.
- ℹ️ **PARA EL DICT**: sacar del sitemap o marcar `noindex` **no borra del índice** — Google tiene que volver a
  rastrear. Y **Search Console no se ve desde aquí** (cuenta del owner): se puede afirmar qué sirve el sitio,
  no qué recuerda Google.
- ℹ️ **«Ají» no existe en el repositorio**: las dos coincidencias son la subcadena de «b*ají*sima cafeína».
- Compuerta: `tsc` limpio · `eslint src` 0 errores (8 avisos) · `npm run build` exit 0 · qa-tools-seo 193/0 ·
  qa-tools-seo-espejo 68/0 · qa-direccionamiento 14/0 · qa-docs 14/14 · qa-encoding 738/0.
APP_VERSION 4.44 → 4.45.
(commit: 58c5caf)

════════════════════════════════════════════════════════════════════════════════

## [2026-08-19] V5.0 · El owner declara el hito: «Pre-Launch Beta»
- **APP_VERSION 4.45 → 5.0.** Quinta generación de la plataforma, declarada por el owner al cerrar TODO el
  trabajo del plan V5: los cinco pasos de la reorganización de consolas (V4.23→V4.35) **y** la lista de nueve
  pendientes del §9 (V4.36→V4.45). Nombre del hito: **«Pre-Launch Beta»** — el corte estable sobre el que
  arranca la etapa siguiente, **«Launch Beta Testing»**.
- Para el DICT: la ficha `versionado` debe recoger el hito y su nombre.
- Cambio de código: solo `src/lib/version.ts` (la insignia pasa a decir V5.0 en las 19 superficies).
(commit: 388af4e)
