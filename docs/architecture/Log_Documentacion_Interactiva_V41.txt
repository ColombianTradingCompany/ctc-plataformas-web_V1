Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V41.0(8ac6f6f).html
(CTC Web Platform, desde 2026-08-22 · plataforma V5.24). Cada entrada se compila
al próximo Version Wrap (V42).

Formato:
## [YYYY-MM-DD] Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pending">)

Recordatorio: registrar la entrada AL TERMINAR el trabajo, no «después». Y sellar
el sha en cuanto exista el commit — de UNO en UNO, no en bloque al final.

════════════════════════════════════════════════════════════════════════════════
QUIÉN ESCRIBE AQUÍ Y QUIÉN LLAMA AL WRAP (desde 2026-09-11, ALINEACION §5)
════════════════════════════════════════════════════════════════════════════════

· EL ASIENTO lo escribe LA SESIÓN DEL COMPONENTE que despliega, en el MISMO commit
  que sube APP_VERSION (lo exige `scripts/qa-arqlog-check.mjs`). Se añade al final;
  nunca se toca un asiento ajeno. `git pull` antes de escribir.
· EL WRAP lo llama SOLO la conversación «Wraps del mapa» (grupo CTC Consolas
  internas, vía `plataforma`). Cadencia: ≥ 5 asientos, un hito de componente cerrado
  o antes de una versión mayor — lo primero que ocurra.
· La cabecera «plataforma VN.N» de este archivo es la referencia del guardián.

════════════════════════════════════════════════════════════════════════════════
CÓMO SE VALIDA UN WRAP — la lista que de verdad hay que correr
════════════════════════════════════════════════════════════════════════════════

✅ LA BATERÍA ES UN ARCHIVO: `docs/architecture/validate_snapshot.mjs`. Corre en
NODE, no en la consola del navegador. Uso, desde `ctc-platform/`:

    node docs/architecture/validate_snapshot.mjs docs/architecture/<nuevo>.html --prev docs/architecture/<anterior>.html [--dump]

`--prev` imprime los contadores del snapshot anterior al lado de los del nuevo;
`--dump` lista nodos, claves de DICT, ids de escenarios y CTX. Correrlo también
contra el snapshot ANTERIOR como control: si pasa en los dos, no es permisivo.

Las NUEVE comprobaciones, y las nueve tienen que volver VACÍAS:
  1. pasos de traza (`q.n` y `q.from`) contra los `.node[id]` reales
  2. `DICT[k].a[]` contra las claves de DICT
  3. `WIRES` contra los nodos reales
  4. `CTX` — nodo real Y claves reales
  5. `ANN` huérfanas: rutas que ya no existen en el FILETREE recién regenerado
  6. `ANN[...].info` contra las claves de DICT
  7. `.ibtn[data-info]` del marcado contra DICT
  8. `.chip[data-info]` — SOLO sobre el marcado ANTERIOR a `<script>`
  9. claves REPETIDAS en el texto de DICT/CTX (JS se queda con la última en silencio)

Y los contadores tienen que subir EXACTAMENTE lo esperado
(V32: 95→99 · V33: 99→102 · V34: 102→104 · V35: 104→107 · V36: 107→113 ·
V37: 113→121 · V38: 121→128 · V39: 128→133 · V40: 133→136 · V41: 136→137).

Además, después de `gen_filemap_tree.mjs`, un `node --check` del bloque `<script>`
extraído (el validador evalúa los literales, no el resto del motor).

⚠️ EL MAPA NODO→FICHAS SE LLAMA `CTX`, NO `NODEDICT` (V34). ⚠️ EL SELECTOR DE
TRAZAS VA POR ÍNDICE (V33): la comprobación buena es
`select.options.length === SCENARIOS.length`. ⚠️ EL DICT TIENE DOS ESTILOS DE
ESCRITURA (V31): contar con `Object.keys(DICT).length` evaluado, nunca por regex.
⚠️ UN BACKTICK EN UN CUERPO DE DICT ROMPE EL LITERAL ENTERO (V38): siempre
`<code>…</code>`. ⚠️ EL SHA DEL NOMBRE ES EL DEL CONTENIDO (V35). ⚠️ LA CADENA
«Última actualización» DEBE NOMBRAR AL PROPIO SNAPSHOT (V38 olvidó la suya).

════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════
LO QUE EL WRAP V41 ENSEÑÓ (léase antes del V42)
════════════════════════════════════════════════════════════════════════════════

· UN WRAP DE UNA SOLA ENTRADA ES UN WRAP PEQUEÑO Y ESTÁ BIEN: V5.24 llegó con su
  asiento escrito al terminar, con la lista exacta de qué tocar (ficha nueva,
  traza, dos fichas vecinas). Compilarlo tomó un parche de 9 bloques. El
  tamaño del wrap lo decide la disciplina del log, no el calendario.

════════════════════════════════════════════════════════════════════════════════

Contexto abierto al cerrar V41 (2026-08-22) — plataforma V5.24:

- **Lo que V41 compiló**: V5.24 — Subastas Tyrian, la puja del comprador
  (lot_auctions + auction_bids, guard trigger, TyrianSection real, /ocp/subastas;
  adjudicar no emite oferta — EUR vs COP). 1 ficha nueva («Subastas Tyrian») ·
  1 traza nueva («La puja Tyrian») · 2 fichas actualizadas (ofertas, cherry) ·
  6 ANN nuevas.
- **Seguimientos acordados sin construir**: ninguno. Todo lo pactado en el
  rediseño del panel (V5.16) y sus derivados está desplegado.
- **Pendientes del owner que este log arrastra**: el número Nequi real; la
  línea del redirect-allowlist de Google en Supabase; la primera generación
  real de Redacción; el escenario de Make de coffeed.redaccion.post_creado;
  recorrer el BLOQUE B del review sobre el panel nuevo; la primera
  jornada-vitrina supervisada; estrenar el escáner visual con soportes REALES;
  y la primera subasta real cuando el bache galardone un Tyrian (hoy la
  sección de Cherry Picked Green dice «no hay subasta abierta»).
- **Línea base de la compuerta al cerrar**: tsc limpio · eslint 0 errores /
  8 avisos · build exit 0 · qa-subastas 30 · qa-fichas 31 · qa-ofertas 36 ·
  qa-kr-panel 119 · qa-changelog 225 · qa-encoding 791/0.

════════════════════════════════════════════════════════════════════════════════

## [2026-08-22] Version Wrap V41 — la puja Tyrian, compilada
- Snapshot nuevo: `Documentacion_Interactiva_V41.0(8ac6f6f).html` (base V40.0 +
  la entrada V5.24 del log V40). Batería 9/9 vacías, `node --check` OK.
  Contadores: 39 nodos · 137 fichas (+1: subastas) · 52 trazas (+1) · 91 wires ·
  34 CTX · 325 ANN (+6) · FILETREE 1927.
- El puntero de HANDOFF («Visual system map») apunta al V41.0.
(commit: 7a94ddf)

## [2026-09-11] Reorganización por componentes — docs, no código
- Sin cambio de sistema: la plataforma sigue en V5.30. Cambia CÓMO se documenta y se
  trabaja: `docs/REFURBISH_PLAN.md` (el plan y sus decisiones), `docs/ALINEACION.md`
  (contratos transversales + regla del backstage + registro de permeación),
  `docs/componentes/<clave>.md` (diez charters: consolas, herramientas-internas, biblia,
  kaffetal-regal, cherry-picked, herramientas-cafe, coffeed, directorio, ctc-tech,
  varietales), `docs/HANDOFF.md` adelgazado a lo transversal y su cronología en
  `docs/archive/HANDOFF_cronologia_2026-07_09.md`; `AGENTS.md` al día (V5.30).
- Fuera del repo: `C:\dev` ordenado (reference_* → reference/<tema>, apps internas en
  apps-internas/, prototipos → commaas-hub/tenants-pendientes/), memoria de Claude
  unificada por espacio.
- Para el wrap V42: ANN nuevas para `docs/ALINEACION.md`, `docs/REFURBISH_PLAN.md`,
  `docs/componentes/` (carpeta) y el archivo de cronología; la ficha DICT «docsistema»
  merece un párrafo (el orden de lectura charter → alineación → handoff); el FILETREE
  regenerado verá `reference/` y `apps-internas/` en vez de `reference_*`.
(commit: d9f4b17)

## [2026-08-22] V5.25–V5.27 · Herramientas del Café: nace «Defectos del Café» (asiento compilado del CHANGELOG el 2026-09-11)
- Tres versiones de OTRAS sesiones sin asiento propio (la lección de V39, repetida; desde
  hoy lo exige `scripts/qa-arqlog-check.mjs`). V5.25: nueva herramienta pública
  `public/tools/defectos-cafe.html` — catálogo interactivo de los 14 defectos del café verde
  del afiche de la FNC + defectos del tueste, clave visual (grano o taza), matriz de
  incidencia, control por etapa y práctica de reconocimiento; ES/EN/DE; registrada en
  `tools`/`tool_versions` (id `defectos-cafe`, orden 20) encendida solo en web. V5.26:
  captura del carrusel (`public/images/herramientas/shots/defectos-cafe.jpg`). V5.27: la
  clave pintaba la foto encima de la etiqueta (`max-height:%` contra fila auto de grid +
  `drop-shadow` = contexto de apilamiento); caja flex de alto fijo. Para el wrap: una línea
  en el DICT «herramientas» y la fila del inventario.
(commit: 90593cd · 4bb787d · 76993c7)

## [2026-09-10] V5.28–V5.29 · BCP · PVC — la Ponderación de Valor de Cosecha entra al sistema (asiento compilado del CHANGELOG)
- Hito: `/bcp/pvc` (owner-only, Business Core) — Ediciones · Tablero (HTML embebido y
  autenticado con puente `window.PVC_DB`) · Parámetros del modelo (versiones inmutables por
  acta) · Dossier (D0–D9 servidos desde `docs/pvc/`). Motor TypeScript `src/lib/pvc/motor.ts`
  (port de `pvc_model_v2.py`, método v2.1.1: edición, escalera Black ×1,15 · Red ×1,30 · Blue
  ×1,60 · Gold ×2,00, pila FCA/CIP/DDP, back-proof 2019–2026, KPIs, huella) anclado a
  `paridad.json` por `qa-pvc-motor.mjs`; `GET /api/pvc/current` sobre la vista
  `public_pvc_current`. Migración `bcp_pvc_core`: `pvc_model_versions`, `pvc_editions` (guard:
  publicada = inmutable; corrección = fila nueva con `correction_of`), `pvc_cycles`,
  `pvc_sources`, `pvc_trigger_watch`, `pvc_forecast_scores`. Semilla PVC-F4-2026 = $2.500.000.
  V5.29: coherencia del dossier (código sin sufijo de versión, D4 ES/EN = «Exportar Reporte»),
  el Python de referencia redondea comercial como Excel y el tablero, `qa-pvc-tablero.mjs`
  (marcadores `@@MODELO`), una sola ruta de publicación. Para el wrap: nodo/DICT nuevo «pvc»
  (¿en el nodo n-bcp?), ANN de `src/lib/pvc`, `docs/pvc`, `/bcp/pvc`, los dos guardianes; y el
  CONFLICTO abierto con «grados» (bandas 80/84/86/88/89 vs dos en dos) — decisión del owner
  (`docs/PVC_BCP_PLAN.md` §8).
(commit: 3ae9914 · 9f57a17)

## [2026-09-11] V5.30 · El vídeo de presentación definitivo (asiento compilado del CHANGELOG)
- `EcosystemSection` (CTC Home, «Tres ofertas») y `BienvenidosSection` (Kaffetal Regal) apuntan
  al vídeo definitivo (`qDAu9mK3KRA`, 5:33) en vez del provisional; el copy deja de prometer
  «un minuto» en las tres lenguas. Para el wrap: una frase en los DICT «home» y «ytembed».
(commit: 12ea217)

## [2026-09-11] V5.31 · La plataforma se trabaja por componentes (docs) + qa-arqlog
- La reorganización del asiento anterior recibe versión propia (V5.31 · Docs) para que la
  insignia y el CHANGELOG la registren. Nuevo guardián `scripts/qa-arqlog-check.mjs` y la
  regla de versionado/wraps en paralelo (ALINEACION §5): asiento del componente, wrap de la
  plataforma desde «Wraps del mapa». Para el wrap: la ficha «versionado» gana el guardián y
  la regla; la ficha «docsistema», el orden de lectura y quién llama al wrap.
(commit: pendiente)

