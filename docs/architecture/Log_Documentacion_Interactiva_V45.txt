Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V45.0(55c8aaa).html
(CTC Web Platform, desde 2026-09-19 · plataforma V5.58). Cada entrada se compila
al próximo Version Wrap (V46).

Formato:
## [YYYY-MM-DD] V5.NN · Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pendiente">)

════════════════════════════════════════════════════════════════════════════════
QUIÉN ESCRIBE AQUÍ Y QUIÉN LLAMA AL WRAP (desde 2026-09-11, ALINEACION §5)
════════════════════════════════════════════════════════════════════════════════

· EL ASIENTO lo escribe LA SESIÓN DEL COMPONENTE que despliega, en el MISMO commit
  que sube APP_VERSION — lo exige `scripts/qa-arqlog-check.mjs` (toda versión del
  CHANGELOG posterior a «plataforma V5.58» tiene que aparecer aquí). Se añade al
  final; nunca se toca un asiento ajeno. `git pull` antes de escribir.
· EL WRAP lo llama SOLO la conversación «WRAP-COMMIT-PUSH (CTC Platforms)» (grupo CTC
  Consolas internas, vía `plataforma`; existe desde el 2026-09-19 — antes la regla
  nombraba una «Wraps del mapa» que nunca existió). Es el NODO FINAL: antes de compilar
  audita que el maestro y los charters estén en el mismo punto (ALINEACION §5.3). Cadencia: ≥ 5 asientos, un hito de componente cerrado
  o antes de una versión mayor — lo primero que ocurra.
· La línea «plataforma V5.58» de la cabecera es la referencia del guardián: el wrap
  siguiente la reescribe con la versión sobre la que cierra.

════════════════════════════════════════════════════════════════════════════════
CÓMO SE VALIDA UN WRAP — la lista que de verdad hay que correr
════════════════════════════════════════════════════════════════════════════════

✅ LA BATERÍA ES UN ARCHIVO: `docs/architecture/validate_snapshot.mjs`. Corre en
NODE, no en la consola del navegador. Uso, desde `ctc-platform/`:

    node docs/architecture/validate_snapshot.mjs docs/architecture/<nuevo>.html --prev docs/architecture/<anterior>.html [--dump]

`--prev` imprime los contadores del snapshot anterior al lado de los del nuevo;
`--dump` lista nodos, claves de DICT, ids de escenarios y CTX. Correrlo también
contra el snapshot ANTERIOR como control: si pasa en los dos, no es permisivo.

Las NUEVE comprobaciones, y las nueve tienen que volver VACÍAS:
  1. pasos de traza (`q.n` y `q.from`) contra los `.node[id]` reales
  2. `DICT[k].a[]` contra las claves de DICT
  3. `WIRES` contra los nodos reales
  4. `CTX` — nodo real Y claves reales
  5. `ANN` huérfanas: rutas que ya no existen en el FILETREE recién regenerado
  6. `ANN[...].info` contra las claves de DICT
  7. `.ibtn[data-info]` del marcado contra DICT
  8. `.chip[data-info]` — SOLO sobre el marcado ANTERIOR a `<script>`
  9. claves REPETIDAS en el texto de DICT/CTX (JS se queda con la última en silencio)

Y los contadores tienen que subir EXACTAMENTE lo esperado
(V32: 95→99 · V33: 99→102 · V34: 102→104 · V35: 104→107 · V36: 107→113 ·
V37: 113→121 · V38: 121→128 · V39: 128→133 · V40: 133→136 · V41: 136→137 ·
V42: 137→139 · V43: 139→140 · V44: 140→146 · V45: 146→148).

Además, después de `gen_filemap_tree.mjs`, un `node --check` del bloque `<script>`
extraído (el validador evalúa los literales, no el resto del motor).

⚠️ EL MAPA NODO→FICHAS SE LLAMA `CTX`, NO `NODEDICT` (V34). ⚠️ EL SELECTOR DE
TRAZAS VA POR ÍNDICE (V33): la comprobación buena es
`select.options.length === SCENARIOS.length`. ⚠️ EL DICT TIENE DOS ESTILOS DE
ESCRITURA (V31): contar con `Object.keys(DICT).length` evaluado, nunca por regex —
y OJO al anclar un parche: las entradas viejas van `clave: {` (con espacio) y las
nuevas `clave:{`. ⚠️ UN BACKTICK EN UN CUERPO DE DICT ROMPE EL LITERAL ENTERO
(V38): siempre `<code>…</code>`. ⚠️ EL SHA DEL NOMBRE ES EL DEL CONTENIDO (V35).
⚠️ LA CADENA «Última actualización» DEBE NOMBRAR AL PROPIO SNAPSHOT (V38 olvidó la
suya). ⚠️ UN PARCHE DE WRAP CAMBIA SUS PROPIAS ANCLAS (V40): cada replace lleva
assert.

════════════════════════════════════════════════════════════════════════════════
LO QUE EL WRAP V45 ENSEÑÓ (léase antes del V46)
════════════════════════════════════════════════════════════════════════════════

· EL WRAP SE LLAMÓ POR PRIMERA VEZ DESDE SU VÍA. «WRAP-COMMIT-PUSH (CTC Platforms)» existe desde el 2026-09-19 y este
  es su primer wrap. Se llamó como FASE 0 de un overhaul ya aprobado (`docs/OVERHAUL_CONSOLAS_PLAN.md`): cuando se va a
  mover media arquitectura, el wrap de ANTES vale más que el de después — es la foto contra la que se mide.

· UN ASIENTO PUEDE QUEDAR INVERTIDO POR OTRO DEL MISMO LOG. El de la V5.53 decía «el Modelo Económico es únicamente del
  BCP (charter consolas)» y el de la V5.55 lo invirtió esa misma tarde. El wrap compila el ESTADO FINAL, no la suma de los
  asientos: se leyó el log entero antes de escribir una sola ficha. Un asiento que corrige a otro anterior debe decirlo
  con su versión («INVIERTE el asiento de la V5.NN»), como hizo el de la V5.55.

· LAS FICHAS DE UNA LÍNEA Y LAS MULTILÍNEA SE PARCHEAN CON EL MISMO AYUDANTE. `add_par(clave, html)` localiza la ficha por
  `^  clave\s*:\s*{` hasta la siguiente ficha, y dentro busca el ÚNICO cierre «backtick , a:[» — vale para los dos
  estilos. `add_a` añade al último `a:[…]` de la ficha. `append_note(nodo)` sigue siendo el del V44. Las entradas de CTX NO
  siempre llevan espacio tras los dos puntos (`"n-bcp":[` frente a `"n-rutas": [`): el patrón es `"nodo"\s*:\s*\[`.

· NINGUNA ANN QUEDÓ HUÉRFANA, PESE A MOVER CUATRO CARPETAS. Las rutas de `src/app/ecp/(app)/cotizador-*` no estaban
  anotadas. ⚠️ El overhaul sí moverá carpetas anotadas (buzón, leads, CRM, nominados…): la comprobación 5 las va a cantar
  en el V46, y cada fase del plan debe dejar en su asiento la lista de rutas que repuntar.

· SE VALIDÓ EN EL NAVEGADOR con la configuración `docs-architecture` de `C:\dev\.claude\launch.json` (puerto 4300):
  `select.options.length === SCENARIOS.length` (59), las dos fichas y la traza nuevas presentes, 7 notas «V45», cero
  referencias colgantes recalculadas en vivo, cero errores de consola. Más `node --check` del bloque `<script>`.

════════════════════════════════════════════════════════════════════════════════

Contexto abierto al cerrar V45 (2026-09-19) — plataforma V5.58:

- **Lo que V45 compiló**: V5.53–V5.58 y los dos asientos DOCS del 19-sep. 2 fichas nuevas (`nivelesconsola`,
  `guardianespejo`) · 1 traza nueva («Un viewer pulsa un botón que emite») · 13 fichas actualizadas · 7 notas de nodo ·
  1 entrada CTX nueva (`n-rutas`) y dos ampliadas · 8 ANN. 40 nodos · 148 fichas · 59 trazas · 95 wires · 36 CTX · 389 ANN.
- **Deuda de compilación**: ninguna.
- **Lo que viene, y ya tiene orden**: `docs/OVERHAUL_CONSOLAS_PLAN.md`, aprobado por el owner («todo lo recomendado»,
  D1–D10). Fase 1: nace la cuarta consola, **LCP**, y se lleva buzón, leads, listas de espera y los cuatro CRM. Fase 2:
  Ecosistema de Valor al BCP y Herramientas Internas de vuelta al ECP. Fase 3: la tabla única Productor·Finca·Lote. Fase 4:
  el circuito del lote sin Sondeo. Fase 5: Kaffetal Regal (exige las cuentas `prueba-*`). Fase 6: stock físico, por briefs.
- **Lo que el mapa TODAVÍA no refleja, y es correcto** (no inventarlo): la LCP; nada del circuito nuevo; los modelos de
  Procesamiento y Logística, que no tienen módulo; las plataformas de pago; el stock físico.
- **Guardianes sin correr**: `qa-guard-check` y `qa-checkout-check` (piden las cuentas `prueba-*`). Y DOS que NO van en
  ninguna batería porque gastan dinero: `qa-cromatografia-modelo` y `qa-transcripciones-nube`.
- **Línea base de este wrap** (solo docs): validate_snapshot 9/9 vacías, con el V44 como control · `node --check` ·
  navegador sin errores · qa-changelog · qa-arqlog · qa-encoding · qa-docs.

════════════════════════════════════════════════════════════════════════════════

## [2026-09-19] V5.59 · Nace la LCP · Lead Control Panel, la cuarta consola (consolas → cherry-picked, ctc-tech, varietales, ctc-home, directorio, herramientas-cafe, terratalento)
- NODO NUEVO para el V46: **`n-lcp`** — LCP · Lead Control Panel, «Relationship»: lo que entra de fuera y a quién se le
  responde. Rail: «LCP · General» (Buzón de entrada, Leads · Recepción, Lista de espera) y «LCP · CRM» (CaaS, Green, Roast,
  X). Acento `#F0708A` (carmesí corporativo aclarado; el azul acero del plan no se distinguía del OCP). WIRES: `n-login` →
  `n-lcp` (misma llave), y lo que hoy sale de `n-ecp` hacia buzón/leads y de `n-ocp` hacia los CRM pasa a salir de `n-lcp`.
  Actualizar las notas de `n-ecp` (pierde buzón, leads y la lista de la portada) y `n-ocp` (pierde el grupo Cherry Picked).
- Fichas que cambian: `consolas` / `rutasmovidas` (42 rutas; primera mudanza que REAPUNTA dos entradas a una consola que no
  existía cuando nacieron: `/bcp/caas`, `/ocp/leads`); `nivelesconsola` (cinco borradores de la lista blanca viven ahora en
  la LCP; la clase no cambió); `leads` (la consola del pilar se DEDUCE de la ruta del tablero: `src/lib/panel/leadsPilares.ts`
  — desaparece `PILLAR_CONSOLE`, el mapa de permisos sin barras que se quedó atrás en dos mudanzas; la compuerta fina mira
  el NIVEL y devuelve el rechazo); `listasdeespera` (la lista reunida: seis listas, dos tablas, un filtro con pendientes).
- Para la ficha `guardianespejo`: QUINTO caso, y el más sutil. `qa-crm-interes` afirmaba «exige admin activo» buscando la
  palabra `requireActiveAdmin`… que aparecía en la RUTA DEL IMPORT (`@/lib/panel/requireActiveAdmin`). Habría pasado con la
  acción sin compuerta alguna. Y `qa-rutas-consolas` no veía las compuertas en array: la lista de espera compartida llevaba
  desde la V5.57 fuera de su mapa «a propósito», que quería decir sin vigilar. Las dos se deducen ahora del rail.
- LECCIÓN: **una lista escrita a mano en un guardián es una consola que el guardián no mira.** `bcp|ecp|ocp` estaba nueve
  veces entre dos guardianes; el paso 1 de la fase fue derivarla de `CONSOLE_ORDER` y verla en verde CON TRES antes de añadir
  la cuarta. Todo lo que enumera consolas lee ya de ahí (`/panel`, conmutador, credenciales, `grantedConsoles`, guardianes).
- LECCIÓN: **lo que sirve a dos consolas no cuelga del árbol de ninguna** (la que dejó `shared.module.css` en PR-B y repitió
  `InteresBoard` en la V4.39). `leadsActions.ts` colgaba del ECP y servía a cuatro tableros de dos consolas: la mudanza se lo
  habría llevado. Vive en `src/components/panel/`, con `LeadModalRow.tsx`.
- TRAZA candidata: «Alguien deja su correo en la portada» → `subscribeNewsletter` (`source: ctc-home`) →
  `newsletter_subscribers` → LCP · Lista de espera (filtro, pendientes) → `marcarContactado` (borrador; abre LCP y ECP) →
  revalida la lista reunida Y el tablero de la fuente.
- ⚠️ ANN QUE EL V46 TIENE QUE REPUNTAR (la comprobación 5 las cantará como huérfanas):
  `ctc-platform/src/app/ecp/(app)/buzon` → `ctc-platform/src/app/lcp/(app)/buzon` ·
  `ctc-platform/src/app/ecp/(app)/ctc-home/page.tsx` → `ctc-platform/src/app/lcp/(app)/lista-espera/page.tsx` ·
  `ctc-platform/src/app/ecp/(app)/leadsActions.ts` → `ctc-platform/src/components/panel/leadsActions.ts`.
  ANN nuevas que merecen entrada: `src/app/lcp/(app)/layout.tsx`, `src/lib/panel/leadsPilares.ts`, `src/app/lcp/(app)/crm`.
- NO inventar en el mapa: `EstructuraModal.tsx` (el diagrama de `/bcp/documentacion`) sigue dibujando TRES consolas — se
  redibuja al cerrar la fase 2. La fase 2 moverá además Directorio, Herramientas y Terratalento al BCP.
- Datos: `panel_users.consoles` ganó `lcp` en las tres credenciales (owner `admin`, colaboradores `viewer`).
(commit: cfb0808)

## [2026-09-19] V5.60 · El nuevo reparto BCP ↔ ECP, y el Tablero de Ejecución (consolas → herramientas-internas, coffeed, socios, directorio, herramientas-cafe, terratalento, ctc-tech, varietales, kaffetal-regal)
- Notas de NODO que el V46 tiene que reescribir: **`n-bcp`** — «lo que la casa ES»: Ecosistema de Valor (Herramientas,
  Directorio, Coffeed, CTC Tech, Varietales, Terratalento, KR Arena + Club) y Configuración del Sistema (usuarios, socios,
  documentación, mapa, consumo, plataformas). **`n-ecp`** — «con qué decide y qué tiene pendiente»: el Tablero de Ejecución,
  Transcripciones y Herramientas Internas por modelo (Contexto · Económico en Origen · Producción · Logístico · Automatizaciones).
  **`n-ocp`** pierde la Arena y el Club. WIRES: lo que salía de `n-ecp` hacia Coffeed, Directorio, Herramientas, Terratalento,
  CTC Tech y Varietales sale ahora de `n-bcp`; lo que salía de `n-bcp` hacia el PVC y los cotizadores, de `n-ecp`.
- Fichas que cambian: `rutasmovidas` (49 rutas; **el VIAJE DE VUELTA** es un concepto nuevo y merece párrafo propio: nueve
  módulos volvieron a una URL que era un talón — se borra el talón, se borra la entrada, la inversa va aparte, se reapunta lo
  que apuntaba a la casa que se deja; y un hijo cuyo padre vuelve a ser página real pasa a talón EXPLÍCITO); `herramientasinternas`
  (vive en el ECP; duró cinco versiones en el BCP); `modeloeconomico` / `pvc` (`/ecp/pvc`; compuerta `"ecp"`; los KPI siguen en el
  Panel del BCP); `coffeed` (la consola de sus dos compuertas se LEE del rail: `consolaDelModulo`); `nivelesconsola` (la casilla de
  tarea abre la consola dueña y el ECP); `arena` (en el BCP, con tres acciones del OCP que abren las dos consolas).
- FICHA NUEVA candidata: **`tablerodeejecucion`** — las tareas DERIVADAS de la casa. No se guardan: se deducen de un estado que
  pide a alguien; lo único persistido es la casilla (`bcp_task_state`). `tareas.ts` (puro: tipos, consola dueña por clave,
  paneles a revalidar) + `tareasCarga.ts` (las cinco lecturas) + `PanelTasks` + `tareasActions`. Cada consola aporta las suyas;
  `/ecp` las enseña todas agrupadas; el Panel del OCP, las suyas.
- FICHA NUEVA candidata: **`espejoamano`** (o párrafo en `guardianespejo`) — el diagrama «espejo del menú» de la documentación
  pintó durante un mes el reparto de antes de la V4.24. Un espejo escrito a mano no refleja. Se genera del rail. Misma familia
  que la lista de consolas nueve veces en dos guardianes (V5.59) y que `PILLAR_CONSOLE`: **todo lo que REPITE el rail se queda atrás;
  todo lo que lo LEE, no.** Tres casos en dos versiones: `CONSOLE_ORDER`, `leadsPilares`, `consolaDelModulo`.
- TRAZA candidata: «Un productor escribe un mensaje» → `producer_comm_log` → `cargarTareas` → aparece en el Panel del OCP y
  en el Tablero de Ejecución del ECP → alguien marca la casilla → `setTaskState` (borrador; abre `ocp` y `ecp`) → revalida `/ocp` y `/ecp`.
- LECCIÓN: **una reescritura ciega de rutas deja tres clases de resto, y cada una la caza otra cosa.** Las URLs absolutas
  (`https://panel…/ocp/arena/…` en un correo) — las cazó el guardián (c), porque el patrón exigía límite de segmento a la
  izquierda. Los imports relativos que cruzan de árbol (`../evaluationActions` desde la Arena) — los cazó `tsc`. Y la PROSA
  (títulos de pestaña «· BCP», cabeceras, «tu sesión del BCP») — no la caza nadie: se buscó a mano, consola por consola.
- ⚠️ ANN QUE EL V46 TIENE QUE REPUNTAR: todo `ctc-platform/src/app/ecp/(app)/{herramientas,directorio,coffeed,ctc-tech,varietales,
  terratalento,plataformas}` → `…/bcp/(app)/…`; `…/ocp/(app)/{arena,club,arenaActions.ts,clubActions.ts}` → `…/bcp/(app)/…`;
  `…/bcp/(app)/{direccionamiento,pvc,cotizador-*,anclas-mercado,automatizaciones,direccionamientoActions.ts}` → `…/ecp/(app)/…`;
  `…/ocp/(app)/{ActionForm.tsx,PanelTasks.tsx,dashboardActions.ts}` → `ctc-platform/src/components/panel/{ActionForm.tsx,
  PanelTasks.tsx,tareasActions.ts}`. ANN nuevas: `src/lib/panel/{tareas.ts,tareasCarga.ts}`.
- NO inventar en el mapa: «Seguimiento de Temas» y «Plataformas de Pagos» están en el cuadro del owner y NO en el rail.
(commit: 257c42d)

## [2026-09-19] V5.61 · Productores, Fincas y Lotes son UNA tabla: `/ocp/kr` (consolas → kaffetal-regal, cherry-picked)
- Nota de NODO `n-ocp`: el grupo «OCP · Kaffetal Regal» es UNA entrada, «Productores, Fincas y Lotes» (`/ocp/kr`), más Fichas
  Técnicas. Galardonados ya no es módulo (filtro de la tabla). La Arena y el Club salieron en la V5.60.
- FICHA NUEVA candidata: **`tablaunica`** — grano de LOTE, con fila propia para la finca sin lote y el productor sin finca; una
  carga de nueve lecturas ligeras para DOS vistas (tabla y mapa) con los MISMOS filtros; la vista completa por parámetro
  (`?lote=` · `?finca=` · `?productor=`), protagonista Lote → Finca → Productor, que lee SOLO lo que abre. `a:` hacia `eva`,
  `visaeudr`, `gradoscalidad`, `ofertas`, `rutasmovidas`.
- Fichas que cambian: `rutasmovidas` (53; **la mudanza «MUCHAS A UNA»** merece párrafo junto al «viaje de vuelta» de la V5.60:
  las sub-rutas viajan pegadas al destino —por eso el dossier y el KML de la Visa se mudaron a `kr/[id]/…`— y el ANCLA no viaja
  en un 308: la traduce el cliente al llegar); `visaeudr` (el constructor de sus campos tiene una fuente, `src/lib/ocp/fincaEudr.ts`);
  `eva` (vive en la vista completa del lote, no en un kanban); `galardonados` (retirada como módulo).
- Para `guardianespejo` / `espejoamano`: TERCER caso de la misma familia en tres versiones — **tres etiquetas para la misma etapa
  en tres pantallas** (`ficha_completa` = «En evaluación (EVA)» · «Ficha enviada» · «En EVA»), y el constructor de la Visa escrito
  dos veces. No es un guardián que copie: es CÓDIGO que se copia. La cura es la misma: una fuente y que la lean.
- LECCIÓN: **convertir, no reescribir.** Las tres páginas (1.427 líneas) pasaron a ser tres secciones con `git mv` y un parche
  que filtra por id y quita el tablero: la lógica de la EVA y de la Visa es la MISMA línea por línea, y `git log --follow` la
  sigue. Reescribir a ciegas una pantalla que no se puede abrir habría sido apostar.
- LECCIÓN: **lo que no se puede mirar se verifica por donde sí se puede.** Sin navegador (OTP real): `tsc`, build, guardianes, y
  SQL — que las 41 columnas pedidas existen (un typo en un `.select()` no lo ve el compilador) y que la lógica de filas, replicada
  en SQL, da 35. Y el SQL encontró algo que el código no decía: 7 lotes sin `finca_id`, que resultaron borradores vacíos — y la
  pregunta destapó que un lote de VARIAS fincas (aportes) habría salido como «sin finca».
- ⚠️ ANN QUE EL V46 TIENE QUE REPUNTAR: `ctc-platform/src/app/ocp/(app)/{productores,fincas,lotes,galardonados}` (y sus hijos) →
  `ctc-platform/src/app/ocp/(app)/kr/…` (`productores/page.tsx` → `kr/ProductorSeccion.tsx`; `fincas/page.tsx` →
  `kr/FincaSeccion.tsx`; `lotes/page.tsx` → `kr/LoteSeccion.tsx`; `lotes/LotesViews.tsx` → `kr/LotePiezas.tsx`; `fincas/[id]` →
  `kr/[id]`). RETIRADOS: `fincas/FincaModalRow.tsx`, `fincas/FincasViewSwitch.tsx`, `productores/ProductoresBoard.tsx`,
  `galardonados/page.tsx`. ANN nuevas: `src/lib/ocp/{etapas.ts,fincaEudr.ts}`, `src/app/ocp/(app)/kr/{page.tsx,carga.ts,KrTabla.tsx}`.
- NO inventar en el mapa: nadie ha visto la pantalla pintada todavía; el owner tiene pendiente abrirla.
(commit: dd3b734)

## [2026-09-19] V5.62 · El estado del circuito del lote, DERIVADO (consolas → kaffetal-regal)
- FICHA NUEVA candidata: **`circuitodellote`** — *en ficha → a evaluar → en evaluación → pendiente de oferta → oferta emitida →
  catálogo activo* (+ *no apto*). No es una columna: `estadoDelCircuito()` (`src/lib/ocp/circuito.ts`, puro) lo deriva de la
  inscripción y su pago, el recibo de la muestra, el grado, la última oferta y el contrato, y dice además LO QUE FALTA. Las
  reglas van de atrás hacia adelante —lo más avanzado gana—, porque los datos se acumulan. `a:` hacia `tablaunica`, `eva`,
  `gradoscalidad`, `ofertas`, `derivadonosepersiste` (si existe; si no, es buen momento para escribirla: la regla tiene ya
  cuatro casos — etapa del comprador, tareas del Tablero, consola del pilar, circuito del lote).
- Para `tablaunica`: gana la columna «Circuito» y su filtro.
- Para `guardianespejo`: el caso que SALIÓ BIEN. `qa-circuito-check` nació con su tabla de verdad escrita desde las notas del
  owner (cada caso cita la nota), y con dos propiedades en vez de ejemplos sueltos: TOTAL (13.440 combinaciones) y MONÓTONA
  (ningún avance hace retroceder). Una propiedad no se puede copiar del módulo que se vigila.
- LECCIÓN: **un plan aprobado también se audita al ejecutarlo.** La fase 4 tenía un problema de ORDEN que nadie vio al
  aprobarla: su guard (no aceptar una oferta sin disponibilidad declarada) depende de una pantalla de la fase 5, bloqueada.
  Desplegado como estaba escrito, ningún productor habría podido aceptar una oferta. Se partió: 4a (lo derivado, sin riesgo)
  sale; 4b espera al owner, y el porqué está escrito en el plan, no en la memoria de nadie.
- NO inventar en el mapa: las pantallas «Lotes a evaluar / en evaluación / pendiente de oferta / catálogo activo» NO existen;
  `/ocp/nominados` sigue siendo donde se confirma el pago, se recibe la muestra y se emite el veredicto. El veredicto NO se partió.
- ANN nuevas: `src/lib/ocp/circuito.ts`, `scripts/qa-circuito-check.mjs`.
(commit: eaea06c)

## [2026-09-19] V5.63 · El rail del OCP es el cuadro del owner, y a la LCP le faltaba su layout raíz (consolas → kaffetal-regal, cherry-picked)
- Nota de NODO `n-ocp`: tres grupos — «Kaffetal Regal» (Productores, Fincas y Lotes), «Catálogo» (Lotes a Evaluar · Lotes en
  Evaluación · Pendiente Oferta · Catálogo Activo · Ofertas CP Aceptadas · Oferta desde CTCx Selection) y «Manejo de Stock
  Físico» (Gestión de Muestras · Compras, SIN módulo). Sin «Panel» ni «Fichas Técnicas» en el rail. `n-bcp` y `n-lcp`: sin «Panel».
- Fichas que cambian: `nominados` → pasa a describir DOS vistas de un componente (`nominados/CircuitoVista.tsx`): `/ocp/a-evaluar`
  y `/ocp/en-evaluacion`; los baches siguen en la segunda porque el veredicto exige un bache en «registro». `circuitodellote`:
  el rail sigue ya el mismo orden que la función. `rutasmovidas` (54): primera mudanza «UNA A MUCHAS» —la inversa de la V5.61—;
  la URL vieja aterriza en la primera parada del camino. `consolas`: la D9 («el rail no promete lo que no hay») tiene una
  excepción decidida por el owner.
- Para `guardianespejo` / `espejoamano`: CUARTO caso de la familia, y el que más enseña. La V5.59 derivó la lista de consolas de
  `CONSOLE_ORDER` en guardianes, `/panel`, conmutador y credenciales… y se le escapó que **una consola nueva necesita además un
  archivo que nadie enumera**: su layout raíz. La LCP vivió tres versiones sin tema —texto lavado sobre blanco— con todo en verde.
  Lo vio el owner en una captura. LECCIÓN: **derivar la LISTA no basta; hay que derivar el INVENTARIO de lo que cada elemento de
  la lista exige.** (h) lo hace ahora para las consolas.
- LECCIÓN: **lo que el owner dibuja se puede dar HOY con etiquetas, sin tocar reglas.** El cuadro pedía seis entradas de Catálogo;
  cuatro ya existían con otro nombre y dos salieron de partir una página. La 4b —que sí cambia reglas— sigue parada, y el owner
  tiene ya su rail. Rótulo y ruta no tienen por qué coincidir (como `/ecp/pvc`).
- ⚠️ ANN QUE EL V46 TIENE QUE REPUNTAR: `ctc-platform/src/app/ocp/(app)/nominados/page.tsx` →
  `…/nominados/CircuitoVista.tsx`. ANN nuevas: `src/app/lcp/layout.tsx`, `src/app/ocp/(app)/{a-evaluar,en-evaluacion,muestras,compras}/page.tsx`.
- NO inventar en el mapa: Gestión de Muestras y Compras están en el rail y NO existen (sus páginas lo dicen).
(commit: cc8d39a)

## [2026-09-20] V5.64 · Kaffetal Regal — la barra del lote en dos líneas, la fase 5 y el mapa multiparcela
- Tanda de INTERFAZ del productor, seis instrucciones del owner en una sesión. Todo dentro de `kaffetal-regal`, salvo una
  migración de base y dos guardianes que se pusieron al día.
- CAMBIO DE VOCABULARIO (lo más importante para el mapa): la barra canónica del lote pasa de
  `FT · FT2 · EUDR · VID → EVA → MUE · SON → GAL → ARE` a DOS líneas:
    línea 1 (expediente):  `FT · FT2 · EUDR · FOTO → VISA`
    línea 2 (comercial):   `MUE → EVA → GRADO → CONT`
  **VISA** = el veredicto documental de CTCx (el chip que antes se llamaba EVA aquí). **EVA** = la EVALUACIÓN con el Q-Grader,
  que es lo que la palabra significa en todo el resto de la casa. `SON` desaparece (el Sondeo ya no es concepto), `GAL` → `GRADO`,
  `ARE` sale de la barra. Definición del owner, textual: MUE cierra cuando CTCx confirma muestra Y pago; en EVA el lote queda
  ABIERTO a recibir dos informes (rueda del café + ficha técnica) que entrega el Q-Grader por un login del «Centro de Calidad»;
  GRADO es el punto de equilibrio donde CTCx decide si va al catálogo; CONT es la oferta de temporada que el productor toma
  declarando su tamaño inicial, o cambia por CaaS · CTCx Selection, o no toma.
- Fichas que cambian: `lotkanbanstepper` (dos filas, nueve chips, chips-botón hacia Evaluaciones/Contratos);
  `circuitodellote` gana un LECTOR nuevo — `estadoDelCircuito()` ya no lo lee solo el OCP: lo importa el panel del productor,
  que es exactamente lo que la V5.62 dejó anotado que pasaría. `fichatecnica`: el paso 4 se llama FOTO y pide 2 fotos.
- Fase 5 del overhaul EJECUTADA (lado productor): B4 «Video del Café» → «Fotos y Video del Café», 2 fotos obligatorias, video
  opcional. Campo nuevo `datasheet.b4_files_foto` con default seguro `[]`.
- ARQUITECTURA · guard trigger nuevo: `guard_lot_fotos_intake` sobre `lots` (migración `lots_fotos_obligatorias`) — el TERCERO
  de esa tabla, junto a `guard_lots_producer_columns` y `guard_lot_protected_columns`; los tres corren y la regla efectiva es su
  intersección. Rechaza `borrador → ficha_completa` sin 2 fotos, y SOLO en la transición (un lote ya cerrado no se re-valida).
  Razón de que sea un trigger y no una Server Action: KR escribe directo contra Supabase con el JWT del productor — en este
  componente «el servidor» ES el trigger. Para la ficha DICT de guards: la tabla `lots` pasa a tener tres.
- B3 · Caracterización Física: el productor reporta UNO de factor/almendra y el otro se DERIVA (`factor × almendra = 17.500`,
  muestra de laboratorio de 250 g). Lo derivado NO se persiste — se calcula al leer, regla de la casa. La Densidad en Verde
  deja de ser obligatoria y baja al bloque opcional.
- CORREGIDO un dato falso de cara al productor: B3 decía «muestra de 205 g» con un rango que llega a 245 g (imposible). El repo
  ya usaba 250 en `computeFactor`. ⚠️ El mismo error sigue en `src/app/ocp/(app)/fichasActions.ts:108` — pendiente con dueño
  `consolas`, no se tocó código ajeno.
- Mapa EUDR (`FincaMapPicker`): todas las parcelas en UN mapa — la editable en oro, las hermanas fijas en gris con su nombre.
  Cada esquina del borrador es un marcador numerado y arrastrable (antes el primer clic no pintaba nada: el `<Polygon>` no tiene
  qué dibujar hasta el segundo punto, y parecía que el toque fallaba). Una sola puerta de entrada al dibujo; el clic suelto en el
  mapa ya no arranca un borrador invisible. La Parcela 1 gana nombre editable.
- Información General enseña el CORREO de la cuenta (de la sesión de Auth, no de `profiles`), en solo lectura.
- Guardianes: `qa-reportado-productor` 38 → 45 y `qa-evaluaciones` 40 → 46, reescritos a las reglas nuevas en vez de dejarlos
  rojos. `qa-evaluaciones` ahora EXIGE que la línea comercial salga de `estadoDelCircuito()`.
- ⚠️ Para el V46: la línea base real de `qa-kr-ficha` era 205, no 207 como decía el charter (ahora 206). El charter se corrigió.
- LECCIÓN: **un guardián que vigila una escalera de nombres es un guardián que hay que reescribir el día que el owner renombra.**
  Los dos rojos de esta tanda no eran regresiones: eran los guardianes haciendo su trabajo, diciendo «esto que cambiaste estaba
  protegido». El trabajo no era apagarlos, era moverlos a la regla nueva.
(commit: fcc8bed)

## [2026-09-20] V5.65 · El vocabulario EUDR asentado, el mapa rehecho y el proceso por variedad
- Segunda tanda del día en `kaffetal-regal`, sobre la retroalimentación del owner a la V5.64.
- VOCABULARIO (lo que el mapa interactivo tiene que reflejar en DICT y en toda ficha que los nombre):
    PASAPORTE = de la FINCA — su debida diligencia EUDR.
    VISA      = del LOTE — se hereda del Pasaporte; es el PRIMER entregable de CTCx y es GRATIS,
                porque la documentación EUDR no necesita al Q-Grader: llega ANTES de la EVA.
    EVA       = «Evaluación de Muestras en Origen» — las muestras van al Q-Grader y vuelven con
                granulometría y perfil sensorial → el Punto y la Tríada → el Grado de Calidad.
  Antes: la finca tenía «Visa», el lote «Sello», y el OCP llamaba «EVA» al veredicto documental.
  La definición quedó ESCRITA en la cabecera de `src/lib/eudr.ts` (no en una bitácora: el próximo
  barrido hace grep) y la vigila `qa-evaluaciones` por las dos caras.
- ⚠️ ANN / fichas a repuntar: `fincaEudrStatus` y `lotEudrStatus` cambian TODAS sus etiquetas
  («Visa vigente» → «Pasaporte vigente», «Sello listo» → «Visa lista»…). Las lee también el OCP.
- Fichas que cambian: `fichatecnica` (A5 pasa a «Pasaporte de la Finca y Visa del Lote»; B1 pierde el
  selector de Especie y gana proceso POR VARIEDAD); `fincamodal` (la pestaña 2 se rehízo); `eudr`.
- ARQUITECTURA · `CafetalEditor`: componente nuevo en `FincaModal.tsx` que montan TANTO la Parcela 1
  como las 2..N. Antes eran dos formularios distintos para el mismo objeto, y de ahí salía que uno
  pidiera una cosa y el otro otra. El estado sigue viviendo en quien lo monta (la 1 en el borrador de
  la finca, con su autosave y su espejo a la parcela 0; las 2..N en su fila, guardado inmediato).
- ARQUITECTURA · el orden del formulario cambió de sentido: la pregunta «¿mayor a 4 ha?» se DECLARA
  antes del mapa. Antes se DEDUCÍA del área, así que el mapa cambiaba de modo mientras el productor
  escribía. La respuesta se guarda en `finca_parcelas.requires_polygon` — columna que existía desde F1
  y que NADIE escribía; `null` = sin contestar y se resuelve por área (cero migración de datos).
- DDL: `finca_parcelas.altitude_masl` (migración `parcelas_altura_y_declaracion_area`), más los dos
  COMMENT que explican la semántica en la propia base.
- B1: `VarietyRow` gana `base`/`special` — el proceso es de cada variedad. `species`,
  `base_processing` y `special_processing` se siguen escribiendo como PROYECCIÓN de la variedad
  dominante porque los leen el OCP, el catálogo público, la ficha pública y el runner de Arena.
  Para el mapa: la fuente es la variedad; esas tres son copias de lectura, no verdades paralelas.
- La declaración final de la Ficha ya no promete la muestra de 2 kg: eso se dirá al confirmar muestras.
- Guardián: `qa-evaluaciones` 46 → 50, probado haciéndolo morder.
- LECCIÓN (la de la tanda): **el owner había dibujado el flujo entero y yo entregué mejoras sueltas
  dentro del flujo viejo** — contexto en el mapa, primer punto visible, arrastre. Todo cierto y nada
  de eso era lo pedido: lo pedido era el ORDEN (preguntar antes de mapear) y la UNIFICACIÓN (un solo
  editor de cafetal). Cuando hay un mockup, la unidad de trabajo es el flujo, no la lista de detalles
  que uno reconoce en él.
- ⚠️ PARA EL V46 / pendiente con dueño `consolas`: el OCP sigue diciendo «EVA» (columna de `/ocp/kr`,
  `EvaReviewCard`) por el veredicto documental, y «Visa EUDR» por lo de la finca. Brief escrito:
  `docs/componentes/briefs/consolas-simplificar-ocp-al-circuito.md`.
(commit: 0ba60b1)

## [2026-09-21] V5.66 · Herramientas del Café — una carpeta por herramienta y el recuento
- ARQUITECTURA · `public/tools/<id>/<archivo>.html`: cada herramienta del repo en su carpeta, con el
  nombre de su `tools.id`. Fuente única `src/lib/tools/carpetas.ts` (pura, sin imports), que lee
  `next.config.ts` → `redirects()`: 16 × 308 desde las URLs planas. Lo compartido se queda en la raíz
  (`ctc-bridge.js`, `assets/`); `h` está reservado para `/tools/h/[slug]`. El proxy no cambia: `/tools/`
  sigue fuera de su matcher y `redirects()` corre antes que el sistema de archivos en los 19 hosts.
- Nodo / ficha `herramientas`: el mapa debe enseñar la carpeta por herramienta y el mapa de
  INTERFACES (herramienta → quién la consume): Rueda del Café → Sneak Peek (hoy, con la V10) y Centro
  de Calidad (previsto); costo-empaque → cotizador de empaque (embebida tal cual); mermas-ctc y
  cogs-verde → cotizadores de lotes y logístico (COPIAS en `public/ocp-apps/`, deuda anotada); cool-pdf
  → CoverFlow y Coffeed Redacción (mecánica copiada).
- DATOS: `tool_versions.src_publico` reapuntado tras el despliegue.
- Guardián nuevo `qa-tools-carpetas` (166); `qa-tools-seo-check` 193 → 257 (recorre carpetas).
- Docs: `docs/componentes/herramientas-cafe/README.md` (recuento) + `<id>/README.md` por herramienta +
  `_candidatas/atlas-cafetero`. Fuentes del owner en `reference/html_tools/<id>/`.
(commit: 3467576)
