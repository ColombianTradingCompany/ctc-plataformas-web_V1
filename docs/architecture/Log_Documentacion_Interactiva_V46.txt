Bitácora de cambios PENDIENTES sobre Documentacion_Interactiva_V46.0(35ebddd).html
(CTC Web Platform, desde 2026-09-23 · plataforma V5.70). Cada entrada se compila
al próximo Version Wrap (V47).

Formato:
## [YYYY-MM-DD] V5.NN · Título corto
- Qué cambió, concreto (archivo/tabla/flujo)
- Si es escenario nuevo, cambio de nodo, entrada de DICT o cambio de arquitectura
(commit: <sha corto o "pendiente">)

════════════════════════════════════════════════════════════════════════════════
QUIÉN ESCRIBE AQUÍ Y QUIÉN LLAMA AL WRAP (desde 2026-09-11, ALINEACION §5)
════════════════════════════════════════════════════════════════════════════════

· EL ASIENTO lo escribe LA SESIÓN DEL COMPONENTE que despliega, en el MISMO commit
  que sube APP_VERSION — lo exige `scripts/qa-arqlog-check.mjs` (toda versión del
  CHANGELOG posterior a «plataforma V5.70» tiene que aparecer aquí). Se añade al
  final; nunca se toca un asiento ajeno. `git pull` antes de escribir.
· EL WRAP lo llama SOLO la conversación «WRAP-COMMIT-PUSH (CTC Platforms)» (grupo CTC
  Consolas internas, vía `plataforma`; existe desde el 2026-09-19 — antes la regla
  nombraba una «Wraps del mapa» que nunca existió). Es el NODO FINAL: antes de compilar
  audita que el maestro y los charters estén en el mismo punto (ALINEACION §5.3). Cadencia: ≥ 5 asientos, un hito de componente cerrado
  o antes de una versión mayor — lo primero que ocurra.
· La línea «plataforma V5.70» de la cabecera es la referencia del guardián: el wrap
  siguiente la reescribe con la versión sobre la que cierra.

════════════════════════════════════════════════════════════════════════════════
CÓMO SE VALIDA UN WRAP — la lista que de verdad hay que correr
════════════════════════════════════════════════════════════════════════════════

✅ LA BATERÍA ES UN ARCHIVO: `docs/architecture/validate_snapshot.mjs`. Corre en
NODE, no en la consola del navegador. Uso, desde `ctc-platform/`:

    node docs/architecture/validate_snapshot.mjs docs/architecture/<nuevo>.html --prev docs/architecture/<anterior>.html [--dump]

`--prev` imprime los contadores del snapshot anterior al lado de los del nuevo;
`--dump` lista nodos, claves de DICT, ids de escenarios y CTX. Correrlo también
contra el snapshot ANTERIOR como control: si pasa en los dos, no es permisivo.

Las NUEVE comprobaciones, y las nueve tienen que volver VACÍAS:
  1. pasos de traza (`q.n` y `q.from`) contra los `.node[id]` reales
  2. `DICT[k].a[]` contra las claves de DICT
  3. `WIRES` contra los nodos reales
  4. `CTX` — nodo real Y claves reales
  5. `ANN` huérfanas: rutas que ya no existen en el FILETREE recién regenerado
  6. `ANN[...].info` contra las claves de DICT
  7. `.ibtn[data-info]` del marcado contra DICT
  8. `.chip[data-info]` — SOLO sobre el marcado ANTERIOR a `<script>`
  9. claves REPETIDAS en el texto de DICT/CTX (JS se queda con la última en silencio)

Y los contadores tienen que subir EXACTAMENTE lo esperado
(V32: 95→99 · V33: 99→102 · V34: 102→104 · V35: 104→107 · V36: 107→113 ·
V37: 113→121 · V38: 121→128 · V39: 128→133 · V40: 133→136 · V41: 136→137 ·
V42: 137→139 · V43: 139→140 · V44: 140→146 · V45: 146→148 · V46: 148→155).

Además, después de `gen_filemap_tree.mjs`, un `node --check` del bloque `<script>`
extraído (el validador evalúa los literales, no el resto del motor).

⚠️ EL MAPA NODO→FICHAS SE LLAMA `CTX`, NO `NODEDICT` (V34). ⚠️ EL SELECTOR DE
TRAZAS VA POR ÍNDICE (V33): la comprobación buena es
`select.options.length === SCENARIOS.length`. ⚠️ EL DICT TIENE DOS ESTILOS DE
ESCRITURA (V31): contar con `Object.keys(DICT).length` evaluado, nunca por regex —
y OJO al anclar un parche: las entradas viejas van `clave: {` (con espacio) y las
nuevas `clave:{`. ⚠️ UN BACKTICK EN UN CUERPO DE DICT ROMPE EL LITERAL ENTERO
(V38): siempre `<code>…</code>`. ⚠️ EL SHA DEL NOMBRE ES EL DEL CONTENIDO (V35).
⚠️ LA CADENA «Última actualización» DEBE NOMBRAR AL PROPIO SNAPSHOT (V38 olvidó la
suya). ⚠️ UN PARCHE DE WRAP CAMBIA SUS PROPIAS ANCLAS (V40): cada replace lleva
assert.

════════════════════════════════════════════════════════════════════════════════
LO QUE EL WRAP V46 ENSEÑÓ (léase antes del V47)
════════════════════════════════════════════════════════════════════════════════

· SE COMPILARON DOCE ASIENTOS DE CUATRO SESIONES (V5.59–V5.70): cinco del nodo final (el overhaul, fases 1–4a y el rail
  del OCP), dos de `kaffetal-regal`, dos de `herramientas-cafe`, tres de `herramientas-internas`. Es el primer wrap que
  compila trabajo de sesiones que no son la que lo llama, y salió: cada asiento traía su lista de ANN a repuntar y sus
  fichas candidatas. Lo que un asiento NO trae, el wrap lo saca del código, no de la memoria — las 118 tablas se contaron
  por SQL (la ficha decía 58 y la tarjeta 76).

· EL WRAP SE LLAMÓ CON OTRA SESIÓN TRABAJANDO EN EL ÁRBOL, y no pasa nada: compila los asientos COMMITEADOS. Los archivos
  sin commit de la otra sesión no están en el log y no entran; el commit del wrap es solo de docs, con rutas explícitas; el
  sha del nombre es el HEAD del momento. Lo único que cambia es que el árbol de archivos (`gen_filemap_tree`) ve un
  archivo suelto que todavía no está en git: se anota y ya.

· LA AUDITORÍA DEL NODO FINAL ENCONTRÓ UN DESFASE Y LO CERRÓ ANTES DE COMPILAR: `ALINEACION` §3 decía que `consolas`
  «le debe la otra mitad» del vocabulario EUDR (V5.65), y el charter `consolas` no lo recogía, ni el dato de los 205 g
  (V5.64), ni el brief `consolas-simplificar-ocp-al-circuito.md`. Un asiento que le deja algo a OTRO componente tiene que
  aterrizar en «Pendientes» de ese charter; si la sesión que lo escribe no puede tocar el charter ajeno, lo hace el wrap.

· LAS ANN HUÉRFANAS SALEN SOLAS: se copia el base, se regenera el FILETREE, se corre la batería, y la comprobación 5 da la
  lista exacta (22 esta vez). Repuntar es renombrar la clave y decir la verdad nueva en `p:`; nunca borrar la anotación.
  Y se repunta ANTES de escribir las ANN nuevas, para que el guardián de repetidas no se confunda.

· LOS AYUDANTES DEL V45 VALEN TAL CUAL (`add_par`, `add_a`, `append_note`, `entrada`) y ganaron tres: `set_head` (kicker y
  título de una ficha — hacía falta porque media docena de fichas decían la consola vieja en el TÍTULO), `prepend_sub` (el
  estado actual delante del `sub` viejo, que queda como historia, como ya hacía la tarjeta del BCP) y `repoint` (una ANN).
  Todas con assert de «exactamente una vez». Las anclas se comprueban ANTES de correr el parche, en una pasada aparte.

· UN NODO NUEVO SON CINCO COSAS: la tarjeta (con `data-info` a una ficha que exista), sus cables, su CTX, su ficha y sus
  ANN. La LCP y el Courier entraron así. Y la lección de la V5.63 vale también aquí: derivar la lista no basta.

· SE VALIDÓ con la batería (9/9 vacías, contadores exactos contra el V45), `node --check` de los bloques `<script>` y en
  el navegador con `docs-architecture` (puerto 4300): las trazas nuevas en el desplegable, cero referencias colgantes
  recalculadas en vivo, cero errores de consola.

════════════════════════════════════════════════════════════════════════════════

Contexto abierto al cerrar V46 (2026-09-23) — plataforma V5.70:

- **Lo que V46 compiló**: V5.59–V5.70. 2 nodos nuevos (`n-lcp`, `n-courier`) · 7 fichas nuevas (`lcp`, `tablerodeejecucion`,
  `tablaunica`, `circuitodellote`, `courier`, `espejoamano`, `derivadonosepersiste`) · 3 trazas nuevas (el Tablero, el
  circuito, el courier) · ~45 fichas actualizadas, 8 de ellas retituladas · 10 cables nuevos y 3 recableados (Directorio,
  el taller y el consumo salen del BCP) · 2 entradas CTX nuevas y 6 ampliadas · 22 ANN repuntadas, 32 nuevas y una ampliada · el badge de
  Postgres a 118 tablas. 42 nodos · 155 fichas · 62 trazas · 105 wires · 38 CTX · 421 ANN.
- **Deuda de compilación**: ninguna. Las fichas viejas conservan su cuerpo original debajo del párrafo «V46 ·…»; la del
  `eva` explica el cambio de objeto de la palabra en vez de reescribir la historia.
- **Lo que viene, y ya tiene orden**: la fase 4b del overhaul, PARADA esperando tres respuestas del owner (recuadro de la
  fase 4 en `docs/OVERHAUL_CONSOLAS_PLAN.md`); la fase 5 (exige las cuentas `prueba-*`); los seis briefs de la fase 6 y el
  del OCP al circuito, en scoping. El próximo wrap: al cerrar la fase 5, o a los cinco asientos — lo primero.
- **Lo que el mapa TODAVÍA no refleja, y es correcto** (no inventarlo): las pantallas del circuito de la 4b (Lotes a
  Evaluar y en Evaluación EXISTEN como vistas de Nominados; lo demás no); Gestión de Muestras y Compras (en el rail, sin
  módulo); los modelos de Producción y Logística; las plataformas de pago; el rename EVA → Visa en el OCP.
- **Guardianes sin correr**: `qa-guard-check` y `qa-checkout-check` (piden las cuentas `prueba-*`). Y DOS que NO van en
  ninguna batería porque gastan dinero: `qa-cromatografia-modelo` y `qa-transcripciones-nube`.
- **Línea base de este wrap** (solo docs): validate_snapshot 9/9 vacías, con el V45 como control · `node --check` ·
  navegador sin errores · qa-changelog · qa-arqlog · qa-encoding · qa-docs.

════════════════════════════════════════════════════════════════════════════════

## [2026-09-23] V5.71 · Herramientas Internas — Cotizador Courier: borrar semanas del recargo
- Acción `borrarCombustible(id)` (emite) en `src/lib/courier/actions.ts`; lista «Semanas anotadas» con «Borrar» en `CourierBoard.tsx`.
- Datos: borrada la fila de prueba del 40 % (16–30 sep, a mano) por orden del owner.
(commit: 7fab5b6)

## [2026-09-23] V5.72 · Herramientas Internas — Cotizador Courier: borrar cotizaciones guardadas
- Acción `borrarCotizacionCourier(id)` (emite) en `src/lib/courier/actions.ts`; botón «Borrar» junto a «Abrir» en `CourierBoard.tsx`.
(commit: 8f6e969)

## [2026-09-23] V5.73 · Herramientas Internas — Cotizador Courier ↔ LCP · CRM CP CaaS (cierre del ciclo 1)
- DATOS: `courier_cotizaciones.lead_id` → `leads(id)` (`on delete set null`), índice parcial. Un item CaaS = un lead del pilar
  `cocreate` (lo valida `leerItemCaas`); un item tiene 0..N cotizaciones courier.
- INTERFAZ NUEVA entre consolas: LCP · CRM CP CaaS (`LeadsBoard.tsx`, tarjeta `cocreate`) LEE `courier_cotizaciones` y enlaza al
  ECP con `/ecp/cotizador-courier?lead=<id>` (nueva, vinculada) y `?abrir=<id>`; el cotizador enlaza de vuelta a
  `/lcp/crm/caas#lead-<id>` (la tarjeta se abre sola por el ancla). ⚠️ quien tenga grant de LCP y no de ECP ve las cotizaciones
  pero no puede abrir el cotizador.
- Acciones nuevas: `itemCaasCourier` (lectura), `editarNotaCotizacionCourier` (borrador; lista blanca 16).
(commit: aa681e4)

## ═══ PARA EL PRÓXIMO WRAP (V47): el Cotizador Courier, ciclo 1 completo (V5.68 → V5.73) ═══
El owner cerró el primer ciclo el 2026-09-23 y pidió que el mapa lo recoja entero. Lo que el wrap debe dibujar:
- NODO NUEVO «Cotizador Courier · FedEx» en ECP · Modelo Logístico (charter `herramientas-internas`, fila 5·a), junto al
  Cotizador logístico: `/ecp/cotizador-courier` · `src/lib/courier/{calculo,combustible,eia,actions,types}.ts` ·
  `src/components/cotizador/{CourierBoard.tsx,courier.module.css}`.
- DATOS (9 tablas `courier_*`, todas service-role-only): acuerdos → descuentos · descuento_adquirido · bonificaciones; tarifas_base ·
  zonas (guía pública, versionadas por `vigente_desde`); recargos + combustible_escalas; cotizaciones (acta inmutable por guard
  trigger, con `lead_id`).
- FUENTES EXTERNAS nuevas del mapa: la guía pública de FedEx Colombia 2026 (cargada una vez por `scripts/seed-courier.mjs` desde
  `apps-internas/courier-fedex/`, fuera del repo) y la **EIA** (serie EER_EPJK_PF4_RGC_DPG), que lee el **cron de los jueves**
  `/api/cron/courier-combustible` (tercer cron de `vercel.json`). fedex.com NO es consultable desde el servidor (Akamai).
- INTERFACES: ECP Cotizador Courier ↔ LCP CRM CP CaaS (arriba). Previstas, SIN construir: la modalidad «Courrier (DHL/FedEx)» del
  cotizador logístico y la Gestión de Muestras del OCP leyendo este costo.
- REGLA para el DICT y las fichas: el acuerdo con FedEx es **CONFIDENCIAL** (cláusula 6) y el repo es público → el mapa describe
  la FORMA del cálculo (lista − zona − adquirido − bonificación → mínimo → + combustible) y **nunca una cifra, número de acuerdo
  ni de cuenta**. `qa-courier-check` (51) vigila la fuga por hash.
- ABIERTO (charter, «Pendientes»): confirmar con FedEx la suma de descuentos y si la caja 10/25 kg lleva descuento; contrastar
  envíos reales; fin de la gracia el 2026-11-24 (escalón por gasto anualizado); segunda tanda = importación, terceros, API de FedEx.

## [2026-09-23] V5.74 · Consolas — el OCP dice Pasaporte · Visa · EVA (cierre de lo que le debían V5.64–V5.65)
- Vocabulario, sin regla nueva: en `/ocp/kr` la columna «EVA» → «Visa» (veredicto documental del lote) y «Visa EUDR» (finca) →
  «Pasaporte EUDR»; `EvaReviewCard` «Veredicto EVA» → «Veredicto de Visa», panel EUDR «Pasaporte de la finca»; `FincaSeccion`
  «Esperando Visa», «Ver Pasaporte EUDR (dossier)»; `FincaEudrEditor` «Pasaporte EUDR de la finca (asistencia CTCx)»; los
  mensajes de `markLotApto`, `registerLotDds`, `setEvaChecklistItem`, `markLotNoApto` ya no dicen «Sello».
- DICT: la ficha `visa`/`eudr` del mapa debe decir que desde la V5.74 el OCP y KR nombran igual los tres objetos (cabecera de
  `src/lib/eudr.ts`). Los identificadores `EvaReviewCard` · `EVA_CHECKLIST_ITEMS` · `lots.eva_checklist` conservan el nombre
  viejo con nota en el archivo.
- Corregido el dato «205 g» → 250 g en la instrucción del escáner (`fichasActions.ts`).
- Guardián: `qa-evaluaciones-check` 50 → 53 (la cara del OCP).
- ANN: el Mapa de Trabajo (`src/lib/workmap/schema.ts`) sigue con la barra anterior a la V5.64 — se redibuja con la 4b.
(commit: 5ef97d6)

## [2026-09-23] V5.75 · Consolas — Asistencia a Proveedores · Proveedor Desacoplado (las tres rutas del proveedor, 1.ª tanda)
- ORIGEN: tres diagramas del owner (Ruta Estándar · Ruta CTCx Selection · Ruta Desacoplado), brief
  `docs/componentes/briefs/consolas-rutas-del-proveedor.md` con sus siete respuestas. Autorizó tocar IDENTIDAD (§1): la sesión
  asistida y la cuenta sin buzón.
- NODOS NUEVOS en OCP · Kaffetal Regal: «Asistencia a Proveedores» (`/ocp/asistencia`) y «Proveedor Desacoplado»
  (`/ocp/desacoplado`), un módulo `src/lib/asistencia/{desacoplado,actions}.ts` + `SesionAsistidaBoton.tsx`.
- FLUJO NUEVO «sesión asistida»: OCP → `auth.admin.generateLink(magiclink)` → `verifyOtp` en la cookie COMPARTIDA (`sb-…`) →
  KR se abre como el productor en otra pestaña; la cookie del panel (`ctc-panel-auth`) no se toca. Solo `role = producer`;
  rastro en `audit_log` (`assisted_session_opened/closed`) + nota «Asistencia CTCx» en `producer_comm_log`. Cerrar = `scope: local`.
- FLUJO NUEVO «desacoplado»: `auth.admin.createUser` con etiqueta `desacoplado-<slug>@ctcexport.com` (sin buzón, contraseña
  aleatoria, `email_confirm`) → `handle_new_user` → `producer_profiles.gestion = desacoplado`; ENTREGA = `updateUserById(email
  real)` + `profiles.email` + `gestion = entregado` + vale de «Recuperar acceso» por correo. `leadEmails.send` NO manda nada a una
  etiqueta (catch-all → Buzón).
- DATOS: migración `producer_profiles_gestion_desacoplado` (`gestion` · `gestion_desde` · `entregado_at`; guard extendido).
  Acta: `docs/migraciones/2026-09-23_producer_profiles_gestion_desacoplado.sql` (carpeta nueva: el acta de cada DDL).
- «CTCx asume el costo» (`asumirEvaluacion`, nominadosActions): inscripción `exento` con `payment_ref = Asumida por CTCx`.
- DICT: `sesionasistida`, `desacoplado`, `gestion`. Ficha `identidad`: dos excepciones autorizadas (2026-09-23). ANN en la
  tabla única (`gestion` bajo el nombre) y en ProducerPanel (insignia + botón).
- Guardianes: `qa-asistencia-check` (57, nuevo) · `qa-rutas-consolas` 500 (MODULOS_LIB). `createLot` ya no lanza.
- ABIERTO: nadie condujo la sesión asistida en vivo (sin `prueba-*`); la 4b (el owner la revisa en el paso siguiente); Ficha
  retenida y descargable (KR); perfil único de CTCx Selection con imagen por lote (Compras); llamada de bienvenida (Tablero).
(commit: f4f7c66)

## [2026-09-24] V5.76 · Consolas — /ocp/kr según el owner; fase 0 del PLAN_CIRCUITO_DEL_LOTE (borrado del juego de prueba)
- PLAN NUEVO para el mapa: `docs/PLAN_CIRCUITO_DEL_LOTE.md` (Etapa 1) sustituye a las fases 4b/5/6 del overhaul; las ocho decisiones
  del owner están en su §6. Cambios de arquitectura DECIDIDOS y sin ejecutar: la Arena se queda en BCP como «sesiones de segunda
  apreciación» (cada apreciación es una `lot_evaluations` más; el grado lo rige UNA, por defecto la inicial → `officialAverages`
  deja de promediar); Ofertas CP Aceptadas + Oferta desde CTCx Selection = staging del Catálogo Activo; Tyrian sigue a subasta
  (MOQ 100 kg CPS); mínimos declarados por lote 6 cargas (Black/Red) · 3 (Blue) · 200 kg (Gold); tarifa $200.000 con Campañas de
  Subvención (30–70 %) en OCP · Stock Físico; Q-Grader = credencial `centro-calidad` con módulos activables (Evaluación · Procesamiento)
  y uso directo; ruptura nunca automática, visible automáticamente.
- `/ocp/kr`: sin `createLot` (retirado); agrupada por productor; ELEMENTO principal (lotes · fincas) que también decide los pines del
  mapa; filtro de Pasaporte por etapa (`KrFila.pasaporte` = `fincaEudrStatus().code`); `?elemento=&pasaporte=`; el KPI «Fincas
  pendientes» del Panel enlaza `?elemento=fincas&pasaporte=en_revision`.
- DATOS: borrados contrato `8618ecda` (+3 liberaciones), oferta, listado y lote `f5187234` con sus dependientes; queda el video
  huérfano en el bucket (Storage API). La base queda SIN contratos, ofertas ni listados: el circuito nuevo arranca en cero.
(commit: 4f3490c)

## [2026-09-24] V5.77 · Consolas — fase 1 del PLAN_CIRCUITO_DEL_LOTE: la Arena rehecha, el Club fuera, Campañas de Subvención
- NODO «Kaffetal Regal Arena» (BCP) CAMBIA DE FUNCIÓN: sesiones de SEGUNDA APRECIACIÓN. `arena_sessions.name`; roster en
  `arena_session_lots` (solo galardonados); `registrarApreciacion` → `lot_evaluations` (bcp_arena, aceptada, `rige_grado=false`);
  `elegirEvaluacionQueRige` → única reescritura de `lots.grade` fuera del veredicto. RETIRADO: runner de jornada, jueces,
  descartes, ganador, `cup_registrations`, `revealSessionIdentities`, `jornada.ts`, la vitrina (invitar · bloquear en sesión),
  KPI y tareas «fila para Arena». Fases `arena·sesion·competido` sin escritor; `arena_scores` dormida.
- REGLA NUEVA (DICT `gradoquerige`): el oficial de un lote sale de LA evaluación que rige (`rige_grado`; por defecto la inicial
  del Q-Grader), no del promedio. `src/lib/evaluations.ts` → `evaluacionQueRige()`; `officialAverages` conserva nombre y forma.
- NODO «Kaffetal Club» RETIRADO del rail y como gate (`signContract`, `publishLot`, `grantClubMembershipOnce`,
  `revokeClubMembership`, `clubEmails.ts`); `club_member_since` dormida. NODO NUEVO «Campañas de Subvención» en OCP · Stock
  Físico (`/ocp/subvenciones`, `subvencionesActions.ts`: 30–70 % sobre la tarifa plana de $200.000; tablas `club_campaigns` y
  `arena_entry_codes` conservan nombre). 308: `/bcp/club` → `/ocp/subvenciones`; `/ocp/club` reapuntada.
- Los reclamos de oficialización (`producer_claim` pendientes) se revisan en `/ocp/kr?lote=` (`LoteSeccion`); `reviewEvaluationClaim`
  pasa a ser solo del OCP. Panel del OCP: KPI «Solicitudes por resolver» y «Reclamos por revisar».
- DATOS: migración `arena_apreciaciones` (acta en `docs/migraciones/`). `ATRIBUTOS_SCA` es la única lista de atributos SCA.
- Guardianes: `qa-jornada-check` retirado; `qa-evaluaciones` 51 (sin Club; la Arena no toca el circuito); `qa-rutas-consolas` 505.
- DIFERIDO de la fase 1: temporadas siguen en `/bcp/arena/temporadas`; `src/lib/arena/` no se renombra; cashback del 80 % y
  `RELEASE_STAIRCASE` en la fase 5; copy de KR (Arena, Club) es de `kaffetal-regal`.
(commit: 1c80795)

## [2026-09-24] V5.78 · Consolas — fase 2 del PLAN_CIRCUITO_DEL_LOTE: el registro (certificaciones con estado, chequeo EUDR, FT2 en el lote)
- REGLA NUEVA (DICT `certificacionconestado`): `finca_certificates.status` declarada → evidencia_pedida → corroborada | retirada.
  Pedir evidencia arranca un reloj: recordatorio SEMANAL al productor (correo por el remitente único + nota en su feed), máximo
  CUATRO; a la quinta semana la certificación se retira del Pasaporte (el registro queda). Fuente pura `src/lib/registro/reglas.ts`
  (`decidirRecordatorio`); servidor `src/lib/registro/certificados.ts` (pedirEvidencia · recordar · retirar · corroborar · reabrir,
  cada uno con audit_log + producer_comm_log); acciones `certificadosActions.ts` (OCP · emite).
- CRON NUEVO (4.º de `vercel.json`, permea a `plataforma`): `/api/cron/recordatorios`, lunes 12:00 UTC, `CRON_SECRET`.
- NODO finca: «Chequeo contra bases EUDR oficiales» = `fincas.eudr_chequeo_notas` + `eudr_chequeo_files` (jsonb lista), en la
  pestaña «Análisis y Evidencia» del editor de asistencia; solo CTC (guard).
- NODO lote: la vista completa (`/ocp/kr?lote=`) monta el set de Fichas (`LotFichasCard`) — escáner IA, compilar reporte,
  **transcribir a mano** (`crearFichaManual`, `source = "ctc"`), oficial ★. `/ocp/fichas` = índice. `src/lib/fichas/soportes.ts`
  única fuente de «qué material tiene el lote».
- DATOS: migración `registro_chequeo_y_certificados` (acta en `docs/migraciones/`); HANDOFF: fila nueva del guard de certificados.
- Guardianes: `qa-registro-check` (50, nuevo).
- PENDIENTE con dueño `kaffetal-regal`: A5 último paso del intake; dossier ES/EN por lote (+ el de la finca); Ficha descargable;
  enseñar el estado de cada certificación al productor (y sus «retiradas» fuera del Pasaporte impreso).
(commit: a5a7f1c)

## [2026-09-24] V5.79 · Consolas → Kaffetal Regal — la fase 2 cierra por el lado del productor (A5 último, estado de certificaciones, dossier ES/EN)
- CÓDIGO AJENO con el sí del owner y línea en §3: `FichaView`, `FichaNav`, `LotKanbanStepper`, `FincaModal`, `EudrDossierDoc`,
  `EvaluacionesTab`, `KaffetalExperience`, `data.ts` (kaffetal-regal); `etapas.ts`, `dossierExtras.ts` (consolas/lib).
- ORDEN DEL INTAKE (DICT `intake`): FT · FT2 · **FOTO · EUDR** → VISA. A5 es el último paso (folio 7). `intake_step` 2 = fotos
  pendientes, 3 = EUDR pendiente. Datos: un borrador en 3 pasó a 2.
- NODO NUEVO (documento) «Dossier del lote» `/kaffetal-regal/dossier/[id]?lang=es|en` (`LotDossierDoc`, presentacional; la ruta
  resuelve y guarda la puerta: dueño del lote). Reúne FT (identidad/trazabilidad), Pasaporte/Visa + DDS, caracterización (ficha
  oficial ★ + evaluación que rige) y certificaciones corroboradas. Sustituye, de cara al productor, a los documentos sueltos.
- ANN: el productor ve el estado de cada certificación (`FincaModal`); el Pasaporte impreso filtra `retirada`.
- Guardianes: `qa-registro-check` 50 → 67 (el lado KR); `qa-evaluaciones` §6 con el orden nuevo.
(commit: pendiente)
