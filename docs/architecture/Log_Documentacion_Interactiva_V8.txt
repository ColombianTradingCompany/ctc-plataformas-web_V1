Log_Documentacion_Interactiva_V8
Status: PENDING -- not yet wrapped. Only .0 (major) versions are ever compiled
into a real HTML file; this log is where every change lives, in plain text,
until a Version Wrap is actually invoked and produces Documentacion_Interactiva_V9.0(<sha>).html.
Covers: everything since Documentacion_Interactiva_V8.0(6c8b062).html, which is
now the current real HTML snapshot (Log_Documentacion_Interactiva_V7.txt is
closed and left as historical record of what went into producing it).

## [2026-07-15] Fix: OTP precedence — user's inbox wins over BCP_OTP_RECIPIENT_EMAIL
- Production bug reported by the owner: a collaborator's login OTP arrived at
  ctcexportmain@gmail.com instead of their own inbox. Cause: sendOtpEmail computed
  `to = BCP_OTP_RECIPIENT_EMAIL || recipient || founder` and that env var is still set
  (Vercel AND .env.local) from the original fixed-address era, so it silently outranked
  every user's delivery_email. Fixed by flipping precedence:
  `to = recipient || BCP_OTP_RECIPIENT_EMAIL || founder` — the env var is now strictly
  a fallback for recipient-less calls, never an override (src/lib/bcp/sendOtpEmail.ts).
- Doc impact when wrapped: one sentence in the "masterlogin" DICT entry (the env var is
  fallback-only) — no node/wire/scenario changes.
(commit: pending)

## [2026-07-16] Partner subdomains finalized + Red de socios in the CTC Home footer
- 5th public subdomain renamed: ctc-content.ctcexport.com (proxy.ts maps it to the unchanged
  internal route /socios/estudio-contenido). Final set: centro-calidad, agente-carga,
  agente-nacionalizacion, master-roaster, ctc-content.
- CTC Home Footer gains a "Red de socios" nav row linking the 5 partner landings
  (prod = subdomain URLs, dev = /socios paths via compile-time NODE_ENV, QuickNav pattern).
- Doc impact when wrapped: none structural (footer link row; subdomain label in DICT/ANN texts).
(commit: pending)

## [2026-07-16] Buzón becomes a mail CLIENT: reply/forward, tags, archive/delete w/ IMAP reflection
- Migration buzon_mail_client: inbound_emails gains tags text[], status inbox/archived/deleted,
  replied_at; new buzon_outbound (service-role-only) records every reply/forward sent.
- src/lib/buzon/mailClient.ts: sendBuzonMail (Resend sends AS the collaborator's own
  @ctcexport.com label — domain verified — else info@; threading via In-Reply-To; an RFC822
  copy APPENDed to Hostinger INBOX.Sent so webmail reflects platform sends);
  moveRemoteMessage (search by message-id → messageMove to INBOX.Archive [created on first
  use — Hostinger has no \Archive] or INBOX.Trash), all best-effort against the mailbox.
- buzonActions rewritten: sendBuzonReply / setBuzonStatus / setBuzonTags /
  getBuzonAttachmentUrls (signed URLs on demand) / markInboundEmailRead — every action
  permission-scoped: owners touch any mail, a collaborator only mail addressed to their label.
- /bcp/buzon UI (BuzonMail.tsx): tabs Recibidos/Archivados/Papelera, expand→auto-mark-read,
  inline composer (reply prefills sender/Re:, original quoted below), tag chips + editor,
  archive/delete buttons ("también en Hostinger").
- Verified live: folder discovery (INBOX.Sent \Sent, INBOX.Trash \Trash, no \Archive);
  append-to-Sent round-trip (self-cleaned); search-by-real-message-id = 1 hit; tsc+eslint
  clean; page compiles + 307 unauth redirect. Authenticated send/archive flows: owner's
  first real use (Resend path identical to proven leadEmails wiring).
- Doc impact when wrapped: extend "buzon" DICT (mail client: outbound via n-third, remote
  reflection) + possible wire n-inbound ↔ n-third both ways.
(commit: pending)

## [2026-07-16] Buzón: full-history backfill + per-recipient inboxes (commit 90b948c)
- syncBuzon now scans ALL envelopes and imports missing messages in 25-msg batches
  (returns `remaining`; the Sincronizar button loops until 0) — whole info@ history
  importable. /bcp/buzon is identity-aware: owners see everything; a collaborator sees
  only mail whose To: contains their own @ctcexport.com label (catch-all preserves To:).
- Pending user wishes (feasible, not yet built): reply/forward via Resend as info@ (+
  IMAP APPEND to Sent), manual tags, archive/delete reflected to Hostinger via IMAP.
- gvg@ bounce recurred: Hostinger catch-all shows Activo but their LMTP still rejects —
  propagation issue on their side, not code; retest / support ticket if it persists.
(commit: 90b948c)

## [2026-07-16] Buzón goes operative via IMAP pull (option A+pull, $0)
- Decision trail: Resend Inbound requires Pro ($20/mo, 2nd domain); Hostinger free tier
  (1 mailbox info@, catch-all included) chosen instead. Catch-all *@ctcexport.com → info@
  configured by owner; BUZON_IMAP_{HOST,USER,PASSWORD} env vars in Vercel + .env.local.
- New src/lib/buzon/syncBuzon.ts (imapflow + mailparser): pulls info@ over IMAP into
  inbound_emails — dedupe by message-id (new partial unique index
  inbound_emails_message_id_uidx), attachments uploaded to Storage (kaffetal-media/buzon/...),
  non-destructive reads (no \Seen), bounded (7-day window / 50 msgs per run), and
  retention cleanup: archived mail >30 days old is deleted from Hostinger (never-archived
  history untouched) so the free 1 GB mailbox acts as a buffer, Supabase as the archive.
- /bcp/buzon gains "Sincronizar buzón" (BuzonSyncBar → syncBuzonNow server action,
  requireActiveAdmin); page copy updated. Resend webhook stays deployed but dormant
  (future real-time upgrade path; both routes feed the same table).
- Verified live: IMAP connectivity against the real mailbox (304 msgs, 18 in 7 days);
  pipeline test archived 2 real messages (SCA welcome + Hostinger forwarder confirmation)
  with fields parsed correctly; tsc+eslint clean; /bcp/buzon compiles + unauth-redirects.
  Docs: INBOUND_EMAIL_SETUP.md rewritten around the IMAP path.
- Doc impact when wrapped: update "buzon" DICT entry (pipeline is now catch-all → info@ →
  IMAP sync; webhook = dormant upgrade) and the n-inbound node text; the "correo" scenario's
  first two steps change accordingly.
(commit: pending)

## [2026-07-15] Partner tier essentials: 5 "couples" (/socios), partner_accounts, /bcp/socios, subdomains
- OCP scaffold: added the missing 5th mirror (Estudio de Contenido, the transversal partner).
- Migrations: user_role gains 'partner'; new partner_accounts (service-role-only: node_type
  check on the 5 slugs, org/contact, invited/active/suspended, invite-email tracking).
- The 5 partner couples at /socios/[partner] (dynamic route, config-driven from
  src/lib/partners/partners.ts — copy/colors/screens/sello verbatim from the v3 board;
  logos extracted from the board's base64 into public/images/socios/): public landing +
  /acceso login (single-factor, POST /api/socios/auth/login: ephemeral verify + role
  'partner' + partner_accounts row for EXACTLY that node; invited→active + last_login_at
  on success) + /panel scaffold (requirePartner gate; v3 screens as cards; change-password
  card; logout). Wrong-node credential rejected with the generic error.
- BCP /bcp/socios (owner-only, sidebar under Administración): emit credential (email+org+
  contact+node; temp password via Resend, partnerEmails.ts; same "public email can't be
  converted" rule), suspend/reactivate (revocable en un clic), resend/reset. audit_log
  entity_type 'partner_account'.
- proxy.ts: 5 new subdomain→route mappings; external DNS/Vercel steps in
  docs/PARTNER_DOMAINS_SETUP.md (user does the clicks).
- Doc impact when wrapped: 5 partner nodes become real surfaces (consider a "Socios" zone
  or extending n-consoles); new DICT "socios-tier" or extend "identidades" (BUILT now, not
  plan); new SCENARIO "Emisión de credencial de socio → login → módulo"; wires: n-bcp →
  partner surfaces via credential issuance; Cherry Picked Green/Roast/X still deferred.
- Verified end-to-end: tsc+eslint clean; all 5 landings 200, bad slug 404, panel unauth
  redirect, bad login 401; QA partner (estudio) — wrong-node 401, right-node 200,
  invited→active+timestamps confirmed via SQL, authenticated panel rendered in browser;
  QA account deleted (cascade).
(commit: pending)
