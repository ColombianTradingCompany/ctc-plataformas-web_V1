# `mermas-rapida` · Calculadora de mermas · Rápida

> Ficha de la herramienta dentro del componente `herramientas-cafe`. Se lee DESPUÉS del charter
> (`docs/componentes/herramientas-cafe.md`) y del recuento (`../README.md`). Estado al 2026-09-21 (V5.66).
> Esta carpeta es el sitio para los briefs y notas de la conversación de esta herramienta.

| | |
|---|---|
| Estado | Viva · `noindex` a propósito |
| Nivel | default |
| Idioma | es |
| Superficies (`tools.kr/cp/web/dc`) | KR |
| Memoria | sí (puente) |
| Trabajos guardados | 0 |

## Dónde vive

**Servida** (`public/tools/mermas-rapida/`, URL `/tools/mermas-rapida/<archivo>`; la URL plana vieja es un 308):
- mermas-rapida.html

**Fuentes del owner** (`C:\dev\ctc-platforms\reference\html_tools\`, fuera de git):
- (sin fuente del owner en reference/)

## Como interfaz en otras partes de la plataforma

**Hoy:**
- Cherry Picked · Gadgets la nombra.

**Previsto / candidato:**
- Paso rápido del intake de KR (estimar el factor antes de la ficha).

> Regla: una pieza que otra parte de la plataforma reutiliza se EXTRAE a una fuente única (datos puros en
> `src/lib/tools/mermas-rapida/`, o la herramienta embebida tal cual) — nunca una copia que se separa. Tocar el
> componente que la consume exige línea en `docs/ALINEACION.md` §3.

## Abierto

- `noindex` por el modo cacao del cuerpo (`FUERA_DEL_INDICE` en `qa-tools-seo-espejo`). El cacao solo existe como nota de cata: la herramienta no se ampute sin el owner.

## Kick-off de su conversación

El del charter (`docs/componentes/herramientas-cafe.md` § Kick-off) con `<id>` = `mermas-rapida` y:

```
Hoy: Revisar Mermas Rápida (el modo cacao y su salida del índice).
```
