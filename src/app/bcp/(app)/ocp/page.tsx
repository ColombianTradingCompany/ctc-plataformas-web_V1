import { ComingSoon } from "../ComingSoon";

// OCP · Operation Control Panel — scaffolding. El detalle de sus módulos
// (operación interna: equipo, procesos, calendario operativo, indicadores)
// se define en una fase posterior.
export default function BcpOcpPage() {
  return (
    <ComingSoon
      title="OCP · Operation Control Panel"
      description="El módulo de operación interna de CTC: equipo, procesos, calendario operativo e indicadores del día a día."
    />
  );
}
