// Kaffetal Club, producer side. The passport was formerly ACTIVATED by
// redeeming a code (redeemClubCode) or REQUESTED (requestClubPassport). Since
// the 2026-07-17 restructuring, membership is granted AUTOMATICALLY when a
// producer's lot competes in an Arena jornada (see finalizeJornada) — there is
// no producer-side action left here. Kept as a placeholder so the module path
// stays stable; delete when nothing imports it.
export {};
